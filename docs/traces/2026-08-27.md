# 修改跟踪 · 2026-08-27

## T-172642 · L4 · 可恢复专家校准与 M3 准备

### 17:26:42 · 开始

- **状态：** 进行中。
- **修改前基线：** `build_dataset.py` 的小样本计划受 8:1:1 中 `val_count` 限制，100 条只覆盖 S1/S2；批次检查点能恢复正式构建，但没有全能力单元校准、单任务硬预算或失败后继续汇总。M3 只有固定 horizon 的 `MineParkingNet` 与演示训练脚本，没有模型注册表、v1/v2 或可复用 Trainer。
- **目标状态：** 建立与 split 配额解耦的 35 单元校准，按 case 原子落盘并可在中断后核对身份、跳过完成项和续跑；同时交付 M3 注册表、v1/v2 与 Trainer 核心的可测试准备层。
- **边界：** 保留旧 1600 条检查点和既有 v0 接口；校准只生成能力证据，不发布训练数据；不在本轮执行长时间全量校准或正式网络训练，不未经确认新增 PyYAML 依赖。
- **预计范围：** `dataset/`、`scripts/`、`model/`、`training/`、相关测试，以及 `spec/planner_dataset.md`、`spec/model.md`。
- **规范：** `EXPTRAJ-CAL-001..003`、`MODEL-REG-001`、`MODEL-VAR-001/002`、`MODEL-TRAIN-002`。

### 17:48:52 · 完成

- **状态：** 完整完成用户要求的校准工具与 M3 准备层；未启动长时间校准或正式训练。
- **数据校准：** 小样本 8:1:1 计划可能漏场景 → `build_calibration_cases` 直接等额枚举 35 个专家能力单元；批次级恢复 → 每个 case 独立 worker、硬墙钟预算、原子终态 JSON、identity、`run_state.json`、`report.json` 和 `cells.csv`。中断后跳过完成 case，只重做当前未落盘 case；单 case 失败/超时继续后续单元。
- **组件所有权：** `scripts/build_dataset.py` 内嵌组件工厂 → `dataset/components.py` 共享所有者，正式构建保持 `build_components` 兼容包装。
- **M3 模型：** 仅固定 horizon v0 → 注册表 `net-v0/v1/v2`；v1 增加条件 GRU 与终止 logits，v2 增加 U-Net 跳连空间 token 和 goal/state 交叉注意力；v0 既有导入和 Tensor 前向不变。
- **M3 训练：** 演示脚本内循环 → `training/Trainer` 核心拥有训练/验证、early stopping、模型/超参数兼容恢复和原子 best/last checkpoint。YAML、曲线和 ADE/FDE 报告按范围推迟到 P3.2/P3.3。
- **验证映射：** 全仓 210 项通过；最终恢复/预算/Trainer 调整后 9 项定向通过；真实 S1 独立 worker 在 5 秒预算内成功；compileall、两个 Spec 检查和 `git diff --check` 通过。
- **契约状态：** `EXPTRAJ-CAL-001..003`、`MODEL-REG-001`、`MODEL-VAR-001/002`、`MODEL-TRAIN-002` 均有实现与自动化证据并标记 ✅；正式模型收敛不由这些结构性状态代替。
- **迁移与回退：** 不修改旧 1600 条检查点；校准输出为独立新目录。相同身份可续跑，参数/车辆/计划变化拒绝复用。回退可移除新增校准、注册表、变体与 training 模块，并恢复脚本内组件工厂，不涉及数据迁移或不可逆操作。
- **知识与记录：** 已更新 `README.md`、`ARCHITECTURE.md`、`LEARNING.md`、`docs/PLAN.md`、两个 Spec；worklog 使用同一 `T-172642`。

## T-175859 · L4 · M3 配置化训练与开环评估

### 17:58:59 · 开始

- **状态：** 进行中；不读取、不轮询正在运行的专家能力校准。
- **修改前基线：** `Trainer` 已有训练/验证、early stopping 和 checkpoint，但只有 Python 对象入口；没有 YAML 配置、训练曲线、训练报告、ADE/FDE/航向误差实现或多 checkpoint 开环比较入口。
- **目标状态：** 安全加载可复现训练 YAML，训练产物包含历史、曲线和验证集开环报告；独立评估入口从兼容 checkpoint 恢复模型，对统一验证集生成可机读指标与 PNG/PDF 对比图。
- **边界：** 不运行正式训练，不读取未完成校准，不据合成证据宣称 v1/v2 优于 v0；完整 V3 中的实际闭环轨迹等待数据、训练和闭环实验。
- **预计范围：** `training/`、`metrics/`、`scripts/`、`configs/training/`、相关测试、PyYAML 依赖说明，以及 MODEL Spec/README/ARCHITECTURE/PLAN。
- **规范：** `MODEL-CONFIG-001`、`MODEL-REPORT-001`、`MODEL-EVAL-001/002`。

### 17:58:59 · 范围升级

- **原状态 → 新状态：** L3 标准功能 → L4 依赖与跨层变更。
- **原因与证据：** 当前 conda 环境无 YAML 解析库；PyYAML 官方文档要求对配置使用 `safe_load` 以限制对象构造。选择显式 PyYAML 依赖，不实现维护和安全成本更高的自制 YAML 子集。
- **指标决策：** ADE/FDE 使用目标 mask 的有效点和每条样本最后有效点；航向误差用 `atan2(sin Δ, cos Δ)` 环绕到 `[-π,π]`，预测 horizon 短于目标有效长度时明确失败，避免截断造成虚假改善。

### 18:18:23 · 完成

- **状态：** 完整完成不依赖校准结果的 M3 配置化训练与开环评估框架；未执行正式训练，也未读取运行中的校准状态。
- **训练入口：** Python 对象配置 → `training/config.py` 用 PyYAML SafeLoader 和严格 schema 解析相对路径；`training/runner.py` 串联注册表模型、共享局部坐标 batch、Trainer、best checkpoint 验证和报告。每 epoch 控制台输出并原子更新 history，结束生成 best/last、report 和 PNG/PDF 曲线。
- **开环评估：** 无统一指标/入口 → `metrics/open_loop.py` 统一 ADE/FDE/环绕航向 MAE 与短 horizon 门禁；`training/checkpoint.py` 只读恢复 Trainer schema v1；`scripts/eval_openloop.py` 在同一 val NPZ 上比较多个 checkpoint 并输出 JSON 与 PNG/PDF 指标图。
- **依赖与安全：** 新增 `requirements-training.txt`，指定 PyYAML 6.x 与 matplotlib 3.x；实际环境安装 PyYAML 6.0.3。YAML 禁止任意对象标签、未知字段及非 JSON 可序列化模型配置；输入数据/checkpoint 不被修改。
- **验证：** 新行为先有缺失模块失败证据；12 项首轮定向通过，兼容性收尾后 9 项定向通过；全仓 221 项通过；CLI help、compileall、MODEL Spec 检查、`pip check` 与 `git diff --check` 通过。
- **契约与知识：** `MODEL-CONFIG-001`、`MODEL-REPORT-001`、`MODEL-EVAL-001/002` 均有自动化证据并标记 ✅；同步 README、ARCHITECTURE、LEARNING 和 PLAN，P3.2 完成，P3.3 保留正式优劣验证与完整 V3 闭环图。
- **迁移、回退与剩余风险：** 既有 `scripts/train.py` 和 v0 接口保持不变；新增入口可通过移除 training/metrics/viz 新文件与 PyYAML 依赖回退，不涉及数据迁移。正式数据完成前不能宣称 v1/v2 收敛或优于 v0，完整 V3 仍需闭环实际轨迹。
- **工作日志：** 同编号记录见 `docs/worklogs/2026-08-27.md`。
