# 修改跟踪 · 2026-08-30

## T-113438 · L4 · 变长轨迹训练流程修正

### 11:34:38 · 开始

- 状态：进行中。
- 修改前基线：v7 `net-v1` teacher-forcing train loss 已降至 0.105，但 train/val 自由滚动 ADE/FDE 仍为 3.459/5.853m 与 3.502/5.923m；train/val/test 停止命中率均为 0，训练样本按固定 task ID 批次顺序重复。
- 目标状态：训练样本每 epoch 可复现地样本级打乱；终止正类不再被长前缀负类淹没；teacher forcing 按配置逐步衰减；每 epoch 持久化 train/val 自由滚动和停止指标。
- 边界：不改变 v7 专家数据、模型输入输出张量或规划器；训练语义和 checkpoint 兼容字段改变，旧 `net-v1` checkpoint 不允许静默续训，正式复训写入新输出目录。
- 预计影响：`model/` 解码与损失、`training/` 配置/Trainer/报告、v7 YAML、测试、模型 Spec 与运行文档。
- 调研依据：PyTorch `BCEWithLogitsLoss` 官方文档建议以负/正样本比例设置正类权重；PyTorch 可复现性文档要求显式生成器；Bengio 等 2015 scheduled sampling 原始论文用逐步从真实前点过渡到模型前点来缩小训练/推理差异。

### 11:49:00 · 完成

- 状态：完整完成训练流程修正与真实数据 smoke；30 epoch 正式复训按用户约定未由 Agent 启动。
- 损失与解码：`model/network.py` 从未平衡停止 BCE → 有效前缀内动态负/正加权；`model/variants.py` 从全程 teacher forcing → 可复现计划采样，预测反馈在再次输入前停止梯度，公开输出形状不变。
- 数据与训练：`training/data.py`/`training/trainer.py` 从固定 batch 顺序和仅 loss 历史 → seed+epoch 确定性样本级重排、1.0→0.2 线性 teacher-forcing 调度，以及逐 epoch train/val 自由滚动 ADE/FDE、停止命中率和长度 MAE。
- 报告与配置：`training/reporting.py`/`training/runner.py` 从单 loss 曲线 → 四面板诊断图与关键控制台指标；v7 配置迁移到 `runs/training/v7-flow-v2/`，旧 checkpoint 因新增训练语义兼容字段被明确拒绝，可用同目录 `last.pt` 恢复新运行。
- 验收：249 项全量 unittest 通过，最终触达范围 7 项定向回归通过，`git diff --check` 无错误；v7 全量数据 1 epoch smoke 生成 best/last、JSON、PNG/PDF，val ADE/FDE 0.861/1.633m、停止命中率 99.67%。
- 剩余风险：smoke 的预测长度 MAE 仍为 42.31 点，需正式训练观察是否随 epoch 收敛；若自由滚动指标停滞或 T3/S4/S6 残差仍集中，再升级模型空间编码，不在本次流程修正中提前改模型。
- 契约与知识：`MODEL-LOSS-002`、`MODEL-TRAIN-003`、`MODEL-REPORT-002` 已完成；README、ARCHITECTURE、LEARNING 已同步。任务索引见 `docs/worklogs/2026-08-30.md` 的 `T-113438`。
- 安全与迁移：未修改或删除专家数据，未覆盖旧训练产物，未执行 commit/push；回退可停用新 YAML 字段或切回旧代码/输出目录，但旧 checkpoint 不得用于新语义续训。

## T-130752 · L4 · 训练策略定型与可复现调优

### 13:07:52 · 开始

- 状态：进行中。
- 修改前基线：`v7-flow-v2/net-v1` 在第 4 epoch 达到 val ADE/FDE 0.585/1.109m，但 `patience=5` 于第 9 epoch 结束，teacher-forcing 比例仍为 0.573；停止命中率 100%，val/test 长度偏差为 -31.44/-27.15 点，T3 为 -68.88 点。
- 目标状态：一次性定型可解释、可恢复、可人工调优的训练流程；课程调度完成前不得早停；停止监督学习终点边界而非依赖极端单点正类加权；候选方案必须用真实 v7 消融证据筛选后才成为推荐配置。
- 边界：本轮不修改专家数据、任务分布、网络空间编码或闭环控制；不覆盖 `v7-flow-v2` 结果；消融写入独立目录，正式长训练仍交给用户执行。
- 调研与证据：PyTorch 官方文档说明 `pos_weight>1` 提高召回，解释当前 100% 命中但系统性提前；Keras 官方 EarlyStopping 提供 `start_from_epoch` 以允许预热；scikit-learn 官方阈值调优要求在独立验证集上优化，不能用训练集代替。现有 best 的 val 阈值扫描在 0.7 仅把长度 MAE 从 32.70 降至 27.04，证明阈值不是主根因。
- 预计影响：`model/network.py` 的停止目标、`training/trainer.py` 的课程感知早停与历史、训练配置/报告、定向测试、模型 Spec 和人工调优文档。

### 13:22:00 · 消融范围调整

- 原计划：以四轮消融直接选择累计停止监督与学习率。
- 新状态：`1e-3` 累计组验证振荡且淘汰；`3e-4` 累计组四轮 ADE/FDE 连续改善至 0.476/1.006m，但停止覆盖仅 52.7%、长度 MAE 113.98 点，阈值扫描最佳仍为 43.26 点，短训练不足以判定累计边界监督失败。
- 调整原因：轨迹质量和停止质量呈不同收敛速度；用四轮阈值结果定型会把欠训练误判为方法无效。低学习率累计组延长至最多 12 epoch；若课程后段长度仍不合格则淘汰该模式，转向终点监督的有界正类权重，不扩大到网络结构。

### 13:46:15 · 完成

- 状态：完整完成训练策略定型、真实消融、自动部署校准和人工调优指南；正式 v3 30 epoch 长训练按边界留给用户执行。
- 方案收敛：单终点全平衡+`1e-3` 从默认方案降为历史复现；累计停止+`1e-3` 因验证振荡淘汰；累计停止+`3e-4` 在同 seed 12 epoch 达到 val/test ADE/FDE 0.312/0.552m 与 0.293/0.527m，成为冻结方案，不再试第三套权重。
- 损失：`model/network.py` 从单终点前缀监督 → 可配置累计终点后缀监督；正式配置关闭会扭曲类别先验的全平衡，旧模式仍可复现。
- 训练：`training/trainer.py` 从课程期间即可累计 patience → 前 15 轮 warmup、teacher forcing 第 16 轮到 0.2 后才激活 early stopping；checkpoint 恢复重建同一 stale 状态并拒绝 deployment 恢复。
- 部署：新增 `training/stop_calibration.py`；best 在独立 val 固定网格上最小化长度 MAE，原 best/last 不变，另存不可续训 `deployment.pt` 与完整校准 JSON。消融选择阈值 0.20，val/test 长度 MAE 23.84/26.83 点、停止率均 100%。
- 配置与使用：正式 v1/v2 输出迁移到 `runs/training/v7-flow-v3/`；调优候选 YAML、`docs/training_guide.md`、README、ARCHITECTURE 和 LEARNING 已同步。旧 v2 checkpoint 可读作对照，但不得跨训练语义恢复。
- 验证：`MODEL-LOSS-003`、`MODEL-TRAIN-004`、`MODEL-CAL-001` 证据完整；Spec 检查、6 份配置加载、258 项全量测试、最终 19 项定向回归和 diff check 均通过；v3 全量数据 1 epoch smoke 生成 best/last/deployment、校准 JSON 和 PNG/PDF。
- 剩余风险：正式 v3 尚未执行；T3/S6 仍是相对最难分组，正式结果须按指南门槛验收，未达到时仅按单变量顺序调整，不回到多参数同时试错。
- 范围与安全：未修改、删除或重建专家数据，未改变模型公开轨迹张量和闭环控制；未覆盖 v2/tuning 结果，未 commit/push。任务索引见 `docs/worklogs/2026-08-30.md` 的 `T-130752`。
- 记录更正：前一检查点标题 `13:22:00` 未由系统时钟读取，应视为“时间未记录”的顺序标记；检查点内容与消融证据不变。

## T-161857 · L4 · 正式网络闭环评测

### 16:18:57 · 开始

- 状态：进行中；用户要求按既定计划进入下一阶段并判断整体闭环效果。
- 修改前基线：正式 `deployment.pt` 已完成开环评测，但 `scripts/run_closed_loop.py` 仍按旧裸权重和 20 点模型加载；网络模式固定使用通用 40m 环境，未恢复数据集 S1–S9 场景、相邻占用、噪声、BEV 和车位容差，且未把当前目标写入 target 通道，因此无法产生可信闭环结论。
- 目标状态：建立 `LOOP-EVAL-001`，从 dataset manifest、schema v2 task metadata 和 Trainer checkpoint 确定性恢复每个任务；输出整体、分组和逐回合闭环结果，以实际场景阈值判定成功。
- 边界：不修改训练权重、专家数据、模型结构或 MPC 参数；T5 动态障碍注入仍不纳入本轮，结果只代表当前二维静态场景闭环。
- 预计影响：`runtime/` 评测编排、`scripts/run_closed_loop.py` CLI、定向测试、`spec/loop.md`、README/ARCHITECTURE 使用与数据流说明。

### 16:36:08 · 方案调整与完成

- 原状态：旧入口逐控制周期（K=1）重建 320 点整段轨迹；同批 4 条闭环 0/4 成功并全部振荡。实现初版另误用了不存在的 `BEVConfig.to_metadata()`，在首回合前明确失败。
- 调整：BEV 契约改为直接核对 resolution/extent/shape；只比较 K=5/10/20，相同 4 条成功数为 1/2/1，因而将 K=10 固定为当前 deployment 默认并在报告中记录，未改 MPC 权重或训练参数。
- 状态：完整完成可信网络闭环评测入口和分层诊断；模型闭环未通过准入。600/600 条 val/test 任务身份均能复原；34 条 val 场景×任务分层结果为 32.4% 成功、29.4% 碰撞，30 条 S9 为 50.0% 成功、33.3% 碰撞，合并 64 条为 40.6% 成功、31.3% 碰撞。
- 变更映射：旧裸 state_dict/20 点/通用环境入口 → `experiments/closed_loop_evaluation.py` 使用 Trainer checkpoint、manifest 与 task metadata 复原场景并写结构化报告；`NetworkSource` 不更新 target 通道 → 每回合 begin 写入当前目标；README 旧占位命令 → 可直接运行的 val/test 命令。
- 结论：MPC 跟踪 RMS 为 0.047/0.043m，碰撞与振荡不是跟踪误差主导；S3 为 0% 成功/100% 碰撞，S4/S6/S8 为 0% 成功，S9/T4 为 20% 成功。下一阶段应增加网络轨迹安全约束/安全过滤与闭环状态分布训练，不继续调 early stopping。
- 验证：`LOOP-EVAL-001` ✅；263 项全量 unittest、Spec 检查、py_compile、diff check 通过；真实 deployment 完成 K=1/5/10/20 冒烟、34 val 与 30 S9 分层闭环报告。
- 契约与回退：输入 NPZ、manifest 和 checkpoint 只读且无迁移；恢复旧脚本即可回退。T5 动态事件仍未注入并在报告显式标记，不将结果描述为动态避障或实车安全证据。
- 文档：README、ARCHITECTURE、LEARNING 与 `spec/loop.md` 已同步；任务索引见 `docs/worklogs/2026-08-30.md` 的 `T-161857`。

## T-164412 · L4 · 碰撞安全约束与闭环状态训练

### 16:44:12 · 开始

- 状态：进行中。
- 修改前基线：正式网络在 64 条分层闭环中成功率 40.6%、碰撞率 31.3%，而 MPC 跟踪 RMS 约 0.045m；当前训练只优化中心轨迹/停止误差，离线数据只包含专家访问状态，运行时也会直接执行未经几何审查的网络轨迹。
- 目标状态：建立 `SAFE-LOSS-001`、`SAFE-DATA-001`、`SAFE-SHIELD-001`，分别覆盖完整车体连续扫掠训练损失、学习器闭环状态的专家重标注、以及可审计的运行时安全回退；三者只依赖统一几何/元数据接口，不出现 S3/S4 等场景特例。
- 边界：不修改原专家数据和既有 checkpoint，不调整 MPC 或专家可达集合；正式规模恢复数据采集与长训练由用户在烟雾验证后执行；二维静态环境安全不得外推为实车认证。
- 预计影响：训练损失/配置/报告、恢复数据构建、运行时轨迹源、闭环评测 CLI、测试、`spec/safety_training.md` 和架构/使用文档。
- 方案依据：DAgger 用学习器诱导状态上的专家标签处理模仿学习分布偏移；ChauffeurNet 证明纯行为克隆不足并使用扰动/碰撞等合成状态与不良事件损失；UniAD 对 ego bounding box 使用多安全余量碰撞损失；运行时安全保障采用“审查不可信控制输出并切换可信安全控制器”的独立防线。

### 17:14:16 · 完成

- 状态：完整完成通用设计、实现、自动验证和真实 smoke；正式 240 源任务恢复采集、30 epoch 安全训练及成对闭环验收按长耗时边界留给用户执行。
- 训练范围：中心点/停止损失 → `training/safety.py` 基于 schema v2 车辆与 BEV 元数据的完整矩形、连续扫掠 occupancy/越界损失；`Trainer` 记录原始 collision loss，并把权重、几何、采样和扫掠参数纳入 checkpoint 恢复语义。4 条真实专家轨迹安全损失为 0，避免把既有安全标签误判为碰撞。
- 数据范围：仅专家起点 → `dataset/recovery.py` 和 `scripts/build_recovery_dataset.py` 选择固定步长、碰撞前最后安全点、位置或航向偏离状态，从当前状态重规划并重新计算机动/可行性证据；检查点绑定输入计划、deployment SHA-256、车辆及全部参数，报告含分组和失败原因。真实 smoke 生成 1 条 43 点恢复样本并合并为 2401 条训练集。
- 运行时范围：未经审查的 NetworkSource → `SafetyShieldSource` 对完整矩形连续扫掠执行场景无关门禁，不安全时从当前状态调用专家回退，并输出干预率/原因/回退失败；闭环实际碰撞由四角离散检查升级为无安全余量的完整矩形连续扫掠，门禁独立使用配置安全余量。真实 1 回合完成 6 次检查、0 次误干预、0 碰撞。
- 泛化边界：实现未出现 S1–S9 或 T1–T5 条件分支；新增场景通过 `BEVConfig`、occupancy、`VehicleConfig`、环境碰撞接口和 selected goal 接入。恢复样本是训练专用，不用于替代原 val/test 或可信场景评测。
- 验证：`SAFE-LOSS-001/SAFE-DATA-001/SAFE-SHIELD-001` ✅；Spec 检查通过；276 项全量 unittest 通过，随后新增的碰撞前零偏离回归及最终 27/6 项定向检查通过；py_compile、YAML 解析和 diff check 通过。
- 契约/迁移/回退：旧 YAML 的碰撞权重默认 0，保持原训练行为；原数据和 v7 checkpoint 未覆盖，v8 使用新目录且不得从 v7 last 续训；训练可把安全权重设 0/移除恢复集回退，运行时可选 `safety-mode none`，历史纯网络与门禁报告保持分离。
- 安全与隐私：仅验证本地二维静态环境；不保证漏检、定位误差、动态障碍、制动距离或实车功能安全。T5 仍未注入动态事件。任务索引见 `docs/worklogs/2026-08-30.md` 的 `T-164412`。
