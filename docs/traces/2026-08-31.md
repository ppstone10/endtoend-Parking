# 修改跟踪 · 2026-08-31

## T-004952 · L4 · 连续净空训练与可回退安全门禁

### 00:49:52 · 开始

- 状态：进行中。
- 修改前基线：v8-safety-v2 从随机权重训练 21 轮后开环 ADE/FDE 为 0.315/0.535m，但同 34 条纯网络闭环成功率 0%、碰撞率 55.9%，较 v7 的 32.4%/29.4% 明显退化；v7/v8 在同一 val 的旧安全损失仅 0.293994→0.293754，梯度非零分量约 6%、38 个 batch 中 4 个零梯度。安全门禁完整评测在第 14 条因当前状态已侵入专家 0.2m 规划余量而回退失败并中断。
- 目标状态：`SAFE-LOSS-002` 用连续净空距离场替代正式训练中的二值最大碰撞指示；`SAFE-TRAIN-001` 从已准入 v7 权重初始化并用原始/普通恢复/碰撞回溯三路确定性 batch；`SAFE-SHIELD-002` 在执行每个控制前保持专家可回退状态，并把无法安全执行记录为回合失败而非批次异常。
- 边界：不修改专家数据、场景、规划器可达集合、MPC 权重或车辆尺度；不新增 SciPy/OpenCV 等依赖；旧 v7/v8 checkpoint 和报告只读，新消融与后续正式输出使用独立目录。
- 消融准入：同 seed、同 v7 初始化、短周期分别比较旧损失+三路采样、净空损失+均匀采样、净空损失+三路采样；完整方案相对冻结 v7 的同口径净空损失至少下降 10%，同时 val ADE≤0.40m、FDE≤0.80m，否则不交付长训练命令。
- 调研依据：[CHOMP](https://publications.ri.cmu.edu/chomp-covariant-hamiltonian-optimization-for-motion-planning) 使用预计算距离场与近障碍势函数产生连续避障梯度；[DAgger](https://proceedings.mlr.press/v15/ross11a.html) 要求在学习器诱导状态分布上学习；[PyTorch Sampler](https://docs.pytorch.org/docs/main/data.html) 支持生成器驱动的确定性采样；[NASA RTA 建议](https://ntrs.nasa.gov/citations/20220015734) 要求监视器覆盖系统状态并在接近不安全状态时切换可信备份。
- 预计影响：`training/safety.py`、batch/配置/runner/checkpoint、运行时 source/engine/评测、三组测试、安全 Spec、训练配置与架构/使用文档。

### 01:07 · 训练与门禁结构完成

- `training/safety.py` 增加无外部依赖的截断有符号净空场和连续净空缺口损失；旧 `occupancy_max` 保留为兼容消融，正式配置使用 `clearance_field`。
- `initialize_from` 只加载模型权重并记录 checkpoint SHA-256，与严格 `resume_from` 互斥；恢复数据三池确定性 batch 作为可选采样策略实现，验证集不重采样。
- 门禁在每个控制落地前审查下一状态扫掠；无法安全回退用专用 `SafetyStopError` 结束单回合，网络自身 `RuntimeError` 保持透传。检查中同时修正了回退失败消息引用错误局部变量的问题。

### 01:25 · 小规模消融决策

- 三组均使用 seed `20260830`、v7 `best.pt` 权重初始化、2658 条最终恢复训练集、300 条原 val、3 epoch；统一评估入口为 `scripts/evaluate_safety_ablation.py`，产物为 `runs/training/v9-safety-ablation/ablation_report.json`。
- 同一连续净空口径：冻结 v7 `1.102705`；旧损失＋三池均衡 `1.306765`（恶化 18.5%）；净空＋均匀 shuffle `0.115092`（下降 89.6%，ADE/FDE `0.2283/0.5717m`）；净空＋三池均衡 `0.128024`（下降 88.4%，ADE/FDE `0.2629/0.5904m`）。
- 决策更正：目标阶段曾计划把三路均衡作为正式方案；消融证明其不优于恢复数据真实占比，故正式 `configs/training/net-v1-safe.yaml` 关闭强制均衡，但保留实现与测试供后续独立实验。正式流程固定为 v7 权重初始化、净空损失、teacher forcing `0.2`、最多 15 epoch、新 v9 输出目录。
- 10 条分层真实门禁 smoke：成功率 30%、碰撞率 0%，共 484 次轨迹检查、4830 次执行前转移检查、55 次轨迹回退、0 次回退失败、0 次安全停止；7 条失败均为振荡，门禁安全通过不代表纯网络闭环准入。

### 01:32:11 · 完成

- 状态：完整完成本轮训练/门禁结构修正与小规模消融；正式长训练留给用户执行，不修改旧数据、旧 checkpoint、场景、规划器或控制器。
- 修改映射：二值 occupancy 峰值正式训练 → 连续净空场缺口；随机重新训练 → v7 `best.pt` 仅权重初始化；默认困难样本过采样计划 → 三池能力保留但按消融关闭；轨迹级回退后才发现不可恢复 → 每控制执行前转移复核与专用 `safety_stop`；门禁异常混用 `RuntimeError` → `SafetyStopError` 与真实模型异常透传；单一门禁干预口径 → 保留轨迹干预率并新增转移阻断率。
- 主要文件：`training/`、`runtime/`、`experiments/closed_loop_evaluation.py`、`scripts/evaluate_safety_ablation.py`、`configs/training/net-v1-safe.yaml`、三组 tuning YAML、相关测试与安全/架构/使用文档。
- 验证：受影响 Python `py_compile` 通过；正式配置解析到 v9 输出、v7 初始化、`clearance_field`、真实占比 shuffle 和 15 epoch；36 项定向测试通过；全仓 292 项测试通过；报告口径修正后 10 项门禁相关测试再次通过；`git diff --check` 通过。
- 产物：`runs/training/v9-safety-ablation/ablation_report.json` 与三组训练报告；`runs/closed-loop/v9-safety-ablation/clearance-uniform/val-shield-10/report.json`。训练产物不进入源码提交。
- 剩余风险：小样本门禁只证明已知静态环境下未碰撞和批量不中断；30% 成功率且倒车样本均振荡，正式 v9 仍须完成全量训练、统一净空复算，以及同索引纯网络/门禁闭环准入。
- Worklog：[T-004952](../worklogs/2026-08-31.md#t-004952--004952013211--l4--连续净空训练与可回退安全门禁)。
