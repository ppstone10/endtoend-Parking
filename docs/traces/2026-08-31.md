# 修改跟踪 · 2026-08-31

## T-004952 · L4 · 连续净空训练与可回退安全门禁

### 00:49:52 · 开始

- 状态：进行中。
- 修改前基线：v8-safety-v2 从随机权重训练 21 轮后开环 ADE/FDE 为 0.315/0.535m，但同 34 条纯网络闭环成功率 0%、碰撞率 55.9%，较 v7 的 32.4%/29.4% 明显退化；v7/v8 在同一 val 的旧安全损失仅 0.293994→0.293754，梯度非零分量约 6%、38 个 batch 中 4 个零梯度。安全门禁完整评测在第 14 条因当前状态已侵入专家 0.2m 规划余量而回退失败并中断。
- 目标状态：`SAFE-LOSS-002` 用连续净空距离场替代正式训练中的二值最大碰撞指示；`SAFE-TRAIN-001` 从已准入 v7 权重初始化并用原始/普通恢复/碰撞回溯三路确定性 batch；`SAFE-SHIELD-002` 在执行每个控制前保持专家可回退状态，并把无法安全执行记录为回合失败而非批次异常。
- 边界：不修改专家数据、场景、规划器可达集合、MPC 权重或车辆尺度；不新增 SciPy/OpenCV 等依赖；旧 v7/v8 checkpoint 和报告只读，新消融与后续正式输出使用独立目录。
- 消融准入：同 seed、同 v7 初始化、短周期分别比较旧损失+三路采样、净空损失+均匀采样、净空损失+三路采样；完整方案相对冻结 v7 的同口径净空损失至少下降 10%，同时 val ADE≤0.40m、FDE≤0.80m，否则不交付长训练命令。
- 调研依据：[CHOMP](https://publications.ri.cmu.edu/chomp-covariant-hamiltonian-optimization-for-motion-planning) 使用预计算距离场与近障碍势函数产生连续避障梯度；[DAgger](https://proceedings.mlr.press/v15/ross11a.html) 要求在学习器诱导状态分布上学习；[PyTorch Sampler](https://docs.pytorch.org/docs/main/data.html) 支持生成器驱动的确定性采样；[NASA RTA 建议](https://ntrs.nasa.gov/citations/20220015734) 要求监视器覆盖系统状态并在接近不安全状态时切换可信备份。
- 预计影响：`training/safety.py`、batch/配置/runner/checkpoint、运行时 source/engine/评测、三组测试、安全 Spec、训练配置与架构/使用文档。

### 01:07 · 训练与门禁结构完成

- `training/safety.py` 增加无外部依赖的截断有符号净空场和连续净空缺口损失；旧 `occupancy_max` 保留为兼容消融，正式配置使用 `clearance_field`。
- `initialize_from` 只加载模型权重并记录 checkpoint SHA-256，与严格 `resume_from` 互斥；恢复数据三池确定性 batch 作为可选采样策略实现，验证集不重采样。
- 门禁在每个控制落地前审查下一状态扫掠；无法安全回退用专用 `SafetyStopError` 结束单回合，网络自身 `RuntimeError` 保持透传。检查中同时修正了回退失败消息引用错误局部变量的问题。

### 01:25 · 小规模消融决策

- 三组均使用 seed `20260830`、v7 `best.pt` 权重初始化、2658 条最终恢复训练集、300 条原 val、3 epoch；统一评估入口为 `scripts/evaluate_safety_ablation.py`，产物为 `runs/training/v9-safety-ablation/ablation_report.json`。
- 同一连续净空口径：冻结 v7 `1.102705`；旧损失＋三池均衡 `1.306765`（恶化 18.5%）；净空＋均匀 shuffle `0.115092`（下降 89.6%，ADE/FDE `0.2283/0.5717m`）；净空＋三池均衡 `0.128024`（下降 88.4%，ADE/FDE `0.2629/0.5904m`）。
- 决策更正：目标阶段曾计划把三路均衡作为正式方案；消融证明其不优于恢复数据真实占比，故正式 `configs/training/net-v1-safe.yaml` 关闭强制均衡，但保留实现与测试供后续独立实验。正式流程固定为 v7 权重初始化、净空损失、teacher forcing `0.2`、最多 15 epoch、新 v9 输出目录。
- 10 条分层真实门禁 smoke：成功率 30%、碰撞率 0%，共 484 次轨迹检查、4830 次执行前转移检查、55 次轨迹回退、0 次回退失败、0 次安全停止；7 条失败均为振荡，门禁安全通过不代表纯网络闭环准入。

### 01:32:11 · 完成

- 状态：完整完成本轮训练/门禁结构修正与小规模消融；正式长训练留给用户执行，不修改旧数据、旧 checkpoint、场景、规划器或控制器。
- 修改映射：二值 occupancy 峰值正式训练 → 连续净空场缺口；随机重新训练 → v7 `best.pt` 仅权重初始化；默认困难样本过采样计划 → 三池能力保留但按消融关闭；轨迹级回退后才发现不可恢复 → 每控制执行前转移复核与专用 `safety_stop`；门禁异常混用 `RuntimeError` → `SafetyStopError` 与真实模型异常透传；单一门禁干预口径 → 保留轨迹干预率并新增转移阻断率。
- 主要文件：`training/`、`runtime/`、`experiments/closed_loop_evaluation.py`、`scripts/evaluate_safety_ablation.py`、`configs/training/net-v1-safe.yaml`、三组 tuning YAML、相关测试与安全/架构/使用文档。
- 验证：受影响 Python `py_compile` 通过；正式配置解析到 v9 输出、v7 初始化、`clearance_field`、真实占比 shuffle 和 15 epoch；36 项定向测试通过；全仓 292 项测试通过；报告口径修正后 10 项门禁相关测试再次通过；`git diff --check` 通过。
- 产物：`runs/training/v9-safety-ablation/ablation_report.json` 与三组训练报告；`runs/closed-loop/v9-safety-ablation/clearance-uniform/val-shield-10/report.json`。训练产物不进入源码提交。
- 剩余风险：小样本门禁只证明已知静态环境下未碰撞和批量不中断；30% 成功率且倒车样本均振荡，正式 v9 仍须完成全量训练、统一净空复算，以及同索引纯网络/门禁闭环准入。
- Worklog：[T-004952](../worklogs/2026-08-31.md#t-004952--004952013211--l4--连续净空训练与可回退安全门禁)。

## T-190435 · L3 · 安全微调闭环退化根因定位与待决策方向

### 19:04:35 · 开始

- 状态：进行中。用户要求定位"开环通过但闭环长期不过准入"的根因，并记录待决策方向。
- 修改前基线：v9(w=0.5) 正式训练后开环/净空全部达标，但纯网络闭环 2.9% 成功/44.1% 碰撞，较 v7 的 32.4%/29.4% 明显退化；按训练指南应单独比较 `collision_loss_weight=0.25/1.0`。
- 目标状态：用单变量实验隔离"净空损失权重""teacher forcing 课程""恢复数据""滚动重规划机制"四类假设，确定安全微调破坏 v7 闭环能力的根因。
- 边界：不修改专家数据、场景、规划器、MPC、门禁；只新增单变量训练配置与独立输出目录；长耗时训练由用户执行、Agent 复核，本轮 Agent 直接执行 B1/B2。

### 19:24 · 五模型对照完成

- 消融序列：v9(w=0.5) → v9b(w=0.25) → v9b(w=1.0) → v9c(w=0 关闭净空) → v9c(TF 课程 1.0→0.2 15 epoch)。
- 统一净空（同一 val、margin 0.1）：v7 1.1027；w0.5 0.0999(-90.9%)；w0.25 0.2148(-80.5%)；w1.0 0.0497(-95.5%)；w0 1.4683(+33%，开环最优 ADE/FDE 0.172/0.300)；TF课程 0.1090(-90.1%)。
- 纯网络闭环（34 条同索引）：v7 32.4%/29.4%；w0.5 2.9%/44.1%；w0.25 17.6%/47.1%；w1.0 5.9%/47.1%；w0 14.7%/50.0%；TF课程 8.8%/44.1%。
- 门禁闭环：全部 0 碰撞、0-1 回退失败，成功率 w0.5 41.2%、w0.25 41.2%、w1.0 38.2%、w0 44.1%、TF课程 41.2%。

### 19:24 · 根因结论

- 决定性证据：v7 成功 11 条在全部 5 个安全模型下一致失败（8 振荡+3 碰撞）→ 排除净空权重与 TF 课程两个单变量，根因在"安全微调路径本身"。
- 未解瓶颈：振荡 100% 占门禁剩余失败；终态位置 0.2-1.2m 但航向偏差 15-117°，指向滚动重规划时终点航向不收敛。
- 两个待决策方向：A) 恢复数据（258 条/9.7%）混入是元凶 → 用纯 v7 原始数据微调隔离；B) 振荡是滚动重规划机制问题 → 回放典型振荡样本分析航向切换。
- 系统性梳理固化到 `docs/closed_loop_diagnosis.md`（开环/闭环验收线、根因、待决策方向、解决路线、验收速查）。

### 状态

- 完整完成本轮单变量隔离与根因定位；两个待决策方向已记录待用户选择后进入下一步实验。
- 修改：`configs/training/net-v1-safe-w0.yaml`、`net-v1-safe-tfcurriculum.yaml`（w0.25/w1.0 为上一轮新增）；产物 `runs/training/v9b-*`、`runs/training/v9c-*`、`runs/closed-loop/v9b-*`、`runs/closed-loop/v9c-*`、`runs/training/v9c-collision-w0/net-v1/clearance_compare.json`；诊断文档 `docs/closed_loop_diagnosis.md`。
- Worklog：[T-190435](../worklogs/2026-08-31.md#t-190435--190435192404--l3--collision_loss_weight-与课程消融后安全微调根因定位)。

## T-211000 · L2 · 方向 B 振荡机制定论

### 21:10 · 开始

- 状态：进行中。用户要求先处理方向 B（振荡是否为滚动重规划机制问题），之后再处理方向 A。
- 修改前基线：`docs/closed_loop_diagnosis.md` 已固化两个待决策方向；方向 B 假设为"车辆在目标附近但每 10 步重规划时网络终点航向与目标不一致导致方向反复切换"。
- 目标状态：用逐重规划回放确定振荡具体机制，区分"重规划机制自身" vs "网络近端预测退化"。

### 21:25 · 诊断证据

- 新增 `scripts/diagnose_oscillation.py`（单模型 5 样本逐重规划剖析）→ 发现轨迹长度随接近目标单调缩短到 37 点下限、终点航向在目标两侧 ±2.8 rad 反复切换。
- 新增 `scripts/diagnose_oscillation_focus.py`（量化车辆距目标 vs 预测终点收敛性）→ 距目标 >5m 时预测终点误差 0.25–0.5m、航向 <0.1 rad；<2m 时终点误差膨胀 1.5–2.7m、航向 ±2~3 rad。
- 新增 `scripts/diagnose_oscillation_compare.py`（v7 成功 vs v9 振荡 7 样本同索引对比）→ 决定性：车辆距目标 <3m 时，v7 预测轨迹 3.7→1.7m 稳定、终点误差 0.8–1.5m、航向误差 0.06–0.23 rad、flips=0；v9 轨迹骤缩 0.7–1.9m、终点误差膨胀 1.5–2.5m、航向误差膨胀 0.4–1.3 rad、flips 1–2。

### 21:40 · 定论

- 结论：非滚动重规划机制问题（v7 用同一引擎+机制成功），而是安全模型近端轨迹预测退化——停止长度预测提前触发（MPC 参考过短）+ 终点航向回归偏差（含方向切换）。
- 疑似根因：净空损失诱导网络"用缩短轨迹规避障碍"（短轨迹天然远离障碍、净空损失更低），牺牲终点收敛。
- 修改：新增 3 个诊断脚本；`docs/closed_loop_diagnosis.md` §3.4 新增机制证据、§4-B 标记已定论、§5 更新困难分层与解决路线优先级。
- 下一步：方向 B 建议解法（训练侧抑制缩短轨迹倾向或单变量缩短重规划周期）与方向 A（恢复数据隔离）待用户决策；方向 A 仍未执行。
- Worklog：[T-211000](../worklogs/2026-08-31.md#t-211000--211000214000--l2--方向-b-振荡机制定论近端停止长度预测提前终点航向回归偏差)。

## T-215000 · L2 · 方向 A 恢复数据隔离定论

### 21:50 · 开始

- 状态：进行中。用户按计划进入方向 A：验证 258 条恢复数据（9.7%）是否为安全微调破坏 v7 闭环能力的元凶。
- 目标状态：从 v7 `best.pt` 初始化、保持 `collision_loss_weight=0.5` 与净空损失，但训练集只用原始 v7 train（2400 条，无恢复样本），与 v9 正式配置单变量隔离恢复数据。
- 边界：不修改场景、规划器、MPC、门禁、净空损失实现；新配置与产物写独立目录。

### 22:05 · 训练与闭环完成

- `configs/training/net-v1-safe-v7data.yaml` 解析正确；训练 best epoch 5、11 epoch 早停，开环 ADE/FDE 0.258/0.553m，统一净空 0.092(-91.7%)，长度 MAE 10.46、校准阈值 0.42。
- 纯网络闭环（34 条同索引）：11.8% 成功（4/34）/35.3% 碰撞；门禁 47.1% 成功/0 碰撞/0 回退失败，干预 275/1348(20.4%)。

### 22:20 · 定论

- 判定：v7 成功 11 条在 v9d 下仍全部失败（8 振荡 + 3 碰撞）→ 恢复数据**不是唯一元凶**；但相对 v9，v9d 成功 2.9%→11.8%、碰撞 44.1%→35.3%、门禁 41.2%→47.1% → 恢复数据**确有加重作用**（疑似占比冲淡原始监督 + 分布与安全模型实际到达状态错配）。
- 修改：`configs/training/net-v1-safe-v7data.yaml`；产物 `runs/training/v9d-v7data/net-v1/`、`runs/closed-loop/v9d-v7data/net-v1/`；`docs/closed_loop_diagnosis.md` §4-A 标记定论、§5 更新路线。
- 两个方向均已定论：根因=安全微调近端预测退化（B），加重因子=恢复数据（A）。
- Worklog：[T-215000](../worklogs/2026-08-31.md#t-215000--215000222000--l2--方向-a-恢复数据隔离定论非根因但有加重作用)。

## T-203400 · L4 · 近端终点对齐训练监督与恢复数据重采

### 20:34 · 开始

- 状态：进行中。用户确认按建议完善：方向 B 定论指向训练侧近端终点/航向监督缺失，方向 A 定论指向恢复数据需用安全模型自身 deployment 重采。
- 修改前基线：`variable_loss_fn` 只有轨迹 MSE + 停止 BCE；净空损失对全部有效点均匀惩罚，近端点（接近目标）无额外监督，导致安全微调后停止长度提前触发、终点距目标与航向误差膨胀（见 `docs/closed_loop_diagnosis.md` §3.4）。
- 目标状态：新增可选的近端终点对齐监督（对 mask 末端 K 点施加相对目标位姿的位置+航向加权），并通过 checkpoint 身份/配置/测试完整落地；另给出用安全模型自身 deployment 重采恢复数据的命令。
- 边界：不改模型结构、MPC、门禁、专家数据、场景；新监督默认关闭（权重 0）保持旧训练语义兼容；净空损失保留但通过终点对齐抵消其"缩短轨迹躲障碍"倾向。
- 预计影响：`model/network.py`、`training/trainer.py`、`training/config.py`、相关测试、`spec/safety_training.md` 追溯与训练指南。

### 20:50 · 实现完成

- 新增 `model/network.py::endpoint_alignment_loss`（对 mask 末端 K 点施加相对目标位姿的位置+航向 MSE），`TrainerConfig` 增加 `endpoint_alignment_weight`（默认 0 关闭）/`endpoint_alignment_tail_points`（默认 8），接入 `_batch_loss` 并纳入 checkpoint 训练超参数兼容字段。
- 新增 `configs/training/net-v1-safe-endpoint.yaml`（v10：w=0.5，其余同 v9 正式）。
- 测试：`tests/test_model.py::TestEndpointAlignmentLoss`（合成对齐/非末端不参与/非法参数）、`test_training_config.py`（字段接受/非法权重）、`test_trainer.py`（启用后总损失提高/checkpoint 拒绝不一致续训）。
- Spec 新增 `SAFE-LOSS-003` 并更新追溯表；训练指南记录 v9 闭环诊断结论与后续单变量方向。

### 20:55 · 完成

- 状态：完整完成训练侧近端终点对齐监督实现，代码/测试/Spec/配置全部落地；真实训练与闭环验收留给用户按约定执行。
- 修改映射：`model/network.py` 新增 `endpoint_alignment_loss`（无 → 有）；`training/trainer.py::TrainerConfig` 新增 2 字段+校验+兼容字段（无 → 有）；`_batch_loss` 叠加 endpoint 项（仅权重>0 时）；`model/__init__.py` 导出；`configs/training/net-v1-safe-endpoint.yaml` 新增；`spec/safety_training.md` 新增 `SAFE-LOSS-003`；`docs/training_guide.md` 记录诊断结论与方向。
- 验证：定向 30 项 + 受影响 28 项 + 全仓 299 项全部通过。
- 剩余风险与下一步：真实效果未知，需用户执行 v10 训练后按同索引闭环准入复核；若成功率回升则确认近端对齐有效，再决定恢复数据重采（用 v10 deployment）；门禁作为安全防线基线。

### 21:10 · 继续 · v10 实测揭示更深根因

- v10（goal 对齐）用户训练并闭环：纯网络 0% 成功/73.5% 碰撞，门禁 35.3%。`train_collision_loss` 0.29–0.41（v9 仅 0.09–0.18），净空复算 0.223(-79.8%)，开环 0.275/0.535。
- 检查专家轨迹：mask 末端 == 目标位姿（100% 位置重合），但**19/300 样本目标位姿处净空仅 0.125m，远低于 required_clearance=0.3m**，且轨迹末端 8 点大量处于 <0.3m 净空区（S1×4、S2×7、S3×3、S4×1、S6×1、S8×3）。
- 原判断更正：近端退化不是"缺监督"而是"净空损失对目标末端物理上不可满足"。goal/target 对齐强制网络把轨迹延伸到目标，与净空惩罚直接冲突 → 碰撞暴增。正确方向是净空损失对目标附近末端区域豁免/降权，安全只约束行驶路径。
- 修正 `endpoint_alignment_loss` 语义（goal 对齐 → target 末端模仿加权），更新测试/Spec，重训 v10-fix。
- v10-fix 结果：开环 0.204/0.340 最优、净空 0.351(-68.2%)，但纯网络 2.9%/52.9%、门禁 32.4%——仍低于 v9d 门禁 47.1%。近端对齐（两种形式）均不能独立解决问题。
- 文档：`docs/closed_loop_diagnosis.md` 新增 §3.5 深层根因（净空与到达目标物理冲突）、§3.6 方向 A 结论。
- 下一步建议：实现净空损失对目标附近末端区域的豁免/降权（安全只约束行驶路径），再做单变量实验。

### 21:20 · 继续 · 净空目标豁免实现与 v11 实测

- 实现 `SweptFootprintLoss` 目标豁免：`goal_exempt_radius_m`/`goal_exempt_weight` 对距目标位姿半径内扫掠点平滑降权净空惩罚（`_apply_goal_exemption`），越界惩罚保留；`forward` 增加可选 goal 参数；TrainerConfig 新增 `safety_goal_exempt_radius_m`/`safety_goal_exempt_weight` 并纳入 checkpoint 兼容字段；runner/ablation 脚本同步；新增 v11 配置（半径 1m、权重 0）与 `TestGoalExemption` 4 项测试。
- v11 实测：开环 0.237/0.491m、净空损失与 v9 相当（豁免区少有点）；纯网络 0% 成功/47.1% 碰撞，门禁 38.2%；失败样本与 v9 高度一致。
- 结论：豁免不能单独解决闭环失败；安全微调整体（净空+恢复数据+微调组合）改变网络滚动重规划行为才是主因。豁免/近端对齐作为能力保留默认关闭。
- 验证：定向 32 项 + 全仓 303 项全部通过。

### 22:00 · 完成

- 状态：完整完成净空目标豁免实现与 v10/v11 系列实测，文档与 Commit 文稿就绪。
- 修改映射：`training/safety.py` 新增 `_apply_goal_exemption`/`goal_exempt_*` 参数（无 → 有）；`training/trainer.py::TrainerConfig` 新增 2 字段+校验+兼容字段；`training/runner.py`、`scripts/evaluate_safety_ablation.py` 同步；`configs/training/net-v1-safe-goal-exempt.yaml`（v11）与 `net-v1-safe-endpoint.yaml`（v10/v10-fix）新增；`tests/test_safety_loss.py::TestGoalExemption` 4 项；`spec/safety_training.md`、`docs/training_guide.md`、`docs/closed_loop_diagnosis.md` 更新。
- 验证：303 项全仓测试通过；v10/v10-fix/v11 均完成净空复算与同索引闭环。
- 剩余风险：纯网络闭环成功率仍远低于 70% 准入线，需用户决策下一步（安全微调整体路径重新设计 vs 门禁方案作为交付基线）。
## T-221500 · L2 · L1 BEV 保真评测实现

### 21:15 · 开始
- 状态：进行中。按 `docs/closed_loop_verification_design.md` §10 落地顺序第 1 步实现 L1：感知→BEV 保真评测。
- 修改前基线：项目只有开环（数据集状态 ADE/FDE）与全闭环两级，感知层失真无法单独追踪；无 BEV 保真指标。
- 目标状态：新增 occupancy（IoU/P/R）与 target（IoU/命中率）逐场景×噪声档评测，输出退化曲线，作为闭环归因的 L1 入口。

### 21:40 · 口径修正
- 初始实现直接用场景 emits_points 障碍全量几何栅格作 occupancy 真值，实测 IoU 仅约 0.13 且 clean/high 差异不明显——因为 360 束 LiDAR 稀疏采样与遮挡导致大量"看不到"的占用被计入真值。
- 修正：occupancy 主口径改为"同一位姿、相同 BEV 配置、3600 束无噪声 LiDAR 投射占用"（传感器可感知上限），几何真值作参考；车辆位姿改为朝向目标车位采样，否则 target 命中率恒 0。修正后 clean IoU≈0.6、P≈1.0、R≈0.6，high 退化至 IoU≈0.35–0.48，退化曲线有诊断价值。

### 22:00 · 完成
- 修改映射：`metrics/bev_fidelity.py`（新增真值栅格化 + 指标 + 聚合）→ `metrics/__init__.py` 无导出变化（脚本直接 import）；`viz/bev_fidelity.py` 新增退化曲线图；`scripts/evaluate_bev_fidelity.py` 新增评测入口；`tests/test_bev_fidelity.py` 新增 11 项测试。
- 文档：`docs/closed_loop_verification_design.md` §10 标记 L1 完成；`docs/training_guide.md` 新增 L1 评测入口与验收口径；`LEARNING.md` 新增"occupancy 真值必须对比传感器可感知真值"经验。
- 验证：L1 定向 11 项测试通过；9 场景全量报告 `runs/bev-fidelity/L1-v1/report.json` 生成；全仓 314 项测试通过。
- 剩余风险：L1 只评测占用/目标通道还原度，未覆盖轨迹规划（L2）与跟踪（L3）；targe 命中率依赖相机视野假设（车头朝向目标）。
- Worklog：[T-221500](../worklogs/2026-08-31.md#t-221500--l2--l1-bev-保真评测实现)
## T-222000 · L2 · L2 访问状态轨迹质量报告实现

### 22:10 · 开始
- 状态：进行中。按设计文档 §10 落地顺序第 2 步实现 L2：把诊断脚本固化为闭环访问状态轨迹质量验收报告。
- 修改前基线：只有诊断脚本 `scripts/diagnose_oscillation_*.py` 可人工剖析振荡，未纳入统一评测输出；无法在批量闭环中区分振荡与碰撞两类失败。
- 目标状态：闭环每次重规划同时采集网络预测与专家重规划，输出访问状态开环误差/近端质量/跨重规划一致性报告。

### 22:30 · 实现与验证
- 修改映射：`metrics/visited_state.py`（访问状态记录 + 分析 + 聚合）→ `scripts/evaluate_visited_state_trajectory.py`（hook 网络源同时取专家重规划）→ `viz/visited_state.py`（退化曲线）→ `tests/test_visited_state.py`（6 项）。
- 冒烟 4 条 + 全量 34 条（v7 deployment）成功；报告显示 S4 近端航向 113°、flips 1.09（振荡），S3 近端长度 3.6m 未骤缩且 vs_ADE 0.149（碰撞），两类失败可区分。
- 验证：L2 定向 6 项测试通过；全仓 320 项测试通过；正式报告 `runs/visited-state/L2-v1-v7/report.json` + 退化曲线。
- 文档：设计文档 §10 标记 L2 完成；训练指南新增入口；LEARNING 新增"访问状态 vs 专家重规划归因"经验。
- 剩余风险：L2 依赖专家重规划成功（专家规划失败的状态记录 expert=None 不参与 pair 统计）；一致性阈值（30°）为启发值。
- Worklog：[T-222000](../worklogs/2026-08-31.md#t-222000--l2--l2-访问状态轨迹质量报告实现)
## T-223000 · L2 · L3 单周期 MPC 跟踪复核实现

### 23:00 · 开始
- 状态：进行中。按设计文档 §10 落地顺序第 3 步实现 L3：单周期 MPC 跟踪复核，把控制层与规划层分离验证。
- 修改前基线：控制层健康仅有 M1 专家+MPC 全闭环 98.5% 证据，无"给定参考轨迹单周期跟踪质量"的独立口径。
- 目标状态：新增脚本对数据集任务取参考轨迹（专家/网络），只运行一个重规划周期，输出跟踪 RMS 与周期末对参考偏差。

### 23:20 · 实现与验证
- 修改映射：`scripts/evaluate_single_cycle_tracking.py`（固定参考单周期引擎 + 聚合）→ `tests/test_single_cycle_tracking.py`（4 项）。
- 实测（v7 34 条）：专家参考 RMS 0.019–0.042m/末端偏差 0.01–0.05m；网络参考 RMS 多数 <0.05m（S4/S6 个别 0.08–0.14m）。
- 验证：L3 定向 4 项测试通过；全仓 324 项测试通过；正式报告 `runs/single-cycle/L3-v1-expert|network/report.json`。
- 文档：设计文档 §10 标记 L3 完成；训练指南新增 L3 入口。
- 剩余风险：单周期只测首个周期跟踪，未覆盖多周期滚动下的 MPC 状态（热启动/进度锚定）；reach_ratio 低是单周期短推进的固有现象。
- Worklog：[T-223000](../worklogs/2026-08-31.md#t-223000--l2--l3-单周期-mpc-跟踪复核实现)
## T-224500 · L2 · L4 顶层效率判据补齐

### 23:40 · 开始
- 状态：进行中。按设计文档 §10 落地顺序第 4 步实现 L4：顶层判据补齐 time_dist_ratio 与蜿蜒度。
- 修改前基线：闭环 `summarize` 只有成功率/碰撞率/误差等，无"绕路/空转"连续度量；`EpisodeResult.meta` 无起终点坐标。
- 目标状态：闭环报告自动输出 time_dist_ratio 与 winding，供训练早停与模型对比。

### 23:55 · 实现与验证
- 修改映射：`metrics/evaluation.py::summarize` 新增两项指标；`experiments/closed_loop_evaluation.py` episode_meta 注入 start_x/y、goal_x/y；`tests/test_top_level_criteria.py`（5 项）。
- 实测（v7 34 条闭环）：time_dist_ratio 2.15±0.22 s/m、winding 1.55±0.80；可复现（成功率 32.4% 与既有报告一致）。
- 验证：L4 定向 5 项通过；全仓 329 项通过；正式报告 `runs/closed-loop/v7-flow-v3/net-v1/val-stratified-34-k10-l4/`。
- 文档：设计文档 §10 标记 L4 完成；训练指南新增 L4 说明。
- 剩余风险：winding 依赖 meta 注入 start/goal，专家随机基线 `run_closed_loop.py --source expert` 未注入（该入口不走 dataset meta）；时间-距离比值对低速策略是启发阈值，需按场景标定。
- Worklog：[T-224500](../worklogs/2026-08-31.md#t-224500--l2--l4-顶层效率判据补齐)
## T-230000 · L2 · v10-fix 闭环验收与诊断固化

### 23:40 · 开始
- 状态：进行中。用户执行 v10（近端对齐）训练后无法用闭环命令验收（目录名笔误 `runs/training/v10` 应为 `v10-endpoint-fix`）；需要补跑 v10 闭环与 L2 报告，并记录"训练效果不可用"的诊断。
- 修改前基线：v10-fix deployment 已存在（epoch 7、endpoint_alignment_weight=0.5、initialize_from v7 best）；v9/v10/v11 系列均未恢复纯网络闭环。

### 23:55 · 闭环与 L2 实测
- 纯网络 34 条：2.9% 成功/52.9% 碰撞（v7 为 32.4%/29.4%）；门禁 32.4%/0 碰撞/0 回退，干预率 19.7%。
- L2 对比 v7：vs_ADE/FDE 0.439/0.609（略优于 v7），但近端航向 71.5°（v7 50.4°）、flips 0.944（v7 0.567）、跨重规划航向跳变 2.471（v7 0.029）——近端对齐监督破坏一致性，纯网络碰撞率因此升至 52.9%。

### 00:10 · 诊断固化
- 修改：`docs/closed_loop_diagnosis.md` §3.5 补充 L2 归因、§5.2 重写解决路线（停止单变量修补、回归 v7、DAgger 完整化、L1-L4 阶梯验收）。
- Worklog：[T-230000](../worklogs/2026-08-31.md#t-230000--l2--v10-fix-闭环验收与诊断固化)
## T-231500 · L3 · 路线 B 分层短距局部规划实现与验证

### 23:20 · 开始
- 状态：进行中。按推荐计划实现路线 B（分层短距局部规划），绕开纯网络近端退化。
- 修改前基线：纯网络闭环 v7 32.4% 成功/29.4% 碰撞；v9/v10/v11 安全微调系列全部失败。
- 目标状态：网络输出全局参考，局部规划器从当前状态到 lookahead 子目标生成短段，MPC 跟踪；验证分层能否提升闭环。

### 23:45 · 实现与调优
- `runtime/sources.py::HierarchicalPlanningSource`：网络全局参考 + 沿参考累计弧长取 lookahead 子目标（多候选 1.0/0.6/0.35/0.15 逐步回退 + 全局目标兜底），局部规划失败全部候选时抛 `SafetyStopError` 优雅结束；`experiments/closed_loop_evaluation.py` 与 `scripts/run_closed_loop.py` 增 `--safety-mode hierarchical`；`runtime/__init__.py` 导出。
- 初版子目标不可达崩溃 → 改多候选回退；S3 紧 bay 运行中某状态 pose_free=False → 改 SafetyStopError。

### 00:15 · 验证结果
- v7 34 条：纯网络 32.4%→分层 58.8% 成功、碰撞 29.4%→0%（无门禁）；S1 100%/S2 80%/S6 100%/S8 100%，S3 0%（safety_stop）、S4 0%（振荡）、S7 0%（平行场景被破坏）。
- 验证：6 项测试通过；全仓 335 项通过；报告 `runs/closed-loop/v7-flow-v3/net-v1/val-hierarchical-34-k10/`。
- 文档：设计文档 §3/§10、训练指南、LEARNING 更新。
- 剩余风险：S3/S4/S7 需迭代 lookahead 策略或场景适配回退；分层用专家局部规划器，纯端到端纯度下降（网络退化为全局意图源）。
- Worklog：[T-231500](../worklogs/2026-08-31.md#t-231500--l3--路线-b-分层短距局部规划实现与验证)
