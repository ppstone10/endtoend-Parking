# 2026-08-24 修改跟踪

## T-111307 · L4 · P2.7 专家系统增强

### 11:13:07 · 开始检查点

- 状态：进行中。
- 修改前基线：Hybrid A* 依赖离散运动基元精确落入目标格；基元步长粗于栅格时存在格点可达性空洞。旧的非运动学直线 analytic shot 会破坏闭环跟踪质量，已回退。
- 目标状态：完成 P2.7 中可自动验收的 Reeds–Shepp 解析扩展、轨迹平滑和梯形速度剖面，优先闭合 S3/S5 紧凑场景与 P2.6 前置的全任务可规划性。
- 已知边界：保持现有车辆运动学、碰撞安全边界和规划器公共调用兼容；不重引入非运动学直连，不提前实现 P2.6 任务层或 P2.8 数据集。
- 预计影响：`planner/` 规划接口与新的几何/后处理模块、定向与场景级测试、对应 Planner Spec，以及 PLAN/ARCHITECTURE 中因实现确立的当前事实。

### 11:25:32 · 验证驱动的方案调整

- 原状态 → 新状态：解析触发邻域从固定 6m 改为默认 `max(6m, 2×车长)`，同时保留显式覆盖和 0 值回退开关。
- 原因：S3 带转向入位在 6m 门限下 5000 节点/4.699s 仍失败；12m 门限下同位姿成功，另外 4 个 S3/S5 转向起点均成功（0.047–5.449s）。
- 影响与证据：只改解析尝试时机，48 词候选、逐位姿碰撞检查与最小转弯半径不变；新增默认矿卡尺度的 S3/S5 转向回归。

### 11:34:17 · 性能门控调整

- 原状态 → 新状态：从解析邻域内“每弹出一个节点就重算全部候选”改为“起点必试、每 5 个展开节点尝试、命中目标格必试”，间隔可配且至少为 1。
- 原因：200 回合质量回归达到 99.0% 成功率、1.0% 碰撞率、规划失败重采 24 次（原基线 98.5%/1.5%/43 次），但总耗时 290.1s 较原 183.4s 增加约 58%；闭环平均步数反而 158.7→155.5，证明增量主要来自规划候选频繁重算。
- 影响：不改变解析候选集、碰撞安全门或目标格空洞的必试保障；新增间隔参数边界测试并重跑性能/质量回归。

### 11:47:52 · 完成检查点

- 状态：完整完成。P2.7 的 Reeds–Shepp 解析扩展、碰撞安全三次捷径和梯形速度剖面已实现，P2.6/P2.8 仍保持在后续任务边界。
- `planner/reeds_shepp.py`：修改前无解析曲线 → 12 个基本公式×4 对称的 48 词族表，去重候选按米制长度排序，支持带符号前进/倒车段与精确位姿采样；算法来源和许可边界记录于 `THIRD_PARTY_NOTICES.md`。
- `planner/hybrid_astar.py`：修改前依赖命中离散目标格后线性拼接 → 在默认两车长解析邻域中以起点/每 5 展开节点/目标格必试节奏尝试 Reeds–Shepp，逐位姿复用搜索边界和膨胀车身碰撞门；`analytic_expansion_distance=0` 可回退旧路径。
- `planner/smoothing.py` / `planner/profile.py`：修改前无后处理 → 前者只接受更短、不跨换向、曲率合规且全位姿无碰撞的 Hermite 捷径，后者以前向/反向约束传播实现起终停车、换向停车、倒车降速和单调时间轴；两者为可选后处理，未改变 `Trajectory(points, dt)`。
- 验证：修改前 planner 定向 6 项 → 最终 10 项；新增 Reeds–Shepp 3 项、平滑 3 项、剖面 4 项；`python -B -m unittest discover -s tests -v` 全仓 111 项通过，Spec 检查 PASS，差异空白检查通过。
- 额外覆盖：1000 组随机 SE(2) 起终位姿均生成候选（失败 0，每组 3–10 条去重候选）；S3/S5 直线与转向紧凑入位回归通过。
- 200 回合闭环：最终结果保存于 `experiments/results/p27_ground_baseline.json`；成功率 99.0%、碰撞率 1.0%、位置误差均值 0.211m、航向误差均值 8.91°、跟踪 RMS 0.0332m、规划失败重采 26 次、耗时 139.4s；较 M1 基线 98.5%/1.5%/0.236m/7.70°/0.0369m/43 次/183.4s，除航向均值增加 1.21° 外均改善，航向仍在 10° 终止阈值内。
- 文档：`spec/planner_dataset.md` 以 `EXPTRAJ-RS-001` / `EXPTRAJ-SMOOTH-001` / `EXPTRAJ-PROFILE-001` 固化契约与追溯；`ARCHITECTURE.md` 同步组件、数据流和不变量；`docs/PLAN.md` 将 P2.7 标记完成。工作日志链接：`docs/worklogs/2026-08-24.md#T-111307`。
- 契约/迁移/回退/恢复：统一 `Trajectory` 契约未变，无数据迁移；解析扩展可参数化禁用，单个候选失败自动继续候选/离散搜索；平滑候选失败返回原轨迹副本。
- 安全/隐私：解析和平滑路径不绕过现有车身/C-space 碰撞边界；无个人数据、授权、外部通信或不可逆数据操作。
- 剩余边界：平滑与速度剖面已完成独立契约和验收，但未提前写入 P2.8 数据集 schema；后续 P2.6/P2.8 需按任务元数据决定是否保存 `VelocityProfile`。

## T-123110 · L4 · P2.6 任务层

### 12:31:10 · 开始检查点

- 状态：进行中。
- 修改前基线：S1–S9 已提供场景环境、车位和 `spawn_zones`，P2.7 已闭合紧凑场景规划前置；但仓库没有 `Task`/`TaskSampler`，runner 仍使用无场景语义的随机位姿对，T1–T5 与难度轴只存在于需求文档。
- 目标状态：建立可复现的“9 场景×5 任务类型×难度轴”采样契约，每个任务携带规划、选位、动态注入和后续数据分层所需的稳定元数据，并以自动测试证明 seed 复现、距离分布、位姿安全和 T1–T5 语义。
- 已知边界：本轮实现任务数据与采样；不提前实现 P2.8 落盘 schema/数据集生成，不实现 FR-SELECT-01 的最优车位评分器，T5 只定义可由闭环引擎执行的注入计划，不改造引擎。
- 预计影响：`sim/tasks.py`、`sim/__init__.py`、任务层 Spec 和定向测试；实现稳定后同步 `ARCHITECTURE.md` / `docs/PLAN.md`。

### 12:44:00 · 契约检查点

- 调研约束：采用 NumPy `SeedSequence` 的稳定整数坐标派生独立随机流；T5 采用条件触发的事件载荷结构，与 ASAM OpenSCENARIO 的 trigger/action 分离一致。
- 方案调整：9×5 不再解释为“45 个单元全部强行出样”，而是完整能力矩阵；支持单元出样，不支持单元保留原因。原因是 S5/S6/S8 等默认场景分别缺少 T3 距离或 T4 候选数，静默放宽会破坏任务定义。
- 契约落点：新增 `spec/task.md`，稳定 ID 为 `TASK-MODEL-001`、`TASK-SAMPLE-001`、`TASK-MATRIX-001`、`TASK-DYNAMIC-001`。
- 实现前失败证据：`python -B -m unittest tests.test_tasks -v` 因 `ModuleNotFoundError: sim.tasks` 失败，证明新增行为尚未存在。

### 12:51:24 · 完成检查点

- 状态：完整完成。P2.6 已提供 T1–T5 任务模型、正交难度轴、可复现采样、45 单元能力表和 T5 动态事件载荷；P2.8 数据生成与运行时事件执行保持在后续边界。
- `sim/tasks.py`：修改前无任务层 → 新增 `TaskType`/`TaskGoal`/`TaskDifficulty`/`Task`/`TaskCapability`/`TaskSampler`；T1/T2/T3 分别执行 4–8/8–15/15–30m 距离约束，T4 保留 3–6 空闲候选且不预选，T5 只描述进度触发与圆形车辆障碍载荷。
- 随机与矩阵：修改前 runner/数据层使用顺序相关随机位姿对 → 根 seed×场景稳定序号×任务类型稳定序号×样本索引经 `SeedSequence` 派生；45 单元中当前 40 个可采样，S5/S6/S7-T4 与 S8-T1/T4 显式不支持，严格模式拒绝、不严格模式跳过。
- 难度与安全：相邻占用 2 侧落实为目标同排左右各一，不可表达时拒绝；起点同时满足距离与 6×3m footprint 安全，极窄可行带以确定性网格兜底；T5 采样不修改环境。
- 公共入口与测试：`sim/__init__.py` 导出任务 API；`tests/test_tasks.py` 从实现前缺模块失败扩展为 13 项契约、矩阵、动态事件与 T1–T5 规划集成回归。
- 验证：任务定向 13 项通过；5 seeds×40 支持单元×5 样本共 1000 任务采样失败 0；全仓 unittest 124 项通过；`spec/task.md` 检查 PASS；`git diff --check` 通过。
- 文档：新增完整 TASK Spec（`TASK-MODEL-001`、`TASK-SAMPLE-001`、`TASK-MATRIX-001`、`TASK-DYNAMIC-001` 均 ✅），ARCHITECTURE 同步任务数据流/不变量，PLAN 将 P2.6 标记完成。工作日志：`docs/worklogs/2026-08-24.md#T-123110`。
- 契约/迁移/回退：新增 API，不改变 `interfaces/` 或既有调用方，无数据迁移；后续调用方可先只消费支持单元，移除任务模块导出即可回退既有随机数据流。
- 剩余风险：代表性的 T1–T5 均可规划，但一个 S9/T3 随机样本触发 Hybrid A* 搜索上限；P2.8 必须按矩阵单元统计并重采规划失败，若论文要求填满全部 45 单元则应扩展场景几何，不能放宽任务定义。

## T-134226 · L4 · P2.4 噪声模型与第三方声明本地化

### 13:42:26 · 开始检查点

- 状态：进行中。
- 修改前基线：P2.3 车辆参数化代码已在 M1 落地但 PLAN 未标完成；LiDAR/Camera 传感器输出为纯净确定性结果，任务元数据虽有噪声等级但没有可执行噪声模型；`THIRD_PARTY_NOTICES.md` 的项目说明全英文。
- 目标状态：复核并关闭 P2.3 状态；实现干净/低/高三档、seed 可复现的 LiDAR 距离噪声/丢点/量程抖动与 Camera 像素噪声/目标误检漏检，并保持默认干净模式兼容；将第三方声明中的项目自写说明中文化。
- 已知边界：不改变第三方项目名称、作者、版权与许可证标识，不擅自翻译或改写许可证法律文本；不提前实现 P2.5 BEV 元数据或 P2.8 数据集；噪声只作用于传感器观测，不改变环境真值。
- 预计影响：`sim/noise.py`、LiDAR/Camera 仿真入口与导出、噪声 Spec/测试、`THIRD_PARTY_NOTICES.md`、ARCHITECTURE/PLAN。

### 13:45:00 · 调研与契约检查点

- 成熟做法：CARLA 将 LiDAR 噪声表达为沿射线的随机扰动与通用丢点率；本项目在此基础上按 FR-SIM-03 增加逐帧量程抖动。所有随机量由传感器私有 RNG 产生，避免污染全局随机状态。
- 参数定位：干净/低/高三档用于可重复的实验强度分层，不声明为任何真实矿卡传感器的标定值；允许调用方传入自定义 profile，为后续鲁棒性曲线保留扩展点。
- 许可证处理：MIT 与 BSD-3-Clause 都要求保留相应版权/许可声明；本文件当前只有项目自写的来源说明而非许可证全文，因此说明可中文化，但专有名称、版权行、SPDX 许可证标识和未来可能加入的许可证原文保持原文。
- 契约 ID：`NOISE-PROFILE-001`、`NOISE-LIDAR-001`、`NOISE-CAMERA-001`、`NOISE-SEED-001`；默认 `clean` 必须与修改前输出兼容。

### 13:52:16 · 完成检查点

- 状态：完整完成。P2.3 经既有实现与回归证据复核后标记完成；P2.4 三档传感器噪声实现并接入；第三方声明的项目说明已中文化。P2.5 BEV 参数化保持为下一工作包。
- `sim/noise.py`：修改前无噪声模型 → 新增 frozen `LiDARNoiseConfig`/`CameraNoiseConfig`/`NoiseProfile`、clean/low/high 三档、参数校验、JSON 元数据与自定义 profile 入口；`NoiseLevel` 从任务层下沉为传感器/任务共享枚举。
- `sim/sensor_sim.py`：修改前每束固定真值点 → 默认 clean 保持原循环；非干净档加入逐帧量程抖动、沿射线高斯距离扰动与独立丢点，允许合法 `(0,4)` 空帧。
- `sim/sensor_camera.py`：修改前只渲染理想目标区域 → 默认 clean 逐像素兼容；非干净档加入目标级漏检/误检和裁剪到 uint8 的像素高斯噪声，环境/内参不变。
- `THIRD_PARTY_NOTICES.md`：修改前项目说明全英文 → 标题和说明中文化；论文题名、项目名、Copyright、BSD-3-Clause/MIT 标识保持原文，并明确未来纳入第三方代码时应附完整许可证文本。
- 测试与契约：实现前 `tests.test_noise` 因 `sim.noise` 缺失失败；最终新增 14 项噪声测试，NOISE Spec 四个规范 ID 均 ✅。定向受影响链路 53 项、全仓 138 项通过；末次局部清晰度收敛后噪声+sim 22 项通过；Spec 检查与 `git diff --check` 通过。
- 文档：ARCHITECTURE 同步真值→噪声观测数据流及 RNG 隔离不变量；PLAN 关闭 P2.3/P2.4。工作日志：`docs/worklogs/2026-08-24.md#T-134226`。
- 契约/迁移/回退：两个传感器只在构造器尾部新增 keyword-only `noise`/`seed`，既有位置调用和帧接口不变，无数据迁移；保持或显式设置 `noise="clean"` 即为兼容/回退路径。
- 安全/隐私与风险：只处理合成观测，无个人数据或外部副作用；RNG 不用于安全决策。low/high 参数未经真实矿卡日志标定，后续有现场数据时应替换 profile 数值并做分布验收。
