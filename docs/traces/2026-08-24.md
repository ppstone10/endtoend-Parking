# 2026-08-24 修改跟踪

## T-111307 · L4 · P2.7 专家系统增强

### 11:13:07 · 开始检查点

- 状态：进行中。
- 修改前基线：Hybrid A* 依赖离散运动基元精确落入目标格；基元步长粗于栅格时存在格点可达性空洞。旧的非运动学直线 analytic shot 会破坏闭环跟踪质量，已回退。
- 目标状态：完成 P2.7 中可自动验收的 Reeds–Shepp 解析扩展、轨迹平滑和梯形速度剖面，优先闭合 S3/S5 紧凑场景与 P2.6 前置的全任务可规划性。
- 已知边界：保持现有车辆运动学、碰撞安全边界和规划器公共调用兼容；不重引入非运动学直连，不提前实现 P2.6 任务层或 P2.8 数据集。
- 预计影响：`planner/` 规划接口与新的几何/后处理模块、定向与场景级测试、对应 Planner Spec，以及 PLAN/ARCHITECTURE 中因实现确立的当前事实。

### 11:25:32 · 验证驱动的方案调整

- 原状态 → 新状态：解析触发邻域从固定 6m 改为默认 `max(6m, 2×车长)`，同时保留显式覆盖和 0 值回退开关。
- 原因：S3 带转向入位在 6m 门限下 5000 节点/4.699s 仍失败；12m 门限下同位姿成功，另外 4 个 S3/S5 转向起点均成功（0.047–5.449s）。
- 影响与证据：只改解析尝试时机，48 词候选、逐位姿碰撞检查与最小转弯半径不变；新增默认矿卡尺度的 S3/S5 转向回归。

### 11:34:17 · 性能门控调整

- 原状态 → 新状态：从解析邻域内“每弹出一个节点就重算全部候选”改为“起点必试、每 5 个展开节点尝试、命中目标格必试”，间隔可配且至少为 1。
- 原因：200 回合质量回归达到 99.0% 成功率、1.0% 碰撞率、规划失败重采 24 次（原基线 98.5%/1.5%/43 次），但总耗时 290.1s 较原 183.4s 增加约 58%；闭环平均步数反而 158.7→155.5，证明增量主要来自规划候选频繁重算。
- 影响：不改变解析候选集、碰撞安全门或目标格空洞的必试保障；新增间隔参数边界测试并重跑性能/质量回归。

### 11:47:52 · 完成检查点

- 状态：完整完成。P2.7 的 Reeds–Shepp 解析扩展、碰撞安全三次捷径和梯形速度剖面已实现，P2.6/P2.8 仍保持在后续任务边界。
- `planner/reeds_shepp.py`：修改前无解析曲线 → 12 个基本公式×4 对称的 48 词族表，去重候选按米制长度排序，支持带符号前进/倒车段与精确位姿采样；算法来源和许可边界记录于 `THIRD_PARTY_NOTICES.md`。
- `planner/hybrid_astar.py`：修改前依赖命中离散目标格后线性拼接 → 在默认两车长解析邻域中以起点/每 5 展开节点/目标格必试节奏尝试 Reeds–Shepp，逐位姿复用搜索边界和膨胀车身碰撞门；`analytic_expansion_distance=0` 可回退旧路径。
- `planner/smoothing.py` / `planner/profile.py`：修改前无后处理 → 前者只接受更短、不跨换向、曲率合规且全位姿无碰撞的 Hermite 捷径，后者以前向/反向约束传播实现起终停车、换向停车、倒车降速和单调时间轴；两者为可选后处理，未改变 `Trajectory(points, dt)`。
- 验证：修改前 planner 定向 6 项 → 最终 10 项；新增 Reeds–Shepp 3 项、平滑 3 项、剖面 4 项；`python -B -m unittest discover -s tests -v` 全仓 111 项通过，Spec 检查 PASS，差异空白检查通过。
- 额外覆盖：1000 组随机 SE(2) 起终位姿均生成候选（失败 0，每组 3–10 条去重候选）；S3/S5 直线与转向紧凑入位回归通过。
- 200 回合闭环：最终结果保存于 `experiments/results/p27_ground_baseline.json`；成功率 99.0%、碰撞率 1.0%、位置误差均值 0.211m、航向误差均值 8.91°、跟踪 RMS 0.0332m、规划失败重采 26 次、耗时 139.4s；较 M1 基线 98.5%/1.5%/0.236m/7.70°/0.0369m/43 次/183.4s，除航向均值增加 1.21° 外均改善，航向仍在 10° 终止阈值内。
- 文档：`spec/planner_dataset.md` 以 `EXPTRAJ-RS-001` / `EXPTRAJ-SMOOTH-001` / `EXPTRAJ-PROFILE-001` 固化契约与追溯；`ARCHITECTURE.md` 同步组件、数据流和不变量；`docs/PLAN.md` 将 P2.7 标记完成。工作日志链接：`docs/worklogs/2026-08-24.md#T-111307`。
- 契约/迁移/回退/恢复：统一 `Trajectory` 契约未变，无数据迁移；解析扩展可参数化禁用，单个候选失败自动继续候选/离散搜索；平滑候选失败返回原轨迹副本。
- 安全/隐私：解析和平滑路径不绕过现有车身/C-space 碰撞边界；无个人数据、授权、外部通信或不可逆数据操作。
- 剩余边界：平滑与速度剖面已完成独立契约和验收，但未提前写入 P2.8 数据集 schema；后续 P2.6/P2.8 需按任务元数据决定是否保存 `VelocityProfile`。

## T-123110 · L4 · P2.6 任务层

### 12:31:10 · 开始检查点

- 状态：进行中。
- 修改前基线：S1–S9 已提供场景环境、车位和 `spawn_zones`，P2.7 已闭合紧凑场景规划前置；但仓库没有 `Task`/`TaskSampler`，runner 仍使用无场景语义的随机位姿对，T1–T5 与难度轴只存在于需求文档。
- 目标状态：建立可复现的“9 场景×5 任务类型×难度轴”采样契约，每个任务携带规划、选位、动态注入和后续数据分层所需的稳定元数据，并以自动测试证明 seed 复现、距离分布、位姿安全和 T1–T5 语义。
- 已知边界：本轮实现任务数据与采样；不提前实现 P2.8 落盘 schema/数据集生成，不实现 FR-SELECT-01 的最优车位评分器，T5 只定义可由闭环引擎执行的注入计划，不改造引擎。
- 预计影响：`sim/tasks.py`、`sim/__init__.py`、任务层 Spec 和定向测试；实现稳定后同步 `ARCHITECTURE.md` / `docs/PLAN.md`。

### 12:44:00 · 契约检查点

- 调研约束：采用 NumPy `SeedSequence` 的稳定整数坐标派生独立随机流；T5 采用条件触发的事件载荷结构，与 ASAM OpenSCENARIO 的 trigger/action 分离一致。
- 方案调整：9×5 不再解释为“45 个单元全部强行出样”，而是完整能力矩阵；支持单元出样，不支持单元保留原因。原因是 S5/S6/S8 等默认场景分别缺少 T3 距离或 T4 候选数，静默放宽会破坏任务定义。
- 契约落点：新增 `spec/task.md`，稳定 ID 为 `TASK-MODEL-001`、`TASK-SAMPLE-001`、`TASK-MATRIX-001`、`TASK-DYNAMIC-001`。
- 实现前失败证据：`python -B -m unittest tests.test_tasks -v` 因 `ModuleNotFoundError: sim.tasks` 失败，证明新增行为尚未存在。

### 12:51:24 · 完成检查点

- 状态：完整完成。P2.6 已提供 T1–T5 任务模型、正交难度轴、可复现采样、45 单元能力表和 T5 动态事件载荷；P2.8 数据生成与运行时事件执行保持在后续边界。
- `sim/tasks.py`：修改前无任务层 → 新增 `TaskType`/`TaskGoal`/`TaskDifficulty`/`Task`/`TaskCapability`/`TaskSampler`；T1/T2/T3 分别执行 4–8/8–15/15–30m 距离约束，T4 保留 3–6 空闲候选且不预选，T5 只描述进度触发与圆形车辆障碍载荷。
- 随机与矩阵：修改前 runner/数据层使用顺序相关随机位姿对 → 根 seed×场景稳定序号×任务类型稳定序号×样本索引经 `SeedSequence` 派生；45 单元中当前 40 个可采样，S5/S6/S7-T4 与 S8-T1/T4 显式不支持，严格模式拒绝、不严格模式跳过。
- 难度与安全：相邻占用 2 侧落实为目标同排左右各一，不可表达时拒绝；起点同时满足距离与 6×3m footprint 安全，极窄可行带以确定性网格兜底；T5 采样不修改环境。
- 公共入口与测试：`sim/__init__.py` 导出任务 API；`tests/test_tasks.py` 从实现前缺模块失败扩展为 13 项契约、矩阵、动态事件与 T1–T5 规划集成回归。
- 验证：任务定向 13 项通过；5 seeds×40 支持单元×5 样本共 1000 任务采样失败 0；全仓 unittest 124 项通过；`spec/task.md` 检查 PASS；`git diff --check` 通过。
- 文档：新增完整 TASK Spec（`TASK-MODEL-001`、`TASK-SAMPLE-001`、`TASK-MATRIX-001`、`TASK-DYNAMIC-001` 均 ✅），ARCHITECTURE 同步任务数据流/不变量，PLAN 将 P2.6 标记完成。工作日志：`docs/worklogs/2026-08-24.md#T-123110`。
- 契约/迁移/回退：新增 API，不改变 `interfaces/` 或既有调用方，无数据迁移；后续调用方可先只消费支持单元，移除任务模块导出即可回退既有随机数据流。
- 剩余风险：代表性的 T1–T5 均可规划，但一个 S9/T3 随机样本触发 Hybrid A* 搜索上限；P2.8 必须按矩阵单元统计并重采规划失败，若论文要求填满全部 45 单元则应扩展场景几何，不能放宽任务定义。

## T-134226 · L4 · P2.4 噪声模型与第三方声明本地化

### 13:42:26 · 开始检查点

- 状态：进行中。
- 修改前基线：P2.3 车辆参数化代码已在 M1 落地但 PLAN 未标完成；LiDAR/Camera 传感器输出为纯净确定性结果，任务元数据虽有噪声等级但没有可执行噪声模型；`THIRD_PARTY_NOTICES.md` 的项目说明全英文。
- 目标状态：复核并关闭 P2.3 状态；实现干净/低/高三档、seed 可复现的 LiDAR 距离噪声/丢点/量程抖动与 Camera 像素噪声/目标误检漏检，并保持默认干净模式兼容；将第三方声明中的项目自写说明中文化。
- 已知边界：不改变第三方项目名称、作者、版权与许可证标识，不擅自翻译或改写许可证法律文本；不提前实现 P2.5 BEV 元数据或 P2.8 数据集；噪声只作用于传感器观测，不改变环境真值。
- 预计影响：`sim/noise.py`、LiDAR/Camera 仿真入口与导出、噪声 Spec/测试、`THIRD_PARTY_NOTICES.md`、ARCHITECTURE/PLAN。

### 13:45:00 · 调研与契约检查点

- 成熟做法：CARLA 将 LiDAR 噪声表达为沿射线的随机扰动与通用丢点率；本项目在此基础上按 FR-SIM-03 增加逐帧量程抖动。所有随机量由传感器私有 RNG 产生，避免污染全局随机状态。
- 参数定位：干净/低/高三档用于可重复的实验强度分层，不声明为任何真实矿卡传感器的标定值；允许调用方传入自定义 profile，为后续鲁棒性曲线保留扩展点。
- 许可证处理：MIT 与 BSD-3-Clause 都要求保留相应版权/许可声明；本文件当前只有项目自写的来源说明而非许可证全文，因此说明可中文化，但专有名称、版权行、SPDX 许可证标识和未来可能加入的许可证原文保持原文。
- 契约 ID：`NOISE-PROFILE-001`、`NOISE-LIDAR-001`、`NOISE-CAMERA-001`、`NOISE-SEED-001`；默认 `clean` 必须与修改前输出兼容。

### 13:52:16 · 完成检查点

- 状态：完整完成。P2.3 经既有实现与回归证据复核后标记完成；P2.4 三档传感器噪声实现并接入；第三方声明的项目说明已中文化。P2.5 BEV 参数化保持为下一工作包。
- `sim/noise.py`：修改前无噪声模型 → 新增 frozen `LiDARNoiseConfig`/`CameraNoiseConfig`/`NoiseProfile`、clean/low/high 三档、参数校验、JSON 元数据与自定义 profile 入口；`NoiseLevel` 从任务层下沉为传感器/任务共享枚举。
- `sim/sensor_sim.py`：修改前每束固定真值点 → 默认 clean 保持原循环；非干净档加入逐帧量程抖动、沿射线高斯距离扰动与独立丢点，允许合法 `(0,4)` 空帧。
- `sim/sensor_camera.py`：修改前只渲染理想目标区域 → 默认 clean 逐像素兼容；非干净档加入目标级漏检/误检和裁剪到 uint8 的像素高斯噪声，环境/内参不变。
- `THIRD_PARTY_NOTICES.md`：修改前项目说明全英文 → 标题和说明中文化；论文题名、项目名、Copyright、BSD-3-Clause/MIT 标识保持原文，并明确未来纳入第三方代码时应附完整许可证文本。
- 测试与契约：实现前 `tests.test_noise` 因 `sim.noise` 缺失失败；最终新增 14 项噪声测试，NOISE Spec 四个规范 ID 均 ✅。定向受影响链路 53 项、全仓 138 项通过；末次局部清晰度收敛后噪声+sim 22 项通过；Spec 检查与 `git diff --check` 通过。
- 文档：ARCHITECTURE 同步真值→噪声观测数据流及 RNG 隔离不变量；PLAN 关闭 P2.3/P2.4。工作日志：`docs/worklogs/2026-08-24.md#T-134226`。
- 契约/迁移/回退：两个传感器只在构造器尾部新增 keyword-only `noise`/`seed`，既有位置调用和帧接口不变，无数据迁移；保持或显式设置 `noise="clean"` 即为兼容/回退路径。
- 安全/隐私与风险：只处理合成观测，无个人数据或外部副作用；RNG 不用于安全决策。low/high 参数未经真实矿卡日志标定，后续有现场数据时应替换 profile 数值并做分布验收。

## T-140125 · L4 · P2.5 BEV 参数化

### 14:01:25 · 开始检查点

- 状态：进行中。
- 修改前基线：`LiDAR2BEV` 与 `Camera2BEV` 分别持有裸 `resolution`/`extent` 参数，默认值为 0.2m 与 20×20m；场景对象不携带 BEV 配置，调用方必须手工保持两路一致；`DatasetGenerator` 只保存数组字段，没有 schema 版本、`bev_meta` 或 `task_meta`。
- 目标状态：建立统一且可校验的 BEV 空间配置，默认 40×40m@0.25m、允许场景覆盖并同时注入两路转换器；将分辨率、范围、通道和形状稳定保存到 NPZ schema v2，同时保留现有显式构造与 v1 数据读取兼容性。
- 已知边界：本轮不提前实现 P2.8 的任务驱动批量生成、分层划分或 3000 条数据生产；不改模型结构，不允许同一 NPZ 混装不同 BEV 形状；现有 P2.4 未提交改动只在必要连接点协同修改。
- 预计影响：共享接口、场景配置、Sensor2BEV 构造、数据集保存/加载、P2.5 Spec 与定向测试；稳定后同步架构和计划。

### 14:08:40 · 调研与契约检查点

- 调研结论：NumPy 官方文档确认 `.npz` 是具名 `.npy` 数组的压缩归档，`np.load(..., allow_pickle=False)` 可拒绝 object/pickle 载荷；Python `json` 官方文档支持通过 `sort_keys=True` 与紧凑分隔符生成确定性文本。因此 schema v2 采用 Unicode JSON 数组保存 `bev_meta`/`task_meta`，不使用 object array、不引入新依赖。
- 配置所有权：共享空间配置放入只依赖 NumPy 的 `interfaces/`，避免 `sim/` 与 `sensor2bev/` 反向耦合；`SceneBundle` 携带配置，两路转换器接收同一个对象并由管道在构造期检查一致性。
- 场景策略：默认 40×40m@0.25m；S9 综合矿场使用 80×80m@0.5m。二者保持 160×160 栅格，既表达场景视野差异，也避免同一模型/批次出现尺寸漂移。
- 数据兼容：v2 新增显式版本、单份 `bev_meta` 与逐样本 `task_meta`；v1 文件仍按原字段成功加载并被识别为版本 1。一个归档内 BEV 元数据或轨迹 `dt` 不一致时拒绝写入，避免静默产生不可解释数据。

### 14:14:24 · 完成检查点

- 状态：完整完成。P2.5 已建立场景→双传感器转换→融合管道→NPZ 的统一 BEV 配置与元数据链路；P2.8 的任务驱动生成、划分和规模化生产保持为下一工作包。
- `interfaces/bev.py` / `interfaces/__init__.py`：修改前只有携带裸空间值的 `BEVTensor` → 新增 frozen `BEVConfig`（默认 40×40m@0.25m、整除/有限值校验、确定形状）与 `BEVTensor.to_metadata()`，公共导出为加法兼容。
- `sim/scenes.py`：修改前场景不描述 BEV → `SceneBundle` 增加默认配置，S1–S8 为 40×40m@0.25m，S9 为 80×80m@0.5m；两类均为 160×160，兼顾远距视野与固定网络输入尺寸。
- `sensor2bev/` / `dataset/pipeline.py` / `scripts/`：修改前两路重复裸参数且只在融合时发现不一致 → 转换器支持共享 `config`，管道在构造期拒绝空间配置不一致，三个演示/训练脚本改为单对象注入；旧式显式 `resolution/extent` 继续可用。
- `dataset/generator.py`：修改前无版本 NPZ → schema v2 新增标量版本、Unicode JSON `bev_meta` 和逐样本 `task_meta`；保存拒绝空集、混合 BEV/`dt`，加载校验元数据与数组对齐，显式禁用 pickle；无版本归档按 v1 加载并保留原数组字段。
- 契约与文档：`IFACE-BEV-003`、`EXPTRAJ-DATA-002`、`EXPTRAJ-DATA-003` 已实现并追溯为 ✅；ARCHITECTURE 同步配置所有权、数据流和 schema 不变量，PLAN 关闭 P2.5。
- 验证：实现前 4 个模块/属性失败符合预期；受影响链路 56 项通过；`scripts/run_sim.py` 实际输出 LiDAR `(3,160,160)`、Camera `(1,160,160)`、融合 `(5,160,160)`；全仓 149 项通过；末次加载一致性加固后相关 38 项通过；`git diff --check` 通过。
- 迁移/回退/安全：现有 v1 文件不迁移，旧数组键和含义不变；旧式显式空间构造可回退，v2 保存不覆盖输入文件。元数据不使用 object/pickle；无个人数据、授权、外部通信或不可逆操作。
- 剩余边界：当前随机 `generate()` 尚不产生真实 `task_meta`，缺省写 `{}`；P2.8 应接入 `Task.to_metadata()`/实际噪声 profile，按 BEV 配置分组归档，并实现 train/val/test 分层和数据检查脚本。工作日志：`docs/worklogs/2026-08-24.md#T-140125`。

## T-143148 · L4 · P2.8 数据管线重构

### 14:31:48 · 开始检查点

- 状态：进行中。
- 修改前基线：`DatasetGenerator.generate(count)` 只在单一环境中随机采样无语义位姿对；P2.6 的 `Task`/能力矩阵尚未进入专家规划与 BEV 生成，仓库没有 split 所有者、任务级失败统计、构建脚本或数据体检入口。
- 目标状态：让数据生成直接消费可复现 Task，保存完整任务/噪声/目标解析元数据；建立 train/val/test=8:1:1 的稳定分层与整场景泛化保留；提供可按配额重采规划失败的构建脚本，以及轨迹长度、倒车比例、场景占比和 BEV+专家轨迹抽检工具。
- 已知边界：T4 本轮只解析出用于专家轨迹监督的确定性可行候选并明确记录策略，不实现 P4.1 五项代价 oracle；T5 保存事件载荷但不提前改造闭环注入执行；不修改网络/训练器，不在实现验证阶段默认生成最终 3000+ 大文件。
- 兼容与运行约束：保留 `generate(int)` 旧入口和 schema v1/v2 读取；新批量入口失败必须携带 task ID 并由构建层按矩阵单元重采；默认测试保留 S9，构建配额使其约占总量 10%，避免同一场景泄漏到 train/val。
- 预计影响：`dataset/` 生成、划分与体检模块，`scripts/build_dataset.py` / `scripts/inspect_dataset.py`，Planner Dataset Spec、测试、ARCHITECTURE 与 PLAN。

### 14:38:10 · 调研与契约检查点

- 成熟做法：参考 scikit-learn 的 `stratify` 与 group split 语义，将“保持任务类别比例”和“整组不泄漏”分成两层；参考 NumPy `SeedSequence` 用稳定整数坐标派生每个分层的随机流。项目不新增 scikit-learn 依赖，以现有 NumPy 实现最小确定性划分。
- 划分方案：S9 作为默认泛化场景，全部进入 test；构建配额预留总量 10% 给 S9，其余任务按场景×任务类型分层划出 10% val，剩余 train，从输入构造层实现目标 8:1:1。
- T4 方案：数据层按候选列表稳定顺序尝试规划并选首个可行目标，元数据明确标记 `first_plannable_candidate`；这只为生成条件轨迹监督，不宣称最优，P4.1 可注入正式 oracle 替换该解析策略。
- T5/噪声：T5 保存动态事件但当前样本仍是触发前名义规划；任务指定噪声档用于构造传感器并把完整 `NoiseProfile` 写入元数据，避免只保留档位名称。
- 契约 ID：新增 `EXPTRAJ-DATA-004`（Task 驱动生成）、`EXPTRAJ-DATA-005`（稳定分层/整场景泛化）、`EXPTRAJ-DATA-006`（构建配额与失败重采）、`EXPTRAJ-DATA-007`（统计与叠加抽检）。

### 17:28:29 · 生产反馈与完成检查点

- 状态：完整完成。P2.8 已从 Task 计划贯通专家规划、融合 BEV、稳定划分、失败重采、schema v2 落盘、统计与叠加抽检，并在 PLAN 中关闭；下一工作包为 P3.1。
- 方案调整：3000 条生产先后暴露相邻占用后目标 footprint 冲突、5 个纯几何合法但当前专家持续不可达单元、S8/T5 仅 forward 可达，以及 split 各自重采导致 task ID 泄漏。最终把相邻占用收紧为目标安全的可表达等级，在 `dataset/build.py` 单独拥有专家可生成能力，并用全计划+已用 ID 保留集约束替代任务；不改变 `sim/tasks.py` 的纯几何能力矩阵。
- `dataset/generator.py` / `pipeline.py`：修改前仅支持单环境随机位姿 → 兼容接收 Task 列表，通过 task 组件工厂构造规划/感知链路；T4 记录首个可规划候选策略，实际目标写入 Camera target 通道，完整 Task/NoiseProfile/选中目标写入逐样本元数据，失败携带 task ID。
- `dataset/splits.py` / `build.py`：修改前无 split 与配额所有者 → 新增 S9 整场景 test、其余按场景×类型稳定分层 val、精确 8:1:1 计划、可表达 difficulty 循环、专家可生成能力、同单元重采、失败统计和跨 split ID 保留门。
- `sim/tasks.py`：修改前相邻占用只有场景级布尔支持且可能让目标与占用车辆冲突 → 按场景×任务类型枚举 0/1/2 可表达等级，重建占用场景后复用矿卡 footprint 检查，T4 继续保证至少 3 个候选空闲位。
- `dataset/inspection.py` / `scripts/`：修改前无规模化构建与体检入口 → 新增 dry-run/正式构建、partial→正式文件→manifest 闭环，以及轨迹长度、倒车距离比例、场景/类型/噪声统计和 BEV+专家轨迹叠加抽检。
- 正式产物：`data/task_dataset/`（Git 忽略）包含 train/val/test schema v2 NPZ 2400/300/300、manifest、三份统计 JSON 与 12 张 PNG；三 split 重叠均为 0，总计 3000 个唯一 task ID，test 仅 S9 且 T1–T5 各 60。
- 统计摘要：train/val/test 平均轨迹长度约 13.40/13.85/17.93m，倒车距离占比约 56.2%/58.8%/60.0%；噪声三档均覆盖。生产记录的规划失败重采为 296/150/66 次，均在原场景×类型×难度内完成。
- 验证：实现前缺少 Task 入口/splits/build/inspection 的失败证据成立；最终全仓 169 项 unittest、语法编译、3000 条归档加载/schema/形状/唯一性检查、12 张抽检图写出与 `git diff --check` 全部通过。
- 契约/兼容/回退：`generate(int)`、v1/v2 读取和原数组键保持兼容；新 API/字段均为加法。T4 当前标签不是最优 oracle，P4.1 可注入 selector；专家能力表属于当前 Hybrid A* 边界，规划器或场景变化时应重跑代表矩阵并更新。无个人数据、外部通信或不可逆数据迁移。工作日志：`docs/worklogs/2026-08-24.md#T-143148`。

## T-195606 · L3 · P2.8 抽检结果可解释性增强

### 19:56:06 · 开始检查点

- 状态：进行中。
- 修改前基线：单样本抽检图只显示黑色 occupancy、较淡的 target、蓝色整段轨迹和绿色起点圆点；没有矿卡车身尺寸、起终朝向、显式目标位姿、前进/倒车区分、换向点或任务质量摘要。CLI 按数组位置均匀取样，不能保证有限图数覆盖 T1–T5。
- 目标状态：把每张图升级为可直接判断“从哪里、以什么姿态、如何行驶、最终是否到位”的验收图；复用 `MINING_TRUCK` 6×3m 配置绘制起点/目标 footprint 和朝向，区分前进/倒车并标记换向，展示任务元数据与终点误差；抽样优先覆盖不同任务类型。
- 已知边界：本轮不修改专家规划结果、数据集 schema、既有 3000 条 NPZ 或训练流程；只增强 inspection 表达与抽样策略，重生成 Git 忽略目录内的抽检 PNG/summary。
- 预计影响：`dataset/inspection.py`、`scripts/inspect_dataset.py`、相关测试、`EXPTRAJ-DATA-008` 轻量契约以及既有抽检产物。

### 20:02:50 · 视觉验收调整

- 原状态 → 新状态：首版增强图已能显示起终矿卡、前进/倒车、换向和误差，但 S9 的固定 80×80m 全 BEV 视野把短泊车动作压缩在画面中央；改为围绕完整轨迹和两端 footprint 自动取景，并裁剪在 BEV 有效范围内。
- 原因与证据：真实 `test/sample_00030` 的路径仅 7.53m，固定全景下 6×3m 车身虽存在但仍不够直观，不能充分满足用户“直观感受效果”的验收目标。
- 影响范围：只调整抽检图坐标轴显示范围，不裁剪底层 BEV 数组，不改变轨迹、误差计算、数据 schema 或规划结果。

### 20:08:03 · 完成检查点

- 状态：完整完成。抽检输出已从“BEV 上的一条轨迹线”升级为可直接判断起始位姿、目标位姿、行驶过程和到位质量的专家泊车验收图；代表抽样在默认 5 张时覆盖 T1–T5。
- `dataset/inspection.py`：修改前只画起点圆点和单色轨迹 → 复用 `MINING_TRUCK` 6×3m 配置绘制起终 footprint/朝向，前进蓝色、倒车紫色、换向菱形、终点黑叉与方向箭头；新增轨迹证据自动取景、任务/难度/目标策略/T5 事件信息及终点位置/航向误差和容差 PASS 判定。
- `dataset/__init__.py` / `scripts/inspect_dataset.py`：修改前按数组位置均匀抽 4 张 → 新增并导出任务类型优先的代表索引选择，CLI 默认抽 5 张，在样本数允许时覆盖全部现有任务类型。
- `tests/test_dataset_inspection.py`：修改前只验证统计和文件存在 → 增加任务类型覆盖、元数据错位拒绝、矿卡 6×3m footprint 及旋转局部目标位姿回归；定向 5 项、数据链路 27 项、全仓 172 项均通过，语法编译与 `git diff --check` 通过。
- 产物与图审：不重建 3000 条 NPZ；在 Git 忽略的 `data/task_dataset/inspection_v2/` 为 train/val/test 各写出 5 张增强 PNG 和 summary，S9 的 T1–T5 五张代表图均人工确认车辆、朝向、轨迹、信息面板与 PASS 状态清晰可见。
- 契约与知识：`EXPTRAJ-DATA-008` 已实现并追溯为 ✅；既有 DATA-007 统计契约、NPZ schema、规划器和训练入口不变。README、DESIGN、ARCHITECTURE、LEARNING 无事实变化，不更新。工作日志：`docs/worklogs/2026-08-24.md#T-195606`。
- 剩余边界：图中 `GOAL CHECK` 只验收专家轨迹终点是否落入任务目标容差，不代表动态事件触发后的闭环避障成功；T5 面板明确显示事件载荷，真实注入与重规划仍属于后续闭环阶段。

## T-201407 · L4 · 任务机动一致性审计与数据门禁

### 20:14:07 · 开始检查点

- 状态：进行中。
- 修改前基线：任务元数据保存 `difficulty.maneuver=forward/reverse`，专家规划器允许 Reeds–Shepp 前后向混合路径；数据生成、构建重采和 inspection 只记录轨迹及全局倒车比例，不检查任务声明与实际主导行驶方向是否一致。增强抽检已实际看到声明 `forward` 而轨迹倒车距离占比超过 80% 的样本。
- 目标状态：定义稳定、可解释的机动一致性判定；在数据统计中输出一致/不一致数量与分层，在 Task 驱动样本生成进入归档前执行门禁，不一致时携带 task ID 和方向占比进入既有同单元重采；保留合法短距离调整和换向轨迹。
- 已知边界：不把“出现任意反向段”直接判为失败，不修改 Hybrid A*/Reeds–Shepp 搜索目标，也不静默改写任务标签；既有正式 NPZ 作为审计输入，只有新构建受门禁约束，是否重建正式 3000 条数据依据审计结果和验证成本决定。
- 预计影响：任务机动语义相关 Spec、`dataset` 方向分析/生成门禁/统计、构建报告与 CLI 输出、测试、架构不变量及现有数据审计证据。

### 20:19:20 · T4 目标解析契约调整

- 原计划 → 新计划：门禁原拟在 `_resolve_goal` 返回后统一拒绝；改为在每个候选规划成功后立即审计，T4 遇到方向不一致候选继续尝试下一候选，单目标或所有候选失败才进入同单元重采。
- 原因：后置门禁会丢弃同一 T4 Task 内可能存在的一致候选，增加无意义重采；继续候选更符合多车位任务已有稳定顺序解析边界。
- 契约影响：默认门禁下新生成 T4 元数据策略名使用 `first_consistent_plannable_candidate`，显式关闭门禁仍为 `first_plannable_candidate`；旧 NPZ 不迁移，P4.1 最优 oracle 边界不变。对应更新 `EXPTRAJ-DATA-004` 与 `EXPTRAJ-DATA-010`。

### 20:34:17 · 完成检查点

- 状态：完整完成。任务机动要求已从只记录元数据升级为可独立复算的审计契约，并在 Task 生成和 partial→正式归档两个边界实施门禁；旧正式数据未被覆盖，已明确审计为不合格。
- `dataset/maneuver.py`：修改前无统一方向判定所有者 → 新增前进/倒车段距离、请求方向占比、换向次数、默认 50% 主导阈值、逐样本审计、归档汇总和严格门禁；非法/静止轨迹及阈值越界明确拒绝。
- `dataset/generator.py` / `build.py`：修改前任意可规划轨迹均可成为标签 → 默认拒绝机动不一致候选并以 `maneuver_inconsistent` 稳定原因进入原难度单元重采；T4 跳过不一致候选；新样本写 `task_meta.dataset.maneuver_audit`；显式关闭生成门禁可回退但仍记录 FAIL。
- `scripts/build_dataset.py` / `inspect_dataset.py`：修改前构建只检查数量/写盘，inspection 只输出长度与全局倒车率 → partial 写出后独立复算，只有一致率 100%、无效 0、缺失 0 才晋升；inspection 增加分层审计和 `--require-maneuver-consistency`，验收图同时显示请求方向占比及双 PASS/FAIL。
- 现有数据审计：`data/task_dataset/maneuver_audit/` 保存三 split 报告；train 1836/2400（76.5%）、val 228/300（76.0%）、test 228/300（76.0%）一致，总计 708 条不一致，严格门禁按预期拒绝；0 无效、0 缺失。原 3000 条 NPZ 保留作为问题证据，不允许直接进入 P3.1 正式训练。
- 验证：实现前 6 类缺口失败证据成立；最终定向 11 项、全仓 182 项 unittest、语法编译、Planner Dataset Spec 检查与差异空白检查通过。真实 10 条 `gate_smoke_v2` 构建经生成门禁、同单元重采和 partial 独立门禁后 train/val/test 均 100%，manifest 保存审计结果；增强图人工确认 GOAL/MANEUVER 双 PASS。
- 契约/文档：`EXPTRAJ-DATA-009`、`EXPTRAJ-DATA-010` 均完成并追溯为 ✅，DATA-004 的 T4 默认策略更新为 `first_consistent_plannable_candidate`；README 同步严格检查命令，ARCHITECTURE 同步方向判定所有权与双门数据流。工作日志：`docs/worklogs/2026-08-24.md#T-201407`。
- 兼容/迁移/回退/安全：NPZ 保持 schema v2 且原数组键不变，元数据只加派生字段；旧归档不原地迁移，正式替换需由原计划重建；`enforce_maneuver_consistency=False` 仅作为显式回退。无个人数据、外部通信或破坏性操作；本轮没有启动耗时的 3000 条重建。

## T-204844 · L4 · 履带钻机运动学与原地转向可行性优化

### 20:48:44 · 开始检查点

- 状态：进行中。
- 修改前基线：专家规划已采用连续位姿 Hybrid A* 与 Reeds–Shepp 解析扩展，轨迹点为车体中心位姿并按 6×3 m 矩形包络逐点碰撞；但运动基元只允许非零线速度下的弧线前进/倒车，最小转弯半径默认由 `|plan_v/max_omega|` 得到 0.625 m，不能表达履带钻机以两履带几何中心为旋转中心的原地转向，也没有对旋转扫掠体积、轨迹运动学可行性和中间车体姿态做独立门禁。
- 目标状态：建立履带钻机低速运动学契约，显式支持 `v=0, omega≠0` 的原地转向；原地旋转每个插值姿态都以两履带几何中心为参考点检查完整矩形包络；为专家轨迹增加可复算的运动学/碰撞可行性审计和数据门禁，并在抽检图显示中心采样点、中间车体姿态与可行性结果。
- 已确认边界：底盘为履带转向钻机，允许原地旋转，旋转中心固定为两条履带的几何中心；保持三阶段公共控制接口 `[v, omega]`，不引入轮式自行车或铰接模型，不在本轮建立质量、惯量、土壤沉陷和履带滑移的高阶动力学。
- 预计影响：规划器运动基元与解析终连策略、车辆配置、专家轨迹审计/生成/归档、inspection 图与统计、相关测试、Planner Dataset/接口契约及架构不变量；旧 NPZ 不原地迁移，需审计或重建后才能作为新模型正式训练数据。

### 20:51:40 · 实施前物理边界待确认

- 原计划 → 当前状态：已确认采用包含原地旋转基元的履带运动学，并以高旋转代价抑制不必要搓地；实现前暂停在碰撞外廓契约，等待确认移动/泊车状态下上装是否收拢、现有以履带几何中心为中心的 6×3 m 矩形是否覆盖全部旋转外廓。
- 原因与证据：旋转中心只决定位姿积分参考点，钻臂、桅杆、支腿或非对称上装会改变围绕该中心的扫掠体积；若先按居中矩形实现，再改为偏置矩形或多边形，将同时返工规划碰撞、审计、可视化和验收数据。
- 已确定方案：采用 `v=(v_R+v_L)/2`、`omega=(v_R-v_L)/b_eff` 的低速理想履带运动学边界，允许 `v=0, omega≠0`；原地旋转只在完全停车时发生，按连续插值姿态检查完整外廓，Reeds–Shepp 仅保留为可行的平移弧线候选，不再代表履带底盘全部可达集合。

## T-204844 · 继续

### 20:58:58 · 用户边界确认

- 待确认 → 已确认：理论车型采用以两履带几何中心为中心的 6×3 m 矩形包络；车辆外廓、线速度、角速度及影响 Hybrid A* 搜索的参数必须集中为可变配置，用户后续只修改配置即可适配实际钻机。
- 实施边界：默认值用于当前理论与数据验证，不宣称等同某一实车标定；配置必须同时驱动规划器、运动模型、MPC、碰撞检查和 inspection，避免只改变图形而规划约束仍使用旧值。
- 验收方向：新增规范与失败证据后实施原地旋转、连续扫掠碰撞、运动学审计/双层数据门禁及中心轨迹可视化；旧数据保持只读，模型版本不符时不得直接晋升为新正式训练集。

### 21:31:50 · 默认角速度职责拆分

- 原默认 → 新默认：不再让原地旋转保守角速度 0.35 rad/s 同时限制平移差速弧线；理论配置使用 `plan_max_omega=0.8 rad/s` 控制平移运动基元与 Reeds–Shepp 半径，保留 `pivot_omega=0.35 rad/s` 控制停车原地旋转。
- 原因与证据：二者共用 0.35 rad/s 时，6×3 m 钻机的解析平移半径从 0.625 m 放大到约 1.43 m；默认配置的 S3/S5 非直线入口回归虽最终通过，但单项耗时 84.277 s，不能满足数据生产的搜索性能。履带底盘允许小半径差速行驶与较慢原地搓转，分离配置更符合两个动作的不同物理/代价边界。
- 影响范围：只调整可编辑理论配置的默认数值；字段、模型版本、运动学方程、执行上限和门禁契约不变。后续重新运行紧凑场景、真实小批量构建和全仓回归，以最终配置结果取代此前 0.35 rad/s 平移烟测数值。

### 21:38:16 · 最终状态与变更映射

- `sim/vehicle_config.py` / `configs/vehicles/tracked_drill_rig.json`：修改前只有代码内固定的 6×3 m 矿卡参数 → 新增 `tracked_pivot_v1` 可编辑 JSON，统一定义居中矩形外廓、执行速度、规划速度/角速度、原地旋转、碰撞裕度与 Hybrid A* 离散/采样参数；保留 `MINING_TRUCK` 和旧预设名作为兼容别名。
- `planner/hybrid_astar.py` / `planner/collision.py` / `planner/profile.py`：修改前仅前后差速弧线、角点式碰撞与纯平移速度剖面 → 增加绕履带几何中心的左右原地旋转基元、履带 rotate-straight-rotate 近目标候选、完整有向矩形与连续扫掠检查，并为零位移转向生成停车且有物理持续时间的速度剖面。
- `dataset/generator.py` / `dataset/feasibility.py` / `dataset/inspection.py`：修改前专家标签缺少模型/运动学/扫掠证据且验收图仅突出轨迹线 → 生成期复核速度、侧滑残差、固定旋转中心、端点和碰撞，归档晋升时独立复算并核对模型版本；验收图显示全部中心点、旋转点、中间车体包络、起终姿态与 GOAL/MANEUVER/FEASIBILITY 三项状态。
- `scripts/` / `experiments/run_experiment.py`：修改前生产入口分别依赖固定默认值 → build/generate/train/closed-loop/inspection 统一注入履带钻机配置，build partial 只有同时通过机动一致性与轨迹可行性门禁才晋升，CLI 支持 `--vehicle-config`。
- `spec/planner_dataset.md` / README / DESIGN / ARCHITECTURE：修改前契约仍以差分矿卡描述 → 增加 `EXPTRAJ-TRACK-001/002`、`EXPTRAJ-DATA-011/012`，明确理论履带运动学、配置所有权、完整外廓碰撞、数据迁移和实车动力学非目标。
- 验收：实现前配置加载、原地旋转、扫掠碰撞、归档门禁与剖面测试均按预期失败；最终 191 项全仓 unittest 全部通过，语法编译、Planner Dataset Spec 检查与 `git diff --check` 通过；默认配置 S3/S5 回归 20.027 s 通过。
- 真实小批量：`tracked_gate_smoke` 生成 train/val/test=8/1/1，三 split 的机动一致率和轨迹可行率均为 100%；S9 抽检图人工确认中心轨迹、中间 6×3 m 包络、起终姿态及三项 PASS 清晰可见。
- 兼容与剩余风险：NPZ 数组 schema 保持 v2，只增加任务元数据；旧 3000 条归档因缺少 `tracked_pivot_v1` 模型与生成期碰撞证据不能通过新严格门禁，进入 P3.1 前必须重建。当前模型是低速理想履带运动学，未包含土壤滑移、履带沉陷、制动延迟或上装非收拢外廓，实车使用前仍需按标定值修改 JSON 并重新验证。
