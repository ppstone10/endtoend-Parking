# 修改轨迹 · 2026-08-21

## T-161234 · L4 · 建立无人矿卡端到端自动泊车项目骨架

### 16:12 · 开始

- 状态：进行中
- 修改前基线：空目录，仅有《无人矿卡端到端自动泊车系统开发文档.docx》与已安装的 adaptive-agent-development 工作流。
- 目标状态：搭建可执行的三阶段迁移骨架 —— 统一接口（interfaces）、Sensor2BEV、Python 仿真环境，以及 README/DESIGN/ARCHITECTURE 文档与验证测试。
- 已知边界：本任务只交付骨架与接口契约，不实现 MineParkingNet 训练、MPC、CARLA 接入与实物底盘。
- 预计影响范围：根目录新文件、interfaces/、sensor2bev/、sim/、tests/、docs/、spec/。

### 16:15 · 前置调研

- 网络调研命中率低（arXiv 引用页、MDPI、CMU 链接多次 404/无关），无法逐篇核验论文细节。
- 但关键设计点均属成熟先例，可直接采用：LiDAR→BEV 采用 ROI 裁剪→降采样→RANSAC 地面滤除→栅格投影，生成占据/高度/密度多通道（KITTI 系 BEV 惯例）；车辆运动模型采用差分驱动（对应统一控制指令 [v, omega] 与最终履带平台）；专家轨迹生成采用 Hybrid A*（Dolgov 2008）；轨迹跟踪采用 MPC。
- 设计决定：控制接口使用线速度 v 与角速度 omega（差分模型），sim 阶段直接实现差分运动学。
- 未验证缺口：端到端网络 MineParkingNet 的具体结构留待阶段四依据文献细化，骨架阶段只定义接口。

### 16:22 · 完成

- 状态：完整完成。
- 文件映射（空目录 → 现状）：
  - 根：新增 `README.md`（用途/安装/运行）、`DESIGN.md`（目标/行为/验收）、`ARCHITECTURE.md`（组件/数据流/不变量）、`.gitignore`、`AGENTS.md`（由工作流安装器生成）。
  - `interfaces/`：新增统一接口 `sensor.py`（LiDARFrame/CameraFrame/CameraIntrinsics）、`bev.py`（BEVTensor）、`state.py`（VehicleState/GoalPose）、`trajectory.py`（Trajectory/TrajectoryPoint）、`control.py`（ControlCmd）。
  - `sensor2bev/`：新增 `lidar_bev.py`（ROI 裁剪→降采样→地面滤除→栅格投影，occupancy/height/density 三通道）。
  - `sim/`：新增 `environment.py`（矩形障碍物+边界+射线投射）、`vehicle_model.py`（差分驱动模型）、`sensor_sim.py`（模拟 LiDAR）。
  - 骨架占位：`model/network.py`（MineParkingNet）、`controller/mpc.py`（MPC）、`planner/hybrid_astar.py`、`dataset/generator.py`，仅定义接口契约，具体实现抛出 NotImplementedError。
  - `scripts/run_sim.py`：阶段一数据流演示。
  - `tests/`：19 个 unittest 全部通过；`spec/interfaces.md` 完整档 Spec（IFACE-* 规范 ID）并通过 check_spec 校验。
  - `docs/traces/2026-08-21.md`、`docs/worklogs/2026-08-21.md`。
- 关键方案取舍：控制指令定为差分驱动形态 `[v, omega]`（对齐最终履带平台）；接口仅依赖 numpy。
- 已完成：接口契约、Sensor2BEV、Python 仿真、文档、Spec、测试。
- 推迟：阶段三（专家轨迹）、阶段四（网络训练）、阶段五（MPC）、阶段六（CARLA）、阶段七八（实物）。
- 验证覆盖：`python -m unittest discover -s tests -v` 19 项通过；`python scripts/run_sim.py` 输出 LiDAR 360 点、BEV (3,100,100)、109 个占据栅格；`audit.py` 通过；`check_spec.py` 通过。
- 未验证缺口：Camera→BEV 未实现（阶段一以 LiDAR 为主）；网络/MPC/实物仅在接口层，无行为实现。
- 关联文档：README、DESIGN、ARCHITECTURE、spec/interfaces.md、docs/worklogs/2026-08-21.md。

## T-171947 · L3 · 实现 Camera→BEV 与 LiDAR/Camera 融合

### 17:20 · 开始

- 状态：进行中。
- 修改前基线：sensor2bev 仅有 LiDAR2BEV（occupancy/height/density 三通道）；CameraFrame/CameraIntrinsics 仅有接口类型；sim 仅有模拟 LiDAR。
- 目标状态：实现 Camera→BEV（IPM/单应变换，目标区域/车位线通道）与 LiDAR/Camera BEV 融合，补齐阶段1 Sensor2BEV 收尾；扩展 BEV 通道契约并更新 Spec。
- 已知边界：只做传感器级后融合（通道叠加），不做网络特征级融合；模拟相机使用环境渲染图像。
- 预计影响范围：sensor2bev/camera_bev.py、sensor2bev/fusion.py、sim 模拟相机、tests、spec/interfaces.md、run_sim.py。

### 17:40 · 完成

- 状态：完整完成。
- 文件映射（无 → 新增）：
  - `sensor2bev/camera_bev.py`：Camera2BEV，IPM 单应反投影生成 `target` 通道。
  - `sensor2bev/fusion.py`：BEVFusion，通道拼接 + `vehicle` 轮廓通道。
  - `sim/camera_model.py`：CameraModel，地面↔像素单应。
  - `sim/sensor_camera.py`：SimulatedCamera，将泊车位目标区域渲染到图像。
  - `tests/test_fusion.py`：模拟相机/Camera2BEV/BEVFusion 测试。
  - 修改：`sim/__init__.py`、`sensor2bev/__init__.py`、`scripts/run_sim.py`、`spec/interfaces.md`（新增 IFACE-BEV-002、IFACE-FUSION-001）、ARCHITECTURE.md。
- 关键方案取舍：融合采用传感器级后融合（通道叠加），不做网络特征级融合；模拟相机与 Camera2BEV 共用同一套单应推导保证互逆。
- 已完成：Camera→BEV、LiDAR/Camera 融合、模拟相机、Spec 与追溯、测试。
- 推迟：车位线/车道线语义通道、多目标区域、多相机；网络特征级融合。
- 验证覆盖：`unittest` 23 项通过；`run_sim.py` 输出融合 BEV (5,100,100)，通道 occupancy/height/density/target/vehicle；`check_spec.py` PASS。
- 未验证缺口：Camera→BEV 依赖相机外参理想化假设（相机在车辆中心上方、无畸变）；真实标定留待 CARLA/实物阶段。
- 关联文档：ARCHITECTURE.md、spec/interfaces.md、docs/worklogs/2026-08-21.md。