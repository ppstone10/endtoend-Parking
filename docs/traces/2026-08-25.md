# 修改跟踪 · 2026-08-25

## T-214912 · L4 · 正式履带数据重建与后续 P3/P4 分批推进

### 02:03:29 · 范围升级与性能调查基线

- 状态：进行中；任务自 2026-08-24 21:49:12 开始，原计划只按既有链路重建 3000 条正式数据，用户后续要求完成后按可独立验收批次继续 P3/P4，并在额度低于 5% 时收口和交接。
- 修改前基线：3000 条 dry-run 配额正确；正式构建以单进程串行处理 train=2400，全部样本成功后才首次写 `train.partial.npz`。运行约 4 小时仍未完成 train，输出目录无检查点。
- 证据：构建进程 CPU 时间随墙钟持续增长、内存约 230–356 MiB 且保持响应；Hybrid A* 有 20000 节点上限，因此没有死循环证据。疑似主要热点为搜索内层对每个运动微步再次执行完整矩形连续扫掠插值，以及失败任务的多目标/重试放大。
- 原计划 → 新计划：不再盲等不可观察、不可恢复的全量运行；停止本次进程，用代表性任务做 `cProfile` 和计时基线，修复重复碰撞计算，并把正式构建改为有进度和可恢复检查点的批量流程后重新生产。
- 边界：不得通过降低 3000 条规模、放宽机动/轨迹门禁、减小 6×3 m 外廓或改变已确认 seed 来换取速度；旧正式数据保持只读，新产物仍写入独立目录。
- 预计影响：`planner/collision.py` / `planner/hybrid_astar.py` 的碰撞调用边界，`dataset/build.py` / `scripts/build_dataset.py` 的进度与恢复机制，相关性能/正确性测试及正式数据产物。

### 02:27:00 · 根因确认与恢复方案落地

- 性能证据：10 条基线约 1.34 s；定位到的 `S1_parking_lot-T1-0029-61dbc892` 单条为 36.42 s。该任务在性能剖析下产生约 1.01 亿次调用，其中 `swept_segment_free` 487347 次、`pose_free` 2038087 次，确认热点是搜索扩展和解析候选反复执行矩形扫掠碰撞，而非死循环、I/O 等待或内存泄漏。
- 实现调整：碰撞检测改为标量宽相位与矩形 SAT 精确判定，解析候选按代价逐个短路；任务请求的前进/倒车方向注入搜索代价和候选排序；单次规划加入配置化 8 s 墙钟预算并在昂贵碰撞检查前后校验。
- 构建调整：正式构建改为默认每 10 条生成、双门禁复核、原子检查点落盘；检查点身份绑定 seed、任务计划、重试参数、批大小和完整车辆模型，同参数重启复核并跳过完成批次，不同身份拒绝混用；split 最终由 schema v2 检查点重新补齐并合并。
- 回归证据：规划器、运动学审计、车辆配置和归档合并定向测试通过；20 条真实烟测完成 train=16、val=2 后在 S9/T2 的 5 次重采上限明确失败，同参数重启约 2 s 恢复全部 18 条完成样本且只重试未完成 test 批次，验证恢复语义成立。
- 正式状态：使用模型 `tracked_pivot_v2` 在独立目录 `data/task_dataset/tracked_v2_3000` 重启 3000 条构建；已越过原首个慢样本位置并持续输出批次进度，旧 `tracked_v1_3000` 未覆盖。
