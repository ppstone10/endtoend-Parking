# 2026-08-28 修改轨迹

## T-101408 · L4 · 数据集失败重采可恢复化

### 10:14:08 · 开始

- 状态：进行中。
- 修改前基线：固定批次只有成功后的 NPZ/JSON 检查点；批内任务超过重试上限时，失败任务 ID、重采游标和累计原因全部丢失，相同命令恢复后会重放同一确定性失败序列。现有 v4 正式构建已完成 293 个批次/1465 条 train 样本，第 294 批在 `S4_dump_area/T3` 中止。
- 目标状态：每次失败都原子持久化未完成批次的重采游标与审计计数；身份一致的重启从新游标继续，成功批次落盘后才清除临时状态。
- 边界：不改任务配额、准入矩阵、车辆模型、规划时限、NPZ schema 或已完成检查点；不把跨难度/跨单元重采作为恢复手段。
- 预计影响：`dataset/build.py`、`scripts/build_dataset.py`、定向回归测试、`spec/planner_dataset.md` 及检查点恢复事实文档。

### 10:29:12 · 完成

- 状态：完整完成；规范 `EXPTRAJ-DATA-015` 证据完整。
- 修改映射：`dataset/build.py` 从仅依赖计划/已用 ID 计算替代索引，变为可接收持久排除 ID 与最小 sample index；`scripts/build_dataset.py` 从“失败即丢失批内进度”变为每次失败前原子写 `part-*.retry.json`、恢复时校验批次身份并从游标续采，成功 NPZ/报告落盘后清理临时状态。
- 现有数据迁移：293 个已完成批次/1465 条 train 样本未改；第 294 批建立 schema v1 retry 状态，保守排除该批 5 个原任务与已知的 104–123 替代 ID，以 `legacy_retry_before_persistence=21` 保留旧进程但不伪造具体失败分类；恢复探针首个任务为 index 124。
- 验证：`tests.test_dataset_build` 15 项通过，两次失败进程的 task ID 互斥；全仓 232 项通过；`check_spec.py spec/planner_dataset.md` PASS；3000 条 dry-run 仍为 2400/300/300 且计划统计未改；`git diff --check` 通过。
- 契约与兼容：不改 identity、NPZ、manifest、准入矩阵、难度配额、规划时限和每次命令重试上限；无 retry 文件的旧目录按空状态处理，损坏/错配状态明确拒绝。
- 回退与恢复：代码回退不会损坏成功检查点，但旧代码会忽略 retry 游标并重现重放问题；指定现有输出目录与原参数即可恢复。本轮未执行长时真实生成，留给用户按约定运行。
- 安全与隐私：仅读写项目内仿真检查点，无个人数据、外部通信或实车控制影响。
- 持久知识：已更新 `ARCHITECTURE.md`、`LEARNING.md`、`spec/planner_dataset.md`；任务索引见 [`docs/worklogs/2026-08-28.md`](../worklogs/2026-08-28.md)。

## T-110656 · L4 · 卸载区车辆尺度与安全净空契约

### 11:06:56 · 开始

- 状态：进行中。
- 修改前基线：正式 v4 构建在第 304 批再次因 `S4_dump_area/T3` 连续 10 次重采耗尽而停止；失败游标已保存，因此没有丢失已完成批次，但继续运行只会在同类几何上消耗大量时间。
- 调查证据：精确复现任务 `S4_dump_area-T3-0294-429d0033` 时，6×3m 车体、0.2m `collision_margin` 和 8s 预算产生约 2974 万次调用并超时；仅把 margin 置零即约 0.03s 成功，而保持 margin 并把车尾物理净空从 0.3m 增至 `0.3m 停车余量 + 0.2m 规划安全裕量` 后约 0.02s 成功。
- 根因判断：问题不是 Hybrid A* 栅格分辨率，也不是 6×3m 车体绝对过大，而是场景目标按裸车体只留 0.3m、规划器又对车体膨胀 0.2m，导致卸载目标有效净空只剩 0.1m；解析连接在挡墙前频繁被拒并触发大范围离散搜索。S9 卸载位另有 `+90°` 令车头而非车尾朝挡墙的语义错误。
- 外部尺度基线：NIOSH 双向运输道路建议至少为最大车辆宽度的 3.5 倍；MSHA 卸载指导要求转入倒车前与挡墙保持至少一车宽、车辆间至少两车宽、停车不得依赖挡墙制动。当前 12m 主路相对 3m 车宽为 4 倍，整体道路并未偏窄；需要修正的是卸载排布和安全停车目标。
- 目标状态：保持实际 6×3m 车体和 0.2m 安全膨胀；S4/S9 卸载位使用同一车辆相对尺度、同一车尾朝挡墙坐标约定和显式净空公式；任务元数据携带几何版本，旧检查点不与新几何混建。
- 边界：不降低碰撞裕量、不增加 8s 单次预算、不调整 Hybrid A* 分辨率、不删除现有 v4 检查点或已生成样本；几何变更后先重新校准，再由用户执行长耗时正式构建。

### 11:29:52 · 重要调整与阶段完成

- 状态：实现与本地验证完整完成；v5 专家准入等待用户执行全能力校准后再定，正式 3000 条生成尚未开始。
- 方案演进：修复净空后，S4/T3 连续 294–303 探针仍有 2/10 在约 28m 跨排任务上耗尽 8s。A/B 将解析接管范围从 12m 提到任务 T3 上限 30m 后，两项均约 0.013s 成功；因此没有继续扩大场景或降低碰撞要求，而是把长距开放场地的解析接管范围纳入统一车辆配置，并将专家模型升级为 `tracked_pivot_v5`。
- 场景映射：`sim/scenes.py` 从固定 6m 卸载位中心距、裸车体 0.3m 后缘净空和 S9 错误 `+90°` 航向，变为 3 倍车宽中心距、`collision_margin + 0.3m` 物理净空、S4/S9 统一 `-90°` 车头航向；双向主路至少 3.5 倍车宽。`sim/tasks.py` 将采样器车辆尺寸/margin 注入 S4/S9，`vehicle_relative_v1` 写入难度元数据和计划指纹。
- 规划与版本映射：`sim/vehicle_config.py`/JSON 从隐式 12m 默认解析范围变为显式 30m，模型版本 v4→v5；旧 v4 计划指纹 `631234…` 与新计划 `8659e9…` 不同，现有 1465 条样本、第 304 批 retry 游标和全部旧目录未删除、不会被新构建续用。
- 校准映射：`dataset/calibration.py` 与 CLI 新增显式全能力探测模式，忽略历史 v4 准入但不直接放宽正式准入；计划覆盖 39 个几何支持单元/117 case，报告返回后再据证据调整 `expert_maneuvers`。
- 使用与训练映射：README、训练 YAML、架构、经验、需求和长任务说明统一指向 v5 新校准/数据目录；正式构建命令明确置于校准报告审查之后。
- 验证：已知报错任务与 294–303 连续 10 个 S4/T3 前进样本全部成功，单条 0.007–0.021s；全仓 `unittest` 237 项通过；`spec/scene.md`、`spec/planner_dataset.md` 检查 PASS；`git diff --check` 通过。
- 外部依据：NIOSH 双向运输道路至少 3.5 倍最大车宽；MSHA 卸载指导要求倒车前保持至少一车宽、车辆间至少两车宽，并在挡墙前停车而非依靠挡墙制动。项目据此采用车辆相对比例，同时保留“仿真参数不替代现场制动/坡度/挡墙标定”的边界。
- 回退：代码、配置、准入和数据目录必须按 v4 或 v5 成套使用；不得把 v5 样本写入 `tracked_pivot_v4_3000`，也不得将两种几何归档合并为同一正式训练集。
- 任务索引：[`docs/worklogs/2026-08-28.md`](../worklogs/2026-08-28.md)。

## T-120028 · L4 · tracked_pivot_v5 校准后专家准入

### 12:00:28 · 开始

- 状态：进行中。
- 修改前基线：v5 全能力校准已完成 117/117 case，报告为非 partial、模型身份 `tracked_pivot_v5`、`probe_full_capability=true`；当前正式准入仍沿用 v4 的 32 单元规则，尚未消费新证据。
- 证据摘要：S4 全任务双向 15/15 成功且单 case 最慢 0.08s；S8/T2、T3 的前进各 2/2 且约 0.03–0.04s，倒车均 0/1；S3/T3 前进 2/2、倒车 0/1，但两次前进各经历一次重采；S9/T1 前进 1/2、倒车 0/1，S9/T3 前进 1/2、倒车 0/1，均不足以稳定准入。
- 目标状态：用完整 v5 报告建立保守且可追溯的正式准入；只开放方向证据稳定的新增单元，不以偶发单次成功或提高正式重试次数掩盖不稳定能力；3000 条计划仍保持 2400/300/300 和 S9 test 隔离。
- 预计调整：S3/T3 从倒车改为仅前进；S8/T2、T3 从排除改为仅前进；S9/T1 从仅前进改为排除。S5/T2、S7/T1/T3/T5、S9/T3 保持排除，S5/T1/T5、S8/T5、S9/T5 等既有方向限制按 v5 证据保持。
- 边界：不改车辆/场景/规划器、NPZ schema、数据量、split、重采机制或规划预算；不启动正式长任务，不修改/删除 v4 数据或 v5 校准证据。

### 12:06:32 · 完成

- 状态：完整完成；`EXPTRAJ-DATA-016` 证据完整，正式 v5 构建已具备准入与命令条件，但按约定未由 Agent 启动长任务。
- 准入映射：`dataset/build.py` 从 v4 的 28 普通+4 S9 单元，变为 v5 的 30 普通+3 S9 单元；S3/T3 改为仅前进，S8/T2/T3 新增仅前进，S9/T1 改为排除，其余排除和方向限制按完整报告保持。
- 取舍：S5/T2 的唯一倒车成功先经历一次超时，S9/T1/T3 前进均只有 1/2 case 成功，因此没有用偶发成功放宽；S8/T2/T3 前进各 2/2、无需重采且约 0.03–0.04s，证据足以恢复。
- 计划结果：3000 条 dry-run 为 2400/300/300；train/val 普通场景覆盖 30 单元，test 只含 S9/T2/T4/T5 各 100 条。S4/T1–T5 继续保留双向监督。
- 验证：准入与校准计划定向测试通过；S3/T3、S8/T2、S8/T3、S9/T5 四项真实生成均通过机动一致性与运动学/碰撞门禁；全仓 237 项通过；Spec 检查与 diff check 通过。
- 文档与契约：更新 `README.md`、`docs/train_long_runtime_diagnosis.md`、`LEARNING.md` 和 `spec/planner_dataset.md`；规范为 `EXPTRAJ-DATA-016`。
- 迁移与回退：v5 正式数据只写 `data/task_dataset/tracked_pivot_v5_3000`；v4 数据、检查点和 v5 校准证据均未修改。回退必须成套恢复 v4 模型/场景/准入/目录，不能混用。
- 安全与隐私：仅改变仿真专家任务准入，不改变实车安全声明，不涉及个人数据、外部通信或实车控制。
- 任务索引：[`docs/worklogs/2026-08-28.md`](../worklogs/2026-08-28.md)。

## T-142800 · L4 · S3 维修区场景可行驶性重构

### 14:28:00 · 开始

- 状态：进行中。
- 修改前基线：`S3_maintenance/T3_LONG` 在 v5 正式 3000 条构建的 train 第 194 批（part-00193）反复耗尽 10 次重采；retry 证据为 39 次失败（36 次 8s 规划超时 + 3 次未找到可行轨迹），全部集中在 `S3_maintenance-T3`。v5 校准报告本已预警 S3/T3 前进"2/2 但各经历一次重采"，正式构建证明该单元在当前几何下系统性不可规划。
- 用户授权方向（替代排除单元方案）：不删除 S3/T3，而是溯源真实矿区维修保养区的通行语义，把 S3 复原为可行驶场景，使 T1–T5 全档位可行可覆盖；场景身份（维修区、三面墙+立柱+设备、紧净空倒车入位、单侧余量 0.5~0.8m）保持不变。
- 目标状态：S3 几何按真实维修区布局重构（加宽/加深作业道或引道，扩展 spawn 覆盖），使 T3(15–30m) 的前进接近+倒车入位可被专家规划稳定求解；计划指纹变化，新数据写入新目录并重新校准。
- 边界：不改车辆配置、Hybrid A* 算法、NPZ schema、批处理/重采机制；沿用 S4/S9 的车辆相对尺度与 geometry profile 版本化模式。
- 预计影响：`sim/scenes.py`、`sim/tasks.py`（如需）、`dataset/build.py`（机动/准入）、`spec/scene.md`、`spec/planner_dataset.md`、`ARCHITECTURE.md`、`README.md`、`LEARNING.md`、定向测试与校准/构建命令。

### 14:32:00 · 范围扩大

- 用户要求：诊断不限于 S3/T3，扩展到全部复杂场景；对每个"场景×任务类型×机动方向"单元评估可规划性、失败类型与根因，识别哪些是场景几何缺陷（需复原为可行驶布局）而非单纯的规划器参数问题。
- 新增证据：S3 起点扩展实验显示两种失败模式——(1) 起点距外墙 < 车辆原地旋转扫掠半径（约 3.6m）时立即"未找到可行轨迹"（被困死）；(2) 有空间但 bay 入口（立柱+0.6m 余量）机动在 8s 内无法收敛（超时）。S3 当前 spawn 区让 T3 起点挤在窄通道角落且部分贴近墙。
- 计划：编写跨场景×任务×机动方向的并行诊断扫描，输出成功率/超时率/无解率/被困死率与平均耗时，据此按单元分类（几何缺陷 / 规划参数 / 可规划），再决定场景复原范围。

### 15:08:00 · 根因转向：规划搜索局限 + 采样不对齐，而非单纯场景空间不足

- 原计划：把 S3 作业道加深加宽、扩展 spawn 即可让 T3 可规划。实证否定：S3 原型在 20–22m 深作业道、无立柱、0.8m 净空、25s/10 万节点下，T3 成功率仍只有 33–50%；全部 Reeds-Shepp/tracked-direct 解析候选在远距离被紧 bay 口碰撞拒绝。
- 新证据：T3 起点与目标 bay 的横向偏移是决定性变量——偏移 ≤1.5m 且与 bay 轴线平行（yaw=-90°）时 REVERSE 8/8 全成功（avg 0.19s）；偏移 4–13m 或随机 ±25–70° 转角时普遍超时。S4/T3 开放挡墙场景可规划是因为目标区开放、无紧 bay。
- 根因判断：Hybrid A* 的欧氏启发式无法引导"远距离斜插进紧 bay 口"的搜索收敛；真实矿区语义中卡车就是沿 bay 轴线对齐后倒车入位，因此"对齐采样"才是可行场景，也符合实际驾驶行为。
- 方案调整：除四个场景几何复原（S3/S6/S7/S9+破碎槽）外，任务采样需为紧 bay 场景（S3/S5/S9 破碎槽）提供"沿目标 bay 轴线对齐"的起点采样（含倒车入位方向），并据此把 S3/T3 恢复为倒车监督。已授权范围确认后实施。

### 16:10:00 · 实施完成

- 状态：完整完成（场景复原 + 对齐采样 + 准入修订 + 验证 + 文档同步）；正式 3000 条生成按约定由用户执行，Agent 只做 100 条烟测。
- 用户授权：方案（1）全部四个场景 + 紧 bay 对齐起点采样 + S3/T3 恢复倒车。
- 修改映射：
  - `sim/scenes.py`：S3 从固定 4.2m bay/浅作业道变为车辆相对尺度（bay_w=车宽+1.2、作业道 3.5 倍车长、spawn 距外墙保持旋转扫掠半径、spot kind=maintenance_bay、geometry_profile=vehicle_relative_v1）；S6 岩石从 SW 象限移出接近走廊（东西两侧空地）；S7 靠岛净空计入 collision_margin 并车辆相对化；S9 破碎槽 CS0 朝向从 +90°（前进入槽）改为 -90°（倒车入槽，与 S5/规格一致）。
  - `sim/tasks.py`：S3/S7 加入 `_VEHICLE_SCALED_SCENES`；新增 `_AXIAL_BAY_KINDS` 与 `TaskSampler._sample_axial_start`，对 maintenance_bay/crusher_slot/fuel_bay 的 T2–T5 起点沿 bay 轴线对齐（yaw=gyaw，按机动从鼻前/鼻后侧 d 米外接近，横向受限；机动与几何不一致回退常规采样）。
  - `dataset/build.py`：S3/T3 恢复倒车（删除 FORWARD 覆盖）；S5/T2 从排除恢复为仅倒车；S9/T2、T5 改为仅倒车；排除集合删除 S5/T2。
  - 测试：test_tasks 新增对齐+可规划回归；test_scenes 新增 S3 车辆相对几何与 S7 靠岛净空断言；test_dataset_build/calibration 更新准入断言。
- 验证：受影响单元（S3/T1–T5、S5/T1/T2/T5、S6/T3、S7/T2、S9/T2/T4/T5）8s 扫描全部 ≥5/6（多数 6/6、毫秒级）；100 条端到端构建烟测仅 3 次失败重采、双门禁通过；全仓 239 项测试通过；`check_spec.py spec/scene.md spec/planner_dataset.md` PASS；3000 条 dry-run 保持 2400/300/300 与 S9 test 隔离。
- 契约与迁移：S3/S7 几何、采样与准入改变计划指纹，正式数据只写新目录 `tracked_pivot_v6_3000`；v4/v5 检查点、校准证据与旧目录保留作归档，不混用。车辆模型版本仍为 tracked_pivot_v5（规划参数未改）。
- 剩余风险：S9/T2、S9/T5 偶发 1/6 失败（多候选目标/前进与倒车入槽的残余不匹配）由同单元重采兜底；S3/T4 首次尝试 3/6（候选目标与对齐起点不完全一致）但全部在重采内完成。未在正式 3000 条上做全量长时验证。
- 持久知识：`spec/scene.md`（SCENE-SCALE-002）、`spec/planner_dataset.md`（EXPTRAJ-DATA-017、修订 DATA-016）、`ARCHITECTURE.md`、`README.md`、`LEARNING.md`、`docs/train_long_runtime_diagnosis.md`（§9.12）；任务索引见 worklog。

## T-171715 · L4 · v6 全准入可达性复核与加固

### 17:17:15 · 开始

- 状态：进行中。
- 用户目标：复核当前 S1–S9 场景是否仍满足初始设计，并在正式 3000 条长任务前确认每个准入场景×任务情况均有可操作规划空间，不接受用反复重采掩盖必然无解或高概率超时。
- 修改前基线：HEAD `1d9b865` 已完成 S3/S6/S7/S9 几何复原及紧 bay 轴线对齐采样；工作树干净；既有验收仅为受影响单元 5/6 或 6/6 加 100 条烟测，TRACE 已明确保留 S9/T2、T5 和 S3/T4 偶发失败风险。
- 独立复核证据：73 项场景、任务、规划、构建与校准定向测试通过；随后以正式车辆配置运行 34 个已准入单元×12 个确定性样本、关闭重采、单 case 10s 硬预算的密集校准，共 408 例，仅 390 成功、15 失败、3 个硬预算超时。S3/T3 为 12/12，说明本轮 S3/T3 修复有效；不稳定集中在 S1/T1/T2/T5、S3/T4、S7/T2、S9/T2/T4/T5，其中 S9/T2 仅 7/12、S9/T5 仅 8/12。
- 风险判断：当前版本尚不满足“每个准入情况稳定可规划”，不得启动正式长任务；需复现失败任务，区分目标类别/方向/占用与起点几何不一致、T4 多候选规划预算累积，以及真正的场景死区后再收紧契约并修复。
- 边界：不启动或覆盖正式数据集，不通过提高 8s 规划预算或增加正式重试次数掩盖问题；保持 6×3m 车辆、0.2m margin、原始 S1–S9 场景语义与 8:1:1 数据划分。

### 17:43:00 · 完成

- 状态：完整完成；当前 v7 已满足正式长任务前的全准入门禁，Agent 未启动 3000 条正式生成。
- 根因更正：v6 的剩余失败并非 S3/T3 场景仍无解；S3/T3 密集复核为 12/12。真正缺口是普通车位固定目标航向无法同时表达前进/倒车入位、轴线采样只检查远端起点而未检查目标到起点的连续 footprint 走廊、相邻占用可能截断入口，以及 T4 随机候选顺序先消耗不匹配目标的规划预算。
- 修改映射：`sim/scenes.py`/`sim/spots.py` 为 S1/S2/S9 普通车位增加 perpendicular/diagonal 语义并修正 S9 停车区基准入口航向；`sim/tasks.py` 按请求机动翻转普通车位倒车目标、以 0.1m 检查整段轴线走廊、按方向过滤占用后目标，并把 T4 起点参考目标置于候选首位；对应回归覆盖 15 个旧失败任务、目标航向和 T4 顺序。
- 验证演进：修改前 408 例为 390 成功/15 失败/3 硬预算超时；相同 34 单元×12、`max_retries=0`、10s case 预算重跑为 408/408、失败 0、预算超时 0，报告位于 `runs/dataset-calibration/tracked_pivot_v7_review_20260828/`。全仓 242 项测试通过；两份 Spec 检查 PASS；3000 dry-run 为 2400/300/300，S9 test 仍为 T2/T4/T5 各 100；`git diff --check` 通过。
- 场景需求结论：S1–S9 注册、图幅、车辆尺寸/车位净空、S3 三面墙+立柱+0.6m 单侧余量、S4/S9 挡墙安全净空、S5 窄槽、S6 设备尺度与接近走廊、S7 靠岛 margin、S8 称重台、S9 三功能区均通过现有语义与尺度回归；所有 34 个正式准入单元均有无重采真实专家轨迹证据。被能力矩阵明确排除的任务不进入正式计划，不能解读为当前训练数据中的无解样本。
- 契约与迁移：新增 `EXPTRAJ-DATA-018`；正式输出迁移到 `data/task_dataset/tracked_pivot_v7_3000`，不得续用或混合 v6 检查点。车辆/规划器配置仍是 `tracked_pivot_v5`，v7 表示数据任务计划版本而非重新缩小车体或放宽安全裕量。
- 剩余风险：408 例覆盖全部准入类别与难度轮换，但不是对无限随机 seed 的数学证明；正式构建仍保留同单元重采和失败游标作为运行时防护。若 v7 同一单元再次连续失败，应停止并返回 retry 证据，不应提高预算或重试次数掩盖。
- 任务索引：[`docs/worklogs/2026-08-28.md`](../worklogs/2026-08-28.md)。
