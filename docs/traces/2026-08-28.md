# 2026-08-28 修改轨迹

## T-101408 · L4 · 数据集失败重采可恢复化

### 10:14:08 · 开始

- 状态：进行中。
- 修改前基线：固定批次只有成功后的 NPZ/JSON 检查点；批内任务超过重试上限时，失败任务 ID、重采游标和累计原因全部丢失，相同命令恢复后会重放同一确定性失败序列。现有 v4 正式构建已完成 293 个批次/1465 条 train 样本，第 294 批在 `S4_dump_area/T3` 中止。
- 目标状态：每次失败都原子持久化未完成批次的重采游标与审计计数；身份一致的重启从新游标继续，成功批次落盘后才清除临时状态。
- 边界：不改任务配额、准入矩阵、车辆模型、规划时限、NPZ schema 或已完成检查点；不把跨难度/跨单元重采作为恢复手段。
- 预计影响：`dataset/build.py`、`scripts/build_dataset.py`、定向回归测试、`spec/planner_dataset.md` 及检查点恢复事实文档。

### 10:29:12 · 完成

- 状态：完整完成；规范 `EXPTRAJ-DATA-015` 证据完整。
- 修改映射：`dataset/build.py` 从仅依赖计划/已用 ID 计算替代索引，变为可接收持久排除 ID 与最小 sample index；`scripts/build_dataset.py` 从“失败即丢失批内进度”变为每次失败前原子写 `part-*.retry.json`、恢复时校验批次身份并从游标续采，成功 NPZ/报告落盘后清理临时状态。
- 现有数据迁移：293 个已完成批次/1465 条 train 样本未改；第 294 批建立 schema v1 retry 状态，保守排除该批 5 个原任务与已知的 104–123 替代 ID，以 `legacy_retry_before_persistence=21` 保留旧进程但不伪造具体失败分类；恢复探针首个任务为 index 124。
- 验证：`tests.test_dataset_build` 15 项通过，两次失败进程的 task ID 互斥；全仓 232 项通过；`check_spec.py spec/planner_dataset.md` PASS；3000 条 dry-run 仍为 2400/300/300 且计划统计未改；`git diff --check` 通过。
- 契约与兼容：不改 identity、NPZ、manifest、准入矩阵、难度配额、规划时限和每次命令重试上限；无 retry 文件的旧目录按空状态处理，损坏/错配状态明确拒绝。
- 回退与恢复：代码回退不会损坏成功检查点，但旧代码会忽略 retry 游标并重现重放问题；指定现有输出目录与原参数即可恢复。本轮未执行长时真实生成，留给用户按约定运行。
- 安全与隐私：仅读写项目内仿真检查点，无个人数据、外部通信或实车控制影响。
- 持久知识：已更新 `ARCHITECTURE.md`、`LEARNING.md`、`spec/planner_dataset.md`；任务索引见 [`docs/worklogs/2026-08-28.md`](../worklogs/2026-08-28.md)。
