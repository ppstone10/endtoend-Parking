# Agent 任务日志 · 2026-08-30

时区：Asia/Shanghai

## 11:01:10–11:04:09 · L0 · 完成 net-v1 正式训练结果、early stopping、任务排布、专家生成与监督训练链路核验：正式运行实际完成 9 epoch，第 4 epoch 最优，连续 5 轮未改善后正常早停；只读补充评估 S9 test，并识别固定顺序无 shuffle、teacher forcing/自由滚动差异、全零初始运动状态及方向代价语义等解释要点；文件：无。

## T-111305 · 11:13:05–11:24:22 · L3 · net-v1 预测误差分组与专家叠加诊断

- 状态：完整完成；新增单 checkpoint 可复现诊断入口，并对 v7 train/val/test 生成分组报告、全局最差和按任务最差预测—专家叠加图。
- 方法：沿用现有 checkpoint、局部坐标和 ADE/FDE 契约，保留逐样本自由滚动预测与停止 logits；按场景、任务、方向、噪声、相邻占用聚合，不引入新依赖或外部方法。
- 修改：新增 `metrics/prediction_analysis.py`、`scripts/analyze_predictions.py`、`viz/prediction_analysis.py` 与测试；同步导出、README、ARCHITECTURE、LEARNING 和 `MODEL-EVAL-003`。
- 验证：9 项开环/模型定向测试通过，末次预测诊断 2 项回归通过；Spec 检查与 diff check 通过；三 split 真实 CLI 均生成 JSON 及 PNG/PDF。
- 结论：train/val 自由滚动 ADE/FDE 分别为 3.459/5.853m 与 3.502/5.923m，排除普通验证过拟合为主因；停止命中率三 split 均为 0，误差主要集中于 T3、S4/S6 和前进任务，噪声不是稳定主因。
- 规范：`MODEL-EVAL-003` ✅；使用方式和模块边界已同步，teacher forcing/停止类别不平衡经验已进入 LEARNING。
- 剩余风险：当前仅完成开环诊断，尚未修改训练流程或重训；应先处理 batch shuffle、停止损失平衡和自由滚动暴露，再根据复训后的长距/复杂场景残差决定是否升级空间模型。

## T-113438 · 11:34:38–11:49:00 · L4 · 变长轨迹训练流程修正

- 状态：完整完成训练流程、检查点兼容和训练诊断修正；正式 30 epoch 复训留给用户执行，演进证据见 [TRACE](../traces/2026-08-30.md#t-113438--l4--变长轨迹训练流程修正)。
- 方案：采用确定性样本级 shuffle、动态平衡停止 BCE 和 1.0→0.2 线性 scheduled sampling；依据 PyTorch 官方损失/可复现性文档与 scheduled sampling 原始论文，不新增依赖。
- 修改：覆盖 `model/`、`training/`、四份 v7 YAML、相关测试，并同步 README、ARCHITECTURE、LEARNING 与 `spec/model.md`；正式输出隔离到 `runs/training/v7-flow-v2/`。
- 契约与恢复：完成 `MODEL-LOSS-002`、`MODEL-TRAIN-003`、`MODEL-REPORT-002`；旧 checkpoint 明确不兼容，新运行可从新目录 `last.pt` 恢复，专家数据和模型公开输出契约不变。
- 验证：249 项全量测试及最终 7 项定向回归通过，diff check 通过；v7 真实 1 epoch smoke 生成完整 checkpoint/JSON/PNG/PDF，val ADE/FDE 0.861/1.633m、停止率 99.67%。
- 风险：一轮 smoke 长度 MAE 仍为 42.31 点，正式训练需持续观察自由滚动与停止时刻；若仍集中于 T3/S4/S6，再评估模型空间编码调整。

## 12:50:46–12:51:57 · L0 · 完成 v7-flow-v2 net-v1 九轮早停复核：第 4 轮验证损失最优、其后连续 5 轮未改善，`patience=5` 正常触发且“已完成”表示训练正常结束；best 的 val/test ADE/FDE 为 0.585/1.109m 与 0.409/0.699m，确认流程修正有效，同时识别 15 轮 teacher-forcing 调度尚未完成、T3/S6 长轨迹平均提前停止为下一步训练契约问题；文件：仅任务日志，分析产物位于 `runs/openloop-analysis/v7-flow-v2/net-v1/`。

## T-130752 · 13:07:52–13:46:15 · L4 · 训练策略定型与可复现调优

- 状态：完整完成；以真实 v7 同 seed 消融定型唯一推荐训练流程，并交付自动 val 停止阈值校准、deployment checkpoint 与人工调优指南，演进证据见 [TRACE](../traces/2026-08-30.md#t-130752--l4--训练策略定型与可复现调优)。
- 取舍：淘汰 `1e-3` 累计方案和靠手调阈值补救；采用 `3e-4`、未平衡累计停止 BCE、15 轮计划采样课程、课程后 early stopping 和 val 自动阈值扫描，依据 PyTorch/Keras/scikit-learn 官方行为约束。
- 效果：12 epoch 调优 best 的 val/test ADE/FDE 为 0.312/0.552m 与 0.293/0.527m；部署校准后长度 MAE 23.84/26.83 点、停止率均 100%，T3/S6 保留为重点人工审查分组。
- 修改：覆盖 `model/network.py`、`training/`、v1/v2/smoke 与调优 YAML、训练测试；新增 `training/stop_calibration.py`、`tests/test_stop_calibration.py` 和 `docs/training_guide.md`，同步 README/ARCHITECTURE/LEARNING。
- 契约与恢复：`MODEL-LOSS-003`、`MODEL-TRAIN-004`、`MODEL-CAL-001` ✅；best/last 保持可恢复，deployment 显式不可续训，v3 输出与旧 v2 隔离，专家数据和轨迹公开输出不变。
- 验证：Spec 检查、6 份训练配置加载、258 项全量测试、末次 19 项定向回归、diff check 及 v3 全量 1 epoch smoke 通过，生成完整 checkpoint/JSON/PNG/PDF。
- 剩余风险：正式 v3 30 epoch 未由 Agent 启动；用户应按指南唯一命令运行并按固定准入线验收，未达标时一次只改一个参数轴。
