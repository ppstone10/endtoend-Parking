## 15:59:04–16:12:34 · L0 · 仅完成诊断：确认项目处于 M2 数据验收恢复、tracked_pivot_v3 数据完成 1600/3000，决定以自动标定、有界重试、任务级原子恢复和多进程生成替代串行盲等；文件：无。
## 16:23:31–16:24:47 · L0 · 仅完成方案效果评估：基于 32 逻辑处理器、当前 17.68 条/分钟及超时占比，给出自动标定、4–8 进程、有界失败、任务级恢复和 M3 并行推进的预期收益与边界；文件：无。
## 16:28:54–16:29:27 · L0 · 仅完成执行计划说明：明确冻结现有 1600 条、先改造生成编排、再做全分层探测、通过后无人值守生成并并行推进 M3，失败时集中修复而非逐批调参；文件：无。
## 16:34:03–16:34:16 · L0 · 仅完成职责澄清：确认此前长耗时仅为专家轨迹、BEV 与数据双门禁构建，端到端模型训练和模型效果验证尚未运行，当前无 Python 进程；文件：无。

## T-165333 · 16:53:33–16:57:58 · L3 · 专家数据生成优化方案沉淀

- **状态与结果：** 完整完成方案分析和知识沉淀；明确以能力校准、三层预算、微批次恢复和低频状态检查替代高重试串行生成及 Agent 持续轮询。
- **方法与调研：** 核对 2026-08-25 运行证据、现行生成器/规划器/检查点实现和 EXPTRAJ-DATA-006；参考 Python 3.12 官方进程池、多进程与 profiler 约束，确定先止损失败重采、后基准化多进程、最后 profiler 驱动微观优化。
- **主要结论：** 当前首要浪费仍是 8 秒规划超时被 CLI 默认 `max_retries=100` 放大；进程池可缩短墙钟时间，但不能修复不可达单元，也不能依赖 Future 等待超时取消已运行任务。
- **修改范围：** 更新 `docs/train_long_runtime_diagnosis.md`，新增待实现的优化分层、预算/熔断、检查点、并行边界、Agent 长任务协议和验收；新增 `LEARNING.md` 精炼可复用经验。
- **验证：** `git diff --check` 通过，Markdown 相对链接目标存在；本轮仅修改文档，未运行代码测试或性能基准。
- **文档影响：** 仅记录建议方案和执行约定，明确标注尚未实现；未修改 README、DESIGN、ARCHITECTURE 或 Spec 的当前能力状态。
- **剩余风险：** `max_retries=5`、30～50 秒任务预算、1/2/4 worker 等均为初始建议，需实现校准模式并用代表性慢单元基准后才能固化为默认值。

## 17:02:10–17:04:00 · L0 · 完成执行计划核验：确认 tracked_v3_3000_final 保留 1600/3000 条且仍绑定 max_retries=100、batch_size=10，当前应先运行带独立日志的 100 条有界探测，结果一次性复核后再决定全量；数据等待期间可同步推进 M3 框架，最终训练与验收后置；文件：无。

## 17:12:03–17:13:32 · L0 · 仅完成探测诊断：100 条任务 86.5 秒完成，但普通集仅覆盖 S1/S2，S3–S8 未进入计划；S9 的 10 条消耗约 67.5 秒并发生 6 次超时重采，仍复现复杂场景瓶颈；确认现有计划至少 count=300 才覆盖全部 30 个普通能力单元，不能据此次结果启动全量；文件：无。

## 17:15:54–17:16:20 · L0 · 完成后续职责划分：现有探测记录和旧检查点已足够，用户暂不需补数据或继续运行；建议先由 Agent 实现全能力单元校准、预算与报告，再由用户执行一次有界命令，期间并行推进 M3 框架，最终训练和验收等待正式数据；文件：无。

## 17:23:04–17:23:28 · L0 · 完成即时执行指令澄清：当前不存在满足全 35 单元、失败不中断和总预算要求的安全命令，用户此刻不应运行 count=300/3000；下一步先由 Agent 实现校准入口，交付后用户仅运行一条有界命令，运行期间再同步推进 M3；文件：无。

## T-172642 · 17:26:42–17:48:52 · L4 · 可恢复专家校准与 M3 准备

- **状态与交付：** 完整完成；交付全 35 专家能力单元校准 CLI、case 硬预算与中断续跑，同时完成 M3 模型注册表、v1/v2 和 Trainer 核心准备层；未运行长校准或正式训练。
- **方案与边界：** 校准与 8:1:1 split 配额解耦，每 case 独立进程和原子终态；旧 1600 条不变。M3 保持 v0 兼容，YAML/曲线/ADE-FDE 后置，不新增缺失的 PyYAML 依赖。
- **主要修改：** `dataset/calibration.py`、`dataset/components.py`、`scripts/calibrate_dataset.py`；`model/registry.py`、`model/variants.py`、`model/prediction.py`、`training/` 及对应测试。
- **契约与恢复：** `EXPTRAJ-CAL-001..003`、`MODEL-REG-001`、`MODEL-VAR-001/002`、`MODEL-TRAIN-002` 标记 ✅；identity 不一致拒绝恢复，当前未落盘 case 可重做，已完成项跳过，不发布 partial 为完整结果。
- **验证：** 全仓 210 项通过；最终调整后校准/Trainer 9 项定向通过；真实 S1 worker 成功；compileall、Spec 检查、CLI help 与 diff check 通过。
- **知识同步：** 更新 README、ARCHITECTURE、LEARNING、PLAN、长耗时诊断、planner/model Spec；完整变更映射见 `docs/traces/2026-08-27.md` 的 `T-172642`。
- **剩余风险：** 105 case 真实全量校准尚待用户执行；多进程正式数据生成、P3.2 YAML/曲线和 P3.3 正式指标/训练仍未实施，不能声称 v1/v2 已收敛或优于 v0。

## 17:56:02–17:57:04 · L0 · 完成一次性运行状态说明：确认校准正在运行且快照为 51/105、当前 case 为 S4/T3；命令通过 run_state、report、cells 和 checkpoints 持续落盘结构化日志，终端仅在结束或中断时输出摘要；依赖校准结论的能力单元决策等待完成，P3.2/P3.3 可在不监控校准的前提下并行推进；文件：无。

## T-175859 · 17:58:59–18:18:23 · L4 · M3 配置化训练与开环评估

- **状态与边界：** 完整完成不依赖校准结果的 M3 工作；未读取或轮询校准，未运行正式训练，未宣称 v1/v2 优于 v0。
- **交付：** 新增安全 YAML 训练入口、三模型示例配置、逐 epoch 日志、原子 history/best/last、训练曲线和 best checkpoint val 报告；新增统一 ADE/FDE/环绕航向 MAE、多 checkpoint 开环 JSON 与 PNG/PDF 图。
- **方案取舍：** 放弃自制 YAML 子集，使用 PyYAML SafeLoader；预测 horizon 不足时拒绝评估，避免截断造成虚假改善；完整 V3 的实际闭环轨迹明确后置。
- **依赖与兼容：** 新增 `requirements-training.txt` 并在指定 conda 环境安装 PyYAML 6.0.3；既有 `scripts/train.py`、v0 接口和数据/checkpoint 均不修改，无数据迁移。
- **主要范围：** `training/`、`metrics/open_loop.py`、`viz/open_loop.py`、`scripts/train_model.py`、`scripts/eval_openloop.py`、`configs/training/` 及 5 个对应测试；完整映射见 `docs/traces/2026-08-27.md` 的 `T-175859`。
- **验证：** 12 项首轮定向、兼容收尾后 9 项定向、全仓 221 项均通过；CLI help、compileall、MODEL Spec、`pip check`、PyYAML/matplotlib 导入和 diff check 通过。
- **规范与知识：** `MODEL-CONFIG-001`、`MODEL-REPORT-001`、`MODEL-EVAL-001/002` 均为 ✅；同步 README、ARCHITECTURE、LEARNING、PLAN 和 MODEL Spec。
- **回退与风险：** 新入口和依赖可独立移除，不影响旧演示训练；正式 train/val 数据、三模型真实训练、优劣结论和完整 V3 闭环图仍待校准与正式数据完成。

## 18:26:39–18:29:36 · L0 · 完成校准与 M2 一次性验收：105/105 case 终态完整、88 成功/17 失败、无 30 秒硬预算超时，运行机制符合契约但 9/35 单元非全成功且 S7/T1、S9/T3 为 0/3；确认旧 3000 条缺少最新车辆/机动/可行性证据，v3 正式集仅完成 train 1600/2400、val/test 未开始，因此 M2 处于校准完成但能力准入和最终数据重建待办；现有 15 张专家轨迹验收 PNG 可展示，正式网络训练曲线尚不存在；文件：无。

## T-215704 · 21:57:04–22:06:23 · L3 · 专家轨迹旋转可视化与代价调整

- **状态与交付：** 完整完成代码、契约和运行说明；原地旋转按事件显示发生位置、LEFT/RIGHT、累计角度及侧栏汇总，数据统计新增旋转事件分布；按约定未启动长耗时校准或 3000 条生成。
- **方案与边界：** 沿用既有 Hybrid A* 时间成本和 inspection 模式，无需网络调研；默认/前进任务中倒车或反方向、以及原地旋转相对常规前进/小转向按约 2:1 计，显式倒车监督任务仍保留倒车主导轨迹以满足能力覆盖和机动门禁。
- **主要修改：** `planner/hybrid_astar.py`、`dataset/inspection.py`、`sim/vehicle_config.py`、`configs/vehicles/tracked_drill_rig.json` 及对应 planner/dataset/config 测试；专家模型升级为 `tracked_pivot_v4`，旧 v3 检查点不混用。
- **验收与验证：** 实现前回归按预期 1 失败/1 导入错误；实现后定向 33 项通过，Spec 检查 PASS，全仓 225 项通过，diff check 无空白错误；真实旧归档 180° 样本预览已人工确认方向/角度和侧栏可辨认。
- **规范与知识：** `EXPTRAJ-PLAN-001`、`EXPTRAJ-DATA-012` 为 ✅；同步 README、ARCHITECTURE、`spec/planner_dataset.md` 和长任务诊断文档中的 v4 生成、检查与留痕命令。
- **数据记录与风险：** 后续应保留校准 `report.json/cells.csv/run_state.json`、正式 `manifest.json/.checkpoints/NPZ` 与各 split `summary.json/PNG`；v4 全能力校准和正式数据分布尚待用户执行，当前不宣称大规模旋转/倒车占比已改善。

## 22:10:11–22:11:23 · L0 · 仅完成旋转方向复核：真实样本轨迹为 -180° 后 +3.396°、最终局部航向 -176.604°，车体数值与多边形朝向正确；确认固定初始坐标与掉头后车体坐标的“左”语义相反，同时发现环形箭头对正负旋转使用相同弯曲参数，视觉方向未可靠区分，属于绘图符号缺陷；文件：无。

## 22:17:15–22:17:31 · L0 · 仅完成车体/屏幕坐标复核并更正前次判断：轨迹局部系为前方 x、左方 y、正 yaw 左转，-180° 后 +3.396° 的最终车头应在直观俯视图向下且向右偏；inspection 直接以 left_y 作为向右增长的屏幕横轴，造成左右镜像，叠加既有环箭头方向缺陷，因此当前图片方向确有问题；文件：无。

## T-222032 · 22:20:32–22:46:35 · L5 · 专家可视化坐标修复与旧数据归档清理

- **状态与结果：** 完整完成；修正车体左正坐标的屏幕镜像和原地旋转箭头，建立 7 条真实 v3 参考样本归档，并完成授权范围内的旧数据清理。
- **授权与安全边界：** 用户明确授权删除大部分旧数据；仅对 `data/task_dataset` 下 20 个已验证显式目标执行删除，归档目录排除，未涉及个人数据、外部通信、源码或训练模型。
- **修改范围：** `dataset/inspection.py`、`scripts/archive_visual_references.py`、相关测试，三份 `configs/training/net-v*.yaml`，以及 README/ARCHITECTURE/LEARNING/Spec/长任务说明；详细映射见 [`T-222032`](../traces/2026-08-27.md)。
- **数据变化：** 912 文件/27,855,463 字节 → 12 文件/1,758,656 字节，删除 26,096,807 字节；剩余归档含 T1–T5、前进/倒车、大角度旋转、统计、PNG、来源和 SHA-256。
- **契约与迁移：** `EXPTRAJ-DATA-012/013` 已采纳且追溯为 ✅；旧全量无法从参考归档恢复，后续必须新建 `tracked_pivot_v4_3000` 目录生成，训练配置在 v4 NPZ 出现前会按预期拒绝启动。
- **验证：** Spec PASS；全仓 228 项 unittest 通过；训练配置 4 项定向测试及三份 YAML 安全解析通过；关键真实 PNG 人工图审通过；归档重读、样本数、图片、哈希、删除后清单与字节数复核通过；`git diff --check` 通过。
- **恢复与风险接受：** 删除不可逆，只能重新生成旧全量数据；用户为破坏性清理的授权与风险接受者。本轮未执行长时校准、v4 全量生成或正式训练。
