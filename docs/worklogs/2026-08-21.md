# Agent 任务日志 · 2026-08-21

时区：UTC+8

## T-161234 · 16:12:34–16:22:01 · L4 · 搭建无人矿卡端到端自动泊车项目骨架

- 状态与结果：完整完成。空目录建成可运行骨架：统一接口、Sensor2BEV、Python 仿真、文档、Spec、测试。
- 方法：按 adaptive-agent-development 工作流接入（install.py + audit.py），L4 走 full-development 重大档；前置网络调研命中率低，关键设计点（LiDAR→BEV 栅格投影、差分驱动、Hybrid A*、MPC）均属成熟先例，直接采用并记录到 TRACE。
- 主要修改：根目录新增 README/DESIGN/ARCHITECTURE/.gitignore；`interfaces/`（9 个数据类型）、`sensor2bev/lidar_bev.py`、`sim/`（环境/差分模型/模拟 LiDAR）、`model/` `controller/` `planner/` `dataset/` 骨架占位、`scripts/run_sim.py`、`tests/`、`docs/traces/2026-08-21.md`。
- 关键方案取舍：控制指令定差分驱动形态 `[v, omega]` 对齐最终履带平台；接口仅依赖 numpy；Camera→BEV 留待后续阶段。
- 契约/数据影响：`spec/interfaces.md` 完整档，规范 ID `IFACE-SENSOR-001`/`IFACE-BEV-001`/`IFACE-STATE-001`/`IFACE-TRAJ-001`/`IFACE-CTRL-001`，全部 ✅。
- 验证：`python -m unittest discover -s tests -v` 19 项通过；`python scripts/run_sim.py` 输出 LiDAR 360 点、BEV (3,100,100)、109 占据栅格；`audit.py` 通过；`check_spec.py` 通过。
- 文档影响：新增 README、DESIGN、ARCHITECTURE、spec/interfaces.md；TRACE T-161234 同编号。
- 剩余风险：网络/MPC/实物仅在接口层无行为实现；Camera→BEV 未实现；具体模型结构待阶段四细化。
- TRACE：docs/traces/2026-08-21.md#T-161234。

## 17:08:00–17:11:54 · L1 · 项目统一使用 conda 虚拟环境规则；文件：AGENTS.md、README.md。

## T-171947 · 17:19:47–17:40:01 · L3 · 实现 Camera→BEV 与 LiDAR/Camera 融合

- 状态与结果：完整完成。补齐阶段1 Sensor2BEV 收尾：Camera→BEV（IPM 单应反投影）与 LiDAR/Camera 通道级后融合。
- 方法：沿用既有模块模式（LiDAR2BEV 的栅格投影与接口）；单应几何沿用成熟 IPM 公式，未触发新调研。
- 主要修改：新增 `sensor2bev/camera_bev.py`（Camera2BEV）、`sensor2bev/fusion.py`（BEVFusion）、`sim/camera_model.py`（CameraModel）、`sim/sensor_camera.py`（SimulatedCamera）、`tests/test_fusion.py`；修改 `sim/__init__.py`、`sensor2bev/__init__.py`、`scripts/run_sim.py`、`spec/interfaces.md`、`ARCHITECTURE.md`。
- 关键方案取舍：采用传感器级后融合（通道叠加），不做网络特征级融合；模拟相机与 Camera2BEV 共用同一套单应推导保证互逆；相机外参用理想化假设（车辆中心上方、无畸变）。
- 契约/数据影响：Spec 新增 `IFACE-BEV-002`（target 通道）与 `IFACE-FUSION-001`（融合通道顺序 occupancy/height/density/target/vehicle 与异常校验），全部 ✅。
- 验证：`unittest` 23 项通过；`run_sim.py` 输出融合 BEV (5,100,100)；`check_spec.py` PASS。
- 文档影响：ARCHITECTURE 更新 Sensor2BEV/仿真组件与数据流；TRACE T-171947 同编号。
- 剩余风险：Camera→BEV 依赖理想化相机外参，真实标定留待 CARLA/实物阶段；车位线/多目标区域/多相机未实现。
- TRACE：docs/traces/2026-08-21.md#T-171947。

## 17:50:00–18:21:21 · L2 · 修复 Camera target 反投影偏移（T-171947 继续）

- 状态与结果：完整完成。修复 target BEV 区域偏离真实泊车位的问题，实测 X[2.1,7.9] Y[-1.5,1.5] 与理论位置一致。
- 根因与最小修复：单应本身正确；根因是演示/测试场景把泊车位放到相机视野外（偏航 53°>半FOV 38.7°），且 `_fill_polygon` 对图像外交点裁剪错误（负坐标切片 `0:-N` 造成全宽填充）；原场景泊车位还在障碍墙内。修复为交点裁剪到 `[0,w-1]` + 重设场景 + 补回归测试。
- 修改文件：`sim/sensor_camera.py`、`scripts/run_sim.py`、`tests/test_fusion.py`、`spec/interfaces.md`。
- 实际验证：25 项 unittest 全通过；run_sim 输出正常；check_spec PASS。
- 文档影响：IFACE-BEV-002 验收改为定量标准。
- TRACE：docs/traces/2026-08-21.md#T-171947。

## T-183629 · 18:36:29–18:51:03 · L3 · 实现 Hybrid A* 专家轨迹生成与训练数据集

- 状态与结果：完整完成。阶段3核心交付：差分驱动 Hybrid A* 专家轨迹生成 + 批量训练样本生成。
- 方法：前置调研获取 PythonRobotics Hybrid A* 参考实现，适配差分驱动（本项目控制为 [v,omega] 而非阿克曼转向）。
- 主要修改：实现 `planner/hybrid_astar.py`、`dataset/generator.py`；新增 `dataset/pipeline.py`（SensorBEVPipeline）、`scripts/generate_dataset.py`、`tests/test_planner.py`、`tests/test_dataset.py`、`spec/planner_dataset.md`；修改 README/ARCHITECTURE。
- 关键方案取舍：远距离规划发散（实测慢样本 22s），采取采样距离限 [3,12]m + 搜索范围包围盒+4m + 探索节点上限三重约束，失败样本重试跳过。
- 契约/数据影响：新增 Spec `EXPTRAJ-PLAN-001`/`EXPTRAJ-DATA-001`，全部 ✅。
- 验证：31 项 unittest 全通过；`generate_dataset.py 5` 生成 5 条样本（轨迹点 49~121、长度 5.0~12.3m、5 通道 BEV）；check_spec 双 Spec PASS。
- 文档影响：README/ARCHITECTURE 更新；TRACE T-183629 同编号。
- 剩余风险：规划最优性未验证（无 Reeds-Shepp 解析扩展）；远距离位姿对快速失败被跳过；数据集落盘格式待阶段四。
- TRACE：docs/traces/2026-08-21.md#T-183629。

## T-185608 · 18:56:08–19:01:30 · L4 · 实现 MineParkingNet 网络与训练

- 状态与结果：完整完成。阶段4核心交付：端到端轨迹生成网络 + 数据落盘/加载 + 训练脚本，训练可收敛。
- 方法：网络调研受阻（arXiv 不可用），采用成熟 BEV 编码 + 条件融合 + 轨迹回归模式，按项目文档契约实现。
- 主要修改：实现 `model/network.py`（MineParkingNet + loss_fn）、`dataset/generator.py` save/load；新增 `scripts/train.py`、`tests/test_model.py`、`spec/model.md`；修改 README/ARCHITECTURE/.gitignore。
- 关键方案取舍：安装 PyTorch 2.13.0 CPU 版；数据集保存全局轨迹，训练时转起始局部系（与 BEV 一致）；LazyLinear 自动推断维度；掩码 MSE 处理变长轨迹。
- 契约/数据影响：新增 Spec `MODEL-NET-001`/`MODEL-LOSS-001`/`MODEL-TRAIN-001`/`MODEL-DATA-001`，全部 ✅。
- 验证：36 项 unittest 全通过；`train.py --samples 20 --epochs 20` loss 3.38→0.021，权重保存成功；check_spec PASS。
- 文档影响：README（PyTorch 依赖、训练命令）、ARCHITECTURE 更新；TRACE T-185608 同编号。
- 剩余风险：未在 CARLA/实物验证泛化；规划失败重试造成数据分布偏差；多模态/速度剖面/验证集留待后续。
- TRACE：docs/traces/2026-08-21.md#T-185608。

## T-190304 · 19:03:04–19:16:05 · L3 · 实现 MPC 轨迹跟踪控制器与 Python 闭环泊车

- 状态与结果：完整完成。阶段5核心交付：MPC 轨迹跟踪控制器 + 网络→MPC→车辆闭环演示。
- 方法：沿用差分驱动模型；纯 numpy 数值梯度下降求解（无 scipy）。
- 主要修改：实现 `controller/mpc.py`；新增 `scripts/run_closed_loop.py`、`scripts/__init__.py`、`tests/test_mpc.py`、`spec/controller.md`；修改 README/ARCHITECTURE。
- 关键方案取舍：修复参考点取点 bug（固定从头取导致车辆停滞，改最近点+预测时域）；控制惩罚默认 0.05（0.1 跟踪过慢，0.001 震荡）。
- 契约/数据影响：新增 Spec `CTRL-MPC-001`/`CTRL-TRACK-001`/`CTRL-TURN-001`，全部 ✅。
- 验证：39 项 unittest 全通过；闭环演示样本位置误差 0.49~1.02m；MPC 跟踪专家轨迹样本1 位置误差 0.29m 航向 8.4°；check_spec PASS。
- 文档影响：README/ARCHITECTURE 更新；TRACE T-190304 同编号。
- 剩余风险：网络短训练致闭环航向误差大（阶段4 改进项）；>180° 大转向收敛慢（动力学限制）。
- TRACE：docs/traces/2026-08-21.md#T-190304。