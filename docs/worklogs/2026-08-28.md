# Agent 任务日志 · 2026-08-28

时区：Asia/Shanghai

## 10:03:07–10:06:45 · L0 · 完成数据集生成中 S4/T3 规划超时与检查点恢复行为诊断；文件：无。

## T-101408 · 10:14:08–10:29:12 · L4 · 未完成批次失败游标持久化

- 状态：完整完成；原命令续建将从 S4/T3 sample index 124 向前重采，不再重放已排除 ID。
- 根因与方案：成功批次已可恢复，但未完成批次的失败 ID/游标未落盘；新增与批次身份绑定的原子 retry JSON，不改准入、难度和规划预算。
- 修改：`dataset/build.py`、`scripts/build_dataset.py`、`tests/test_dataset_build.py`、`spec/planner_dataset.md`、`ARCHITECTURE.md`、`LEARNING.md`；现有 v4 检查点新增第 294 批 retry 迁移状态。
- 迁移/恢复：保留 293 个完成批次/1465 条样本；旧失败以中性 legacy 计数归档，不伪造分类。无 retry 状态的旧目录兼容，错配/损坏状态拒绝恢复。
- 验证：定向 15 项、全仓 232 项、Spec 检查、3000 条 dry-run 和 diff 检查均通过；长时真实生成按约定由用户执行。
- 规范：`EXPTRAJ-DATA-015` ✅；完整证据见 [`docs/traces/2026-08-28.md#t-101408--l4--数据集失败重采可恢复化`](../traces/2026-08-28.md#t-101408--l4--数据集失败重采可恢复化)。
- 剩余运行风险：S4/T3 仍可在新任务上达到单次命令的 10 次重试上限，但此时原命令再次执行会继续前进，无需再改代码或参数。

## 10:42:29–10:45:30 · L0 · 完成长耗时构建 ETA 与 S4/T3 高频规划超时诊断；文件：无。

## 10:52:05–10:52:25 · L0 · 完成校准覆盖粒度、规划分辨率与训练/最终效果影响说明；文件：无。

## T-110656 · 11:06:56–11:29:52 · L4 · 卸载区车辆尺度、净空与长距规划修复

- 状态：部分完成；代码、规范和本地验证完整完成，正式准入等待用户执行 v5 全能力校准并返回报告，3000 条生成未启动。
- 根因：S4/S9 卸载目标只按裸 6×3m 车体留 0.3m，未容纳 0.2m 碰撞膨胀；S9 航向令车头而非车尾朝墙；修复后约 28m T3 跨排任务又暴露 12m 解析接管范围与 30m 任务契约不匹配。
- 修改：S4/S9 采用车辆相对道路/卸载位尺度与 `margin + 0.3m` 后缘净空；解析范围配置为 30m，版本升级 `tracked_pivot_v5`；新增 39 单元/117 case 全能力探测入口、计划指纹隔离、训练路径和项目知识同步。
- 数据与恢复：旧 `data/task_dataset/tracked_pivot_v4_3000`、1465 条已完成样本及第 304 批 retry 状态全部保留；新 v5 必须使用独立校准和数据目录，不能原命令续建旧目录。
- 验证：S4/T3 294–303 连续 10 样本均在 0.007–0.021s 规划成功；全仓 237 项通过；两份 Spec 检查与 diff check 通过。
- 规范：`SCENE-SCALE-001`、`EXPTRAJ-TRACK-001`、`EXPTRAJ-PLAN-001`、`EXPTRAJ-CAL-001`；完整证据见 [`docs/traces/2026-08-28.md#t-110656--l4--卸载区车辆尺度与安全净空契约`](../traces/2026-08-28.md#t-110656--l4--卸载区车辆尺度与安全净空契约)。
- 剩余风险：v4 准入矩阵尚未按 v5 完整报告更新；在 Agent 审查 `runs/dataset-calibration/tracked_pivot_v5_full/report.json` 前不得启动正式 3000 条构建。

## T-120028 · 12:00:28–12:06:32 · L4 · tracked_pivot_v5 校准后专家准入

- 状态：完整完成；117/117 v5 全能力报告已转化为正式专家准入，3000 条长任务可由用户启动。
- 证据与取舍：报告为 86 成功、29 失败、2 硬预算超时；只开放稳定方向，不以 S5/T2、S9/T1/T3 的偶发成功放宽准入。
- 修改：`dataset/build.py`、准入/校准回归测试、`spec/planner_dataset.md`、README、长任务说明与 LEARNING；S3/T3 改前进，S8/T2/T3 恢复前进，S9/T1 排除。
- 计划与迁移：3000 条仍为 2400/300/300，30 个普通单元，S9 test 为 T2/T4/T5 各 100；只写 `tracked_pivot_v5_3000`，不动 v4 数据和 v5 校准证据。
- 验证：定向测试通过，四项关键真实轨迹双门禁通过，全仓 237 项、Spec 检查和 diff check 通过。
- 规范：`EXPTRAJ-DATA-016` ✅；完整证据见 [`docs/traces/2026-08-28.md#t-120028--l4--tracked_pivot_v5-校准后专家准入`](../traces/2026-08-28.md#t-120028--l4--tracked_pivot_v5-校准后专家准入)。
- 剩余风险：正式 3000 条尚未运行；若仍有偶发失败，现有 retry 游标会持久化，使用同一命令恢复。

## T-142800 · 14:28:00–17:08:34 · L4 · S3/S6/S7/S9 场景可行驶性复原与紧 bay 对齐采样
- 状态：完整完成；正式 3000 条生成按约定由用户执行，Agent 完成 100 条端到端烟测。
- 根因：v5 正式构建在 S3/T3 反复耗尽重试；诊断发现远距起点横向偏移≥4m 或带随机大转角时解析候选全碰撞、离散搜索难收敛（改场景空间无效），S6/T3 是岩石堵接近走廊，S7/T2 是靠岛净空未计 margin，S9/T2 是破碎槽朝向错误。
- 修改：sim/scenes.py（S3 车辆相对作业道+deep apron+maintenance_bay、S6 岩石移位、S7 靠岛净空计 margin、S9 破碎槽改倒车入槽）、sim/tasks.py（S3/S7 车辆相对化、_sample_axial_start 紧 bay 对齐采样）、dataset/build.py（S3/T3 恢复倒车、S5/T2 重新准入仅倒车、S9/T2/T5 仅倒车）、tests、spec/README/ARCHITECTURE/LEARNING/诊断文档。
- 验证：受影响单元 8s 扫描全部 ≥5/6（多数 6/6、毫秒级）；100 条烟测仅 3 次重采且双门禁通过；全仓 239 项通过；Spec 检查 PASS；3000 dry-run 保持 2400/300/300。
- 规范：SCENE-SCALE-002、EXPTRAJ-DATA-017（修订 DATA-016）；证据见 [docs/traces/2026-08-28.md#t-142800--l4--s3-维修区场景可行驶性重构](../traces/2026-08-28.md#t-142800--l4--s3-维修区场景可行驶性重构)。
- 迁移：新数据写 	racked_pivot_v6_3000；v4/v5 检查点与校准证据保留作归档，不混用。
- 剩余风险：S9/T2、T5 偶发 1/6 失败由同单元重采兜底；正式 3000 条全量未长时验证。

## T-171715 · 17:17:15–17:43:00 · L4 · v7 全准入可达性复核与入口走廊加固

- 状态：完整完成；S1–S9 初始场景语义与尺度复核通过，34 个正式准入单元均通过无重采真实专家生成门禁，正式 3000 条由用户执行。
- 基线与根因：v6 的 408 例密集复核仅 390 成功、15 失败、3 超预算；S3/T3 已为 12/12，剩余问题来自普通车位前进/倒车共用固定目标航向、只检查起点端点而未检查连续入口走廊、相邻占用截断入口及 T4 候选顺序。
- 修改：S1/S2/S9 普通车位增加结构化 kind 与方向一致航向；TaskSampler 按机动翻转倒车目标、检查 0.1m 连续 footprint 入口、过滤被占用截断的目标并优先 T4 起点参考目标；同步 README、ARCHITECTURE、LEARNING、诊断文档和 `EXPTRAJ-DATA-018`。
- 验证：15 个旧失败任务回归通过；相同 34×12 校准在 `max_retries=0` 下 408/408、失败/预算超时均 0；全仓 242 项、两份 Spec、3000 dry-run（2400/300/300，S9 T2/T4/T5 各 100）及 diff check 通过。
- 迁移与风险：正式输出改为 `data/task_dataset/tracked_pivot_v7_3000`，不得续用 v6 检查点；408 例不是无限 seed 的数学证明，运行时同单元重采和失败游标继续保留。
- 规范与证据：`EXPTRAJ-DATA-018` ✅；完整证据见 [`docs/traces/2026-08-28.md#t-171715--l4--v6-全准入可达性复核与加固`](../traces/2026-08-28.md#t-171715--l4--v6-全准入可达性复核与加固)。

## 17:49:47–17:50:40 · L0 · 完成 v7 正式 3000 条数据极速生成真实性核验：NPZ 实际为 2400/300/300、检查点跨度 64.86s、仅 4 次非超时同单元替换，三 split 机动一致性与轨迹可行性均 100%；文件：`data/task_dataset/tracked_pivot_v7_3000/manifest.json`、`docs/worklogs/2026-08-28.md`。

## 17:52:21–17:53:53 · L1 · 确认当前尚未执行模型训练，完成 v7 train/val/test 严格统计并各生成 5 张专家轨迹可视化；文件：`data/task_dataset/tracked_pivot_v7_3000/inspection/`、`docs/worklogs/2026-08-28.md`。
## 17:56:05–17:56:55 · L0 · 完成 v7 正式训练前计划核验：确认尚未训练、现有 YAML 仍指向 v5 且 horizon 60 小于 v7 最大有效轨迹 307，当前 CPU-only，应先更新 v7 配置并做 1 epoch net-v1 smoke 再由用户执行正式训练；文件：无。

## T-175859 · 17:58:59–18:05:01 · L3 · v7 训练配置迁移与全量 smoke

- 状态：完整完成；三套正式训练配置已迁移到 v7，并新增独立 `net-v1` 单轮 smoke 配置，正式长训练等待用户执行。
- 方法：沿用现有严格 YAML、全量 train/val 和 Trainer 流程；单批实测约 0.18s，因此未引入抽样契约或额外 smoke 数据。首次运行发现旧 `dt=0.1` 与 v7 实际 `dt=0.2` 不符，保留校验并统一修正。
- 修改：`configs/training/net-v0.yaml`、`net-v1.yaml`、`net-v2.yaml` 使用 v7、320 点上限、0.2s 时间步及隔离输出目录；新增 `net-v1-smoke.yaml`；README 同步 smoke、正式训练、恢复与评估命令。
- 验证：v7 train/val 最大有效长度分别为 307/296，四份配置均为 horizon 320；全量 2400/300 条 1 epoch smoke 约 55s 完成并生成 best/last、JSON、PNG/PDF；10 项训练相关单元测试通过。
- 结果：smoke 指标 ADE 5.7082m、FDE 7.7727m、yaw MAE 16.185°，仅证明训练链路可运行，不作为收敛或最终效果结论。
- 文档：仅 README 使用入口发生变化；模型契约和架构未变化，无需修订 Spec、DESIGN 或 ARCHITECTURE。
- 剩余风险：当前为 CPU-only；正式 30 epoch 预计约 30 分钟以内但受 early stopping 和机器负载影响，完成后仍需审查曲线、开环指标及预测可视化。
