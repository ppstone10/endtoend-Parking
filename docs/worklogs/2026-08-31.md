# Agent 任务日志 · 2026-08-31

时区：Asia/Shanghai

## 00:32:36–00:38:16 · L0 · 完成 v8-safety-v2 正式训练与闭环准入复核：第 21 轮正常早停、best 为第 16 轮，Val ADE/FDE 0.315/0.535m、停止长度 MAE 13.32 点通过开环门槛，但同 34 条纯网络闭环成功率由 v7 的 32.4% 降至 0%、碰撞率由 29.4% 升至 55.9%，安全损失与 v7 几乎不变（0.293994→0.293754）；门禁在安全子集 10 条达到 30% 成功、0 碰撞和 9.8% 干预率，但完整评测因当前状态已侵入规划安全余量而回退失败，结论为模型不准入且需修正安全损失梯度、权重初始化/困难样本采样及可回退运行时门禁；文件：runs/training/v8-safety-v2/net-v1/、runs/openloop-analysis/v8-safety-v2/net-v1/val/、runs/closed-loop/v8-safety-v2/net-v1/。

## T-004952 · 00:49:52–01:32:11 · L4 · 连续净空训练与可回退安全门禁

- 状态：完整完成训练/门禁结构修正与三组小规模消融；演进证据见 [TRACE T-004952](../traces/2026-08-31.md#t-004952--l4--连续净空训练与可回退安全门禁)。
- 边界与验收：未改专家数据、场景、规划器、MPC、车辆尺度或旧产物；要求统一净空损失较 v7 至少下降 10%，同时 ADE≤0.40m、FDE≤0.80m，并让门禁失败成为可汇总回合结果。
- 方案：实现连续有符号净空场扫掠损失、v7 仅权重初始化、可选确定性恢复三池 batch、执行前转移门禁、专用 `SafetyStopError` 和统一消融评估脚本；正式配置基于消融采用真实占比 shuffle。
- 消融结果：v7/旧损失均衡/净空均匀/净空均衡为 1.102705/1.306765/0.115092/0.128024；采用组安全损失下降 89.6%，ADE/FDE 0.228/0.572m，明确淘汰旧损失和强制三池均衡。
- 门禁 smoke：10 条分层任务 30% 成功、0% 碰撞，484 次轨迹检查、4830 次转移检查、55 次轨迹回退、0 回退失败和 0 安全停止；轨迹干预率保持 11.4%，转移阻断率独立报告。
- 修改：`training/`、`runtime/`、`experiments/closed_loop_evaluation.py`、`scripts/evaluate_safety_ablation.py`、训练/消融配置、相关测试，以及 README/DESIGN/ARCHITECTURE/LEARNING/训练指南/安全 Spec。
- 验证：`py_compile` 与正式 YAML 解析通过；36 项定向、292 项全仓测试通过；报告口径修正后 10 项相关测试再次通过；`git diff --check` 通过。
- 兼容与回退：旧 YAML 默认继续使用 `occupancy_max`；`initialize_from` 与 `resume_from` 互斥；旧 checkpoint/报告只读；运行时可显式选择 `none`；训练产物写独立 v9 目录。
- 剩余风险与用户动作：小样本只验证已知静态环境安全防线，倒车任务仍振荡；用户需执行正式 v9 长训练，再统一复算净空损失并对同索引运行纯网络/门禁闭环准入。

## T-171919 · 17:19:00–17:27:19 · L2 · 正式 v9 净空复算与同索引闭环准入验收

- 状态与结果：完整完成 v9 验收流程，结论为净空/开环通过但纯网络闭环不通过准入。v9 `report.json` 为 completed、best epoch 4、早停，Val ADE/FDE 0.2387/0.4878m、长度 MAE 11.42 点、停止命中率 100%、校准阈值 0.37；从 v7 `best.pt`(epoch 21) 权重初始化，train 2658 条含恢复、val 300 条。
- 统一复算（`scripts/evaluate_safety_ablation.py`，同 val、margin 0.1m）：v7 净空 1.102705 → v9 0.099924，下降 90.9%（门槛 ≥10%），ADE/FDE 0.2387/0.4878m 达标。
- 同索引闭环（34 条 val 分层、k=10、seed=0、deployment.pt，两模式索引一致）：纯网络 1/34 成功（2.9%）、碰撞 15/34（44.1%）未达 ≥70%/≤10% 门槛；门禁 14/34 成功（41.2%）、碰撞 0%、1402 次检查/345 干预(24.6%)/39 转移阻断/5 安全停止/0 回退失败，碰撞率达标但成功率不足。
- 修改：仅新增报告产物，无源码改动；文件：runs/training/v9-safety-v1/net-v1/ablation_report.json、runs/closed-loop/v9-safety-v1/net-v1/val-pure-34-k10/、val-shield-34-k10/。
- 验证与缺口：两份闭环报告索引/seed/重规划周期一致；小样本 34 条分层只代表 val 抽样，S3 全部 collision→safety_stop 且倒车振荡仍集中；剩余动作为按训练指南单变量比较 `collision_loss_weight=0.25/1.0`，不混改其他参数。

## T-190435 · 19:04:35–19:24:04 · L3 · collision_loss_weight 与课程消融后安全微调根因定位

- 状态与结果：完整完成 B1/B2 两项隔离实验，定位安全微调退化根因。v9b(w=0.25/1.0) 与 v9c(w=0 关闭净空 / TF 课程) 全部完成训练、统一净空复算与同索引闭环；结论为安全微调路径本身破坏 v7 闭环能力，净空权重与 TF 课程均非根因。
- 证据：v7 成功 11 条在 w=0.25/0.5/1.0/w0/TF课程 五个模型下全部稳定失败（8 振荡+3 碰撞）；w=0 开环最优(ADE/FDE 0.172/0.300)但纯网络仅 14.7% 成功/50% 碰撞，关闭净空不恢复闭环；TF课程 8.8%/44.1% 与 v9 持平。所有门禁 0 碰撞、0-1 回退失败，成功率 38-44%，振荡为唯一未解瓶颈（终态位置 0.2-1.2m 但航向偏差 15-117°）。
- 修改：新增 configs/training/net-v1-safe-w0.yaml、net-v1-safe-tfcurriculum.yaml 两个单变量配置；产物 runs/training/v9b-*/、v9c-*/ 与 runs/closed-loop/v9b-*/、v9c-*/。
- 验证与缺口：每个 checkpoint 完成统一净空复算与 34 条同索引闭环；w=0 净空损失反超 v7 基线（1.468>1.103）说明净空损失仍有效，但未改变闭环结局；尚未验证"恢复数据 vs 纯 v7 权重微调"或"振荡的滚动重规划机制"单独假设，需用户决策后进入下一步。
- 下一步建议：单独比较原始 v7 数据(无恢复)微调以隔离恢复数据影响；或调查振荡由滚动重规划方向切换导致的具体机制；门禁成功率达 44.1%(w0) 但纯网络仍未准入。

## 20:30:00–20:45:00 · L0 · 完成系统性梳理并固化闭环诊断：开环验收全部达标（ADE/FDE/MAE/停止/净空），闭环纯网络成功率长期 2.9%~17.6% 远低于 70%、碰撞率 44%~50%，根因排除净空权重与 TF 课程两个单变量（v7 成功 11 条在 5 个安全模型下一致失败），当前唯一瓶颈为振荡（航向不收敛，终态位置 0.2-1.2m 但航向偏 15-117°），门禁已 0 碰撞/0 回退失败；两个待决策方向（A 恢复数据是否元凶、B 滚动重规划机制）已记录，解决路线与验收速查见 docs/closed_loop_diagnosis.md；文件：docs/closed_loop_diagnosis.md、docs/traces/2026-08-31.md#t-190435。

## 21:10:00–21:40:00 · L2 · 方向 B 振荡机制定论：近端停止长度预测提前+终点航向回归偏差

- 状态与结果：完整完成方向 B 诊断并定论，非滚动重规划机制问题而是安全模型近端轨迹预测退化。对 v7(成功)/v9(振荡) 同样本逐重规划对比，新增两个诊断脚本：`scripts/diagnose_oscillation_focus.py`（单模型重规划剖析）、`scripts/diagnose_oscillation_compare.py`（v7/v9 同索引近端对比）。
- 证据：车辆距目标 <3m 时 v9 预测轨迹骤缩至 0.7–1.9m（v7 稳定 3.7→1.7m）、预测终点距目标膨胀至 1.5–2.5m（v7 0.8–1.5m）、终点航向误差膨胀至 0.4–1.3 rad（v7 0.06–0.23）、轨迹含 1–2 次方向切换（v7 为 0）；MPC 追着"短而偏航"参考轨迹摆动。
- 结论与疑似根因：净空损失诱导网络"用缩短轨迹规避障碍"（短轨迹天然远离障碍、净空损失更低），牺牲终点收敛；v7 用同一引擎+机制可成功，排除重规划机制自身。
- 修改：`scripts/diagnose_oscillation.py`、`diagnose_oscillation_focus.py`、`diagnose_oscillation_compare.py`；`docs/closed_loop_diagnosis.md` §3.4/§4-B/§5 更新。
- 剩余风险与下一步：方向 B 建议解法为训练侧抑制缩短轨迹倾向（停止监督与净空损失再平衡、近端终点/航向加权）或单变量缩短重规划周期；方向 A（恢复数据隔离）待执行。

## 21:50:00–22:20:00 · L2 · 方向 A 恢复数据隔离定论：非根因但有加重作用

- 状态与结果：完整完成方向 A。新增 `configs/training/net-v1-safe-v7data.yaml`（v7 `best.pt` 初始化、`collision_loss_weight=0.5`、仅原始 v7 train 2400 条无恢复），训练 best epoch 5、ADE/FDE 0.258/0.553m、净空 0.092(-91.7%)。
- 证据：v9d 纯网络 11.8% 成功/35.3% 碰撞，门禁 47.1% 成功/0 碰撞/0 回退失败（当前最高门禁成功率）；v7 成功 11 条在 v9d 下仍全部失败（8 振荡+3 碰撞）。
- 结论：恢复数据不是唯一元凶（去掉后仍摧毁 v7 能力），但确有加重作用（相对 v9：成功 2.9%→11.8%、碰撞 44.1%→35.3%、门禁 41.2%→47.1%），疑似 9.7% 占比冲淡原始监督且分布错配。
- 修改：`configs/training/net-v1-safe-v7data.yaml`；产物 `runs/training/v9d-v7data/`、`runs/closed-loop/v9d-v7data/`；`docs/closed_loop_diagnosis.md` §4-A/§5 更新。
- 剩余风险与下一步：两方向均定论——根因是安全微调近端预测退化（B），恢复数据为加重因子（A）；下一步建议在训练侧抑制"缩短轨迹躲障碍"（近端终点/航向加权）或单变量缩短重规划周期，并用安全模型自身 deployment 重新设计恢复数据。

## T-203400 · 20:34:00–20:40:13 · L4 · 近端终点对齐训练监督与恢复数据重采准备

- 状态与结果：完整完成训练侧近端终点对齐监督实现（`SAFE-LOSS-003`）。新增 `endpoint_alignment_loss` 对 mask 末端 K 点施加相对目标位姿的位置+航向 MSE，`TrainerConfig` 新增 `endpoint_alignment_weight`(默认0)/`endpoint_alignment_tail_points`(默认8)并纳入 checkpoint 兼容字段；新增 `configs/training/net-v1-safe-endpoint.yaml`（v10，w=0.5）。
- 修改：`model/network.py`、`model/__init__.py`、`training/trainer.py`、`tests/test_model.py`、`tests/test_trainer.py`、`tests/test_training_config.py`、`spec/safety_training.md`、`docs/training_guide.md`、`configs/training/net-v1-safe-endpoint.yaml`。
- 验证与缺口：定向 30 项、受影响 28 项、全仓 299 项全部通过；真实闭环效果未知，需用户执行 v10 训练后同索引闭环准入复核；恢复数据重采指令待 v10 结果确认后提供。
- 知识：`SAFE-LOSS-003` ✅；诊断结论与单变量方向固化到 `docs/closed_loop_diagnosis.md` 与训练指南；TRACE [T-203400](../traces/2026-08-31.md#t-203400--l4--近端终点对齐训练监督与恢复数据重采)。

## 21:30:00–22:10:00 · L4 · v10 实测与净空-到达目标物理冲突定论

- 状态与结果：v10（goal 对齐近端监督）训练+闭环后纯网络 0% 成功/73.5% 碰撞、门禁 35.3%，显著劣于 v9（2.9%/44.1%）；修正为 target 末端模仿（v10-fix）后开环最优（0.204/0.340）但纯网络仍 2.9%/52.9%、门禁 32.4%。
- 根因新发现：逐样本检查净空场发现 **19/300 样本目标位姿处净空仅 0.125m，远低于 required_clearance=0.3m**，且轨迹末端 8 点大量处于 <0.3m 净空区（S1×4、S2×7、S3×3、S4×1、S6×1、S8×3）；专家轨迹 mask 末端 100% 等于目标位姿。净空损失对目标末端物理上不可满足，与"轨迹必须到达目标"根本对立——网络只能绕开目标（v9 振荡）或强贴目标（v10 碰撞）。
- 修改：`model/network.py::endpoint_alignment_loss` 语义修正（goal→target）、`spec/safety_training.md` SAFE-LOSS-003 行为更新、`docs/closed_loop_diagnosis.md` §3.5/§3.6；新增 `configs/training/net-v1-safe-endpoint.yaml`(v10-fix) 与产物 `runs/training/v10-endpoint*`、`runs/closed-loop/v10-endpoint*`。
- 验证：定向 30 项全过；v10/v10-fix 均完成净空复算与同索引闭环。
- 剩余风险与下一步：近端对齐两种形式均不能独立解决；正确方向为净空损失对目标附近末端区域豁免/降权（安全只约束行驶路径），待用户确认后实现并单变量实验。

## 22:10:00–22:40:00 · L4 · 净空目标豁免实现与 v10/v11 系列定论

- 状态与结果：完整实现 `SweptFootprintLoss` 目标豁免（`safety_goal_exempt_radius_m`/`safety_goal_exempt_weight`，对目标半径内扫掠点平滑降权净空、保留越界惩罚），v11（半径 1m、权重 0）实测纯网络 0% 成功/47.1% 碰撞、门禁 38.2%，失败样本与 v9 高度一致。
- 结论：净空豁免（v11）与近端对齐（v10/v10-fix）都不能单独解决闭环失败；根因是安全微调整体（净空+恢复数据+微调组合）改变网络滚动重规划行为。豁免/近端对齐作为能力保留默认关闭，不作为正式默认。
- 修改：`training/safety.py`（`_apply_goal_exemption`+2 参数+forward goal）、`training/trainer.py`（2 字段+校验+兼容字段）、`training/runner.py`、`scripts/evaluate_safety_ablation.py`、`configs/training/net-v1-safe-goal-exempt.yaml`、`tests/test_safety_loss.py::TestGoalExemption`、`spec/safety_training.md`、`docs/training_guide.md`、`docs/closed_loop_diagnosis.md` §3.5b。
- 验证与缺口：定向 32 项、全仓 303 项通过；v11 完成净空复算与同索引闭环；纯网络成功率仍远低于 70% 准入线。
- 剩余风险与下一步：需用户决策——重新设计安全微调整体路径，或把门禁方案（0 碰撞/0 回退失败）作为阶段交付基线；Commit 文稿已提供。
## 20:58:40–21:0x:xx · L3 · 闭环验证设计与三步可验证路线定稿

- 状态与结果：完整完成判断与设计记录。用户要求判断轨迹架构路线（路线 A 一次全轨迹循迹 vs 路线 B 全局参考+短距局部重规划+MPC），整合网络/行业方法并形成过程完整、每一环可验证的方案记录。
- 判断：路线 B 为主（业界分层标准、可纠正近端退化、CARLA/实车接口不变），路线 A 保留为对照口径；当前 replan_every 滚动重规划已接近 B，缺口是"以完整轨迹为参考的短距局部规划"显式层级。
- 调研：网络受限（arxiv/google/github/waymo 不可达），基于 NVIDIA DRIVE/Isaac 官方（闭环为主验证、开环仅数据扩展）、CARLA 官方（录制重放对比、传感器替换）、DAgger（学习器状态专家重标注）等可访问来源与既定文献。
- 设计：四层验证阶梯 L1 BEV 保真度（感知）/L2 访问状态轨迹质量（含 B 指标）/L3 单周期 MPC 跟踪（用户点名）/L4 全闭环；B=L2 度量、C=训练侧让 L2 达标手段（近端对齐已实现 SAFE-LOSS-003、恢复数据按当前 deployment 重采）；顶层判据新增时间-距离效率比。
- 修改：仅新增 `docs/closed_loop_verification_design.md`，无源码改动。
- 剩余风险与下一步：设计未实现；落地顺序为 L1 评测脚本 → L2 固化访问状态报告 → L3 复核 → L4 效率指标 → v10 训练闭环准入 → 路线 B 局部层。
## T-221500 · 21:15:00–22:05:00 · L2 · L1 BEV 保真评测实现

- 状态与结果：完整完成验证阶梯 L1（感知→BEV 保真评测）。新增 `metrics/bev_fidelity.py`（occupancy 双真值栅格化 + IoU/P/R + target 命中率 + 聚合）、`viz/bev_fidelity.py`（退化曲线）、`scripts/evaluate_bev_fidelity.py`（逐场景×噪声档评测入口）。
- 关键口径：occupancy 主真值改用"高分辨率（3600 束）无噪声 LiDAR 投射占用"而非场景几何全量栅格；车辆位姿朝向目标采样。实测 9 场景 clean occ IoU 0.53–0.63（P≈1.0、R≈0.6）、high 退化至 0.35–0.48、target 命中率 0.93–0.99。
- 修改：`metrics/bev_fidelity.py`、`viz/bev_fidelity.py`、`scripts/evaluate_bev_fidelity.py`、`tests/test_bev_fidelity.py`（11 项）；文档 `docs/closed_loop_verification_design.md` §10 标记 L1 完成、`docs/training_guide.md` 新增入口、`LEARNING.md` 新增经验。
- 验证：L1 定向 11 项通过；全仓 314 项通过；全量报告 `runs/bev-fidelity/L1-v1/report.json` + 退化曲线。
- 剩余风险与下一步：L1 只覆盖感知层；下一步按设计 §10 落地 L2（访问状态轨迹质量报告固化）。
- TRACE：[T-221500](../traces/2026-08-31.md#t-221500--l2--l1-bev-保真评测实现)
