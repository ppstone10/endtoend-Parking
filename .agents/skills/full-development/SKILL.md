---
name: full-development
description: 按影响与不确定性选择适当严谨程度，开发功能、重构、迁移或结构变更。适用于用户可见功能、跨层工作、公共契约、数据库或协议变更、依赖变更、架构重组、安全或隐私敏感工作，以及超出 focused-fix 边界的任务。
---

# 完整开发

使用能够安全覆盖变更的最轻档位。

## 1. 选择档位

- **标准：** 边界明确、行为已知且容易回退的功能或重构。
- **重大：** 跨层或结构性工作、公共契约、数据/数据结构/协议变更、大范围迁移或存在实质设计不确定性。
- **关键：** 安全、隐私、授权、破坏性或不可逆数据操作、生产发布或受监管行为。

按最高风险维度判定，不能按修改行数判定。明确超出标准档或判级不清楚时，阅读 [references/impact-and-risk.md](references/impact-and-risk.md)。

## 2. 只加载权威上下文

- 读取 DESIGN 中与行为意图相关的章节。
- 读取 ARCHITECTURE 中与边界和不变量相关的章节。
- 只有安装、运行或用户说明可能变化时才读取 README。
- 按受影响概念搜索 LEARNING 与 trace 文件名，禁止顺序通读历史。
- 只有功能级行为契约或验收相关时，才按关键词读取对应 `spec/` 文件，禁止通读全部 Spec。
- 扩大搜索前先检查源码入口、所有者、接口、测试和已知依赖方。
- 用户明确要求命名、文件拆分或注释治理，或触达范围出现多职责文件、难以准确命名的函数、与代码不符的注释时，读取 [references/code-clarity-guide.md](references/code-clarity-guide.md)；普通任务不加载。

## 3. 在新方案前完成有限调研

引入新功能、新方法、新依赖、新协议或项目没有成熟先例的方案时，先阅读 [references/research-guide.md](references/research-guide.md)，再确定实现设计。调研应优先权威来源并在证据足够时停止，不得用无限搜索替代项目判断。完全沿用项目现有模式时可跳过，并说明依据。

## 4. 确认重大边界

标准档应从仓库推断安全细节，只有不同选择会改变用户可见行为或范围时才询问。

重大或关键档实施前，确认以下方面中尚未解决且会产生实质影响的事项：

- 目标行为和验收标准；
- 非目标和兼容性；
- 所有权、接口和数据形态；
- 迁移、发布、回退或恢复；
- 安全、隐私、性能和运行限制。

权威项目资料已经回答的问题不得再次询问。

## 5. 规划可独立验证的增量

- 有利于审查或回退时，将准备性重构与行为变化分开。
- 每个增量保持可构建且行为完整。
- 为每个增量定义所需证据。
- 优先使用项目已有模式和基础设施。
- 避免投机式扩展和无关清理。
- L4/L5 在实质修改前加载 `track-change-trace` 并建立开始检查点；方案可能演进的 L3 按需建立。
- L3 新增或改变用户可见行为、需要跨会话验收或分阶段交付时，加载 `spec-driven-change` 使用轻量档。
- L4/L5 涉及公共契约、数据结构、协议、迁移、安全、隐私、权限或多客户端一致性时，加载 `spec-driven-change` 使用完整档；纯内部行为不变重构可跳过。

## 6. 实现与验证

- 标准档完成简短影响检查后直接实施。
- 重大和关键档按检查点推进，每次跨越边界后重新评估范围。
- 对本轮新增或实质修改的声明执行触达范围清晰度检查；不借机扫描或整理整个仓库。
- 范围、方案、契约、数据、迁移、风险或验证方向发生实质变化时，向当天 TRACE 追加检查点，不重写旧结论。
- 实施期间运行定向检查。
- 已启用 Spec 时先建立稳定规范 ID 与验收追溯；实现发现契约缺口时先修订 Spec，再继续实现。
- 完成时运行覆盖全部受影响范围的最小聚合检查。
- 后续成功检查已经覆盖先前检查时，除非相关代码后来发生变化，否则不重复验证。
- 关键档必须保留授权、回退、恢复和未验证风险的证据。

## 7. 核对项目知识

- 对照已确认的设计和架构检查实现。
- 已启用 Spec 时逐项核对规范 ID、测试或人工入口、实现符号、验证证据和真实状态。
- 当前项目事实、持久经验或 TRACE 证据变化时调用 `sync-project-knowledge`。
- 不得仅为了记录文件变化而修改概述文档。
- 报告交付行为、受影响范围、验证、文档影响和剩余风险。

## 8. 收敛交付

- 只使用已有实现、差异和验证证据回答最小收尾关卡，不因收尾自动增加命令。
- 标准档发现明确缺口时只补最小操作；重大或关键档读取 [references/delivery-closure.md](references/delivery-closure.md)。
- 明确最终状态，不使用“基本完成”等模糊表述。
- 修改代码、测试、脚本、构建配置、数据结构或协议后，提供基于实际差异的 Commit 文稿，不自动执行 `git commit` 或 `git push`。
- 已建立 TRACE 的任务在完成前追加最终变更映射，并让 worklog 使用同一任务编号链接它。
