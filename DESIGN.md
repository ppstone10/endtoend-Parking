# 设计

## 目标

- 论文主题"基于端到端的无人矿卡自动泊车研究"的实验系统：从纯 Python 仿真、CARLA 高保真仿真到履带式小车实物平台逐步迁移的无人矿卡低速自动泊车系统。
- 当前阶段为 Python 仿真（需求基准见 `docs/REQUIREMENTS.md` v2）：须产出论文级实验结果——真实矿场 9 场景 × 5 任务变体实验矩阵、多车位选优、全套指标与可视化。
- 三个阶段统一输入输出接口：更换传感器来源与底盘执行接口即可迁移，其余模块保持不变。
- 原始传感器数据（LiDAR 点云 / Camera 图像）经 Sensor2BEV 转换为统一 BEV 表示，供端到端网络直接消费。
- 端到端网络 MineParkingNet 输出未来 N 个局部轨迹点，由 MPC 控制器完成底层轨迹跟踪，网络不直接输出油门、转向。
- 网络训练同时约束中心轨迹模仿、完整车体连续扫掠安全，并用当前策略闭环访问状态上的专家恢复标签缩小训练/部署分布偏移；运行时安全门禁作为独立防线，不替代纯网络能力评测。
- 坐标统一采用车辆中心局部坐标系。
- 当前理论车辆是履带转向钻机：外廓默认是以两履带几何中心为中心的 6×3 m 矩形，允许完全停车后绕该中心原地旋转；几何、速度和搜索参数可配置并随训练数据记录版本。

## 非目标

- 不在本系统内实现 CARLA 仿真器或 CAN 总线协议本身，只提供接入适配层。
- 不做高速行驶或避障竞速类功能，系统面向低速泊车场景。
- Python 理论模型不声称覆盖履带—地面滑移、土壤沉陷、质量/惯量和真实制动性能；这些由 CARLA/实车标定与闭环试验补充。
- 网络不直接输出油门、转向等底层控制量。
- Python 阶段不做最小可验证实现，按论文工作量标准交付（对比基线、消融、批量实验、可视化全套）。

## 整体行为

三个阶段共享同一数据流：

```
LiDAR点云 / Camera图像
  → Sensor2BEV 环境表示模块
  → BEV + 车辆状态 + 目标泊车位姿
  → MineParkingNet 端到端轨迹生成网络
  → 未来N个局部轨迹点
  → MPC 轨迹跟踪控制器
  → [v_cmd, omega_cmd]
  → 平台执行器（Python车辆 / CARLA车辆 / 履带车底盘）
```

- 阶段一（Python 仿真，当前阶段）：二维矿区泊车场景库、可配置履带钻机低速运动学（含原地转向与完整外廓扫掠）、模拟传感器、Sensor2BEV、经机动/可行性门禁的专家轨迹与分层数据集、MineParkingNet 训练、多车位选优、MPC 滚动闭环泊车、全套指标与可视化。
- 阶段二（CARLA）：接入 CARLA LiDAR/Camera，使用同一 Sensor2BEV 与 MineParkingNet，MPC 控制 CARLA 车辆，验证传感器与动力学真实性及噪声鲁棒性。
- 阶段三（实物）：CAN 通信与底盘控制、真实 LiDAR/Camera、实时 BEV、定位、MPC 人工轨迹跟踪，最终接入端到端轨迹生成。

## 开发顺序

M1 地基修复（MPC 重做+滚动闭环引擎+指标模块）→ M2 平台与数据（场景矿区化+任务层+专家系统+分层数据集）→ M3 网络与训练（v1 变长/v2 主模型+配置化训练）→ M4 实验矩阵（基线+消融+多车位选优+鲁棒性/泛化+全套图表）→ CARLA 迁移 → 履带车底盘测试 → 实车端到端验证。

M1 出口判据：专家轨迹+MPC 闭环成功率 ≥95%。M1 未达标不启动后续实验。详细需求与里程碑见 `docs/REQUIREMENTS.md`。

## 评价指标

- 泊车成功率（阈值随场景车位容差）、碰撞率、最终位置误差、最终航向误差、路径长度、泊车时间、MPC 跟踪误差、网络推理时间。
- 多车位选优：选位准确率（与评分器 oracle 一致率）、选位代价差；泊车成功率按自选位/指定最优位双口径。
- 开环轨迹质量：ADE/FDE/航向误差（验证集）。
- 全部指标由批量实验 runner 自动统计，多 seed 报告均值±标准差。

## 验收边界

- 阶段一按六维标准验收（详见 `docs/REQUIREMENTS.md` §1）：接口定义好、轨迹输出有效（网络进闭环）、MPC 跟踪有效（专家+MPC ≥95%）、效果可视化（V1–V5）、效果可验证（指标+批量实验+基线消融）、模型可讲通（对比表+消融表+数据集统计）。
- 阶段二：同一网络在 CARLA 传感器与动力学下闭环泊车成功。
- 阶段三：真实传感器 → BEV → MineParkingNet → MPC → CAN → 履带底盘全链路闭环。
- 各阶段之间只允许替换传感器来源和底盘执行接口，网络输入输出契约保持不变。
