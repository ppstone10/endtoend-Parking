# 设计

## 目标

- 构建从纯 Python 仿真、CARLA 高保真仿真到履带式小车实物平台逐步迁移的无人矿卡低速自动泊车系统。
- 三个阶段统一输入输出接口：更换传感器来源与底盘执行接口即可迁移，其余模块保持不变。
- 原始传感器数据（LiDAR 点云 / Camera 图像）经 Sensor2BEV 转换为统一 BEV 表示，供端到端网络直接消费。
- 端到端网络 MineParkingNet 输出未来 N 个局部轨迹点，由 MPC 控制器完成底层轨迹跟踪，网络不直接输出油门、转向。
- 坐标统一采用车辆中心局部坐标系。

## 非目标

- 不在本系统内实现 CARLA 仿真器或 CAN 总线协议本身，只提供接入适配层。
- 不做高速行驶或避障竞速类功能，系统面向低速泊车场景。
- 网络不直接输出油门、转向等底层控制量。

## 整体行为

三个阶段共享同一数据流：

```
LiDAR点云 / Camera图像
  → Sensor2BEV 环境表示模块
  → BEV + 车辆状态 + 目标泊车位姿
  → MineParkingNet 端到端轨迹生成网络
  → 未来N个局部轨迹点
  → MPC 轨迹跟踪控制器
  → [v_cmd, omega_cmd]
  → 平台执行器（Python车辆 / CARLA车辆 / 履带车底盘）
```

- 阶段一（Python 仿真）：二维矿区泊车环境、车辆运动模型、模拟传感器、Sensor2BEV、专家轨迹生成、训练数据、MineParkingNet 训练、MPC 闭环泊车。
- 阶段二（CARLA）：接入 CARLA LiDAR/Camera，使用同一 Sensor2BEV 与 MineParkingNet，MPC 控制 CARLA 车辆，验证传感器与动力学真实性及噪声鲁棒性。
- 阶段三（实物）：CAN 通信与底盘控制、真实 LiDAR/Camera、实时 BEV、定位、MPC 人工轨迹跟踪，最终接入端到端轨迹生成。

## 开发顺序

阶段0 统一接口 → 阶段1 Sensor2BEV → 阶段2 Python 仿真 → 阶段3 专家轨迹生成 → 阶段4 网络训练 → 阶段5 Python 闭环测试 → 阶段6 CARLA 迁移 → 阶段7 履带车底盘测试 → 阶段8 实车端到端验证。

## 评价指标

- 泊车成功率、碰撞率、最终位置误差、最终航向误差、路径长度、泊车时间、MPC 跟踪误差、网络推理时间。

## 验收边界

- 阶段一：数据集、网络模型、闭环泊车实验指标可作为验收产出。
- 阶段二：同一网络在 CARLA 传感器与动力学下闭环泊车成功。
- 阶段三：真实传感器 → BEV → MineParkingNet → MPC → CAN → 履带底盘全链路闭环。
- 各阶段之间只允许替换传感器来源和底盘执行接口，网络输入输出契约保持不变。