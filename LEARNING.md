# 项目经验

## 专家数据长任务：先建立预算与准入，再考虑并行

- **触发：** 专家数据生成出现长时间无检查点、复杂任务反复规划超时，或需要由 Agent 协助运行数十分钟以上的任务。
- **规则：** 先用分层小样本校准生成专家能力矩阵，再设置单任务、任务单元和整次运行三层预算；不可稳定规划的单元隔离处理。只有在失败重采已经受控后，才用进程级并行扩大吞吐。
- **运行约定：** 长任务必须依靠程序自身的持久日志、微批次检查点和受控退出运行。只持久化成功批次不足以保证失败后向前推进；确定性重采还必须在每次失败后原子记录排除 ID 和下一游标，否则相同命令会重放同一失败序列。Agent 只确认启动和首个检查点，之后仅在里程碑、退出、预算触发或用户询问时读取一次状态，不做高频轮询，也不在运行中反复试调参数。
- **原因：** 本项目 2026-08-25 的 1600 条样本证据显示，93.8% 的失败是 8 秒规划超时，固定超时至少占已落盘耗时的约 65%；`max_retries=100` 比缺少并行更先放大浪费。进程池能缩短墙钟时间，但不能修复不可达单元，也不能靠 Future 等待超时取消已经运行的任务。
- **证据与完整方案：** [`docs/train_long_runtime_diagnosis.md`](docs/train_long_runtime_diagnosis.md)。

## 小数据集构建不能代替全能力校准

- **触发：** 使用 `build_task_plan` 的小 `count` 试图估算全场景专家生成性能。
- **规则：** 性能校准必须直接枚举专家能力矩阵，不得复用 8:1:1 数据集计划的激活单元逻辑。每个校准 case 独立落终态检查点；中断后只重做当前未落盘 case。
- **原因：** 现有数据集计划以 `val_count` 限制普通激活单元；`count=100` 只覆盖 S1/S2，无法代表 S3–S8 长尾。该次 10 条 S9 已占 100 条探测总耗时约 78%，证明容易样本会掩盖复杂场景瓶颈。
- **实现：** `dataset/calibration.py` 与 `scripts/calibrate_dataset.py`；完整契约见 `EXPTRAJ-CAL-001..003`。

## 规划超时先核对场景净空与碰撞裕量契约

- **触发：** 某场景在固定目标附近大量 Hybrid A* 超时，尤其是关闭 `collision_margin` 后立即成功。
- **规则：** 不先降低车辆尺寸、碰撞裕量或栅格分辨率；先计算“物理车体后缘/侧缘到障碍的净空”，确认场景目标是否额外保留了规划膨胀量。车辆尺度、margin 或车辆相对场景版本变化必须进入任务元数据和计划指纹，并使用新校准/数据目录。
- **原因：** S4 旧目标按裸 6m 车长仅留 0.3m，规划器再膨胀 0.2m 后有效操作净空只剩 0.1m；已知 T3 任务因此在 8s 内产生约 2974 万次调用，而保持 0.2m margin、将物理净空改为 0.5m 后快速成功。该症状与“矿场整体太小”或 0.5m 搜索栅格过粗不同。
- **实现与契约：** `sim/scenes.py`、`sim/tasks.py::TaskSampler._build_scene`，见 `SCENE-SCALE-001`。

## 不同 horizon 的模型不能靠截断获得更好开环指标

- **触发：** 在同一验证集比较固定长度和变长轨迹模型，或模型最大 horizon 小于数据中的最长有效轨迹。
- **规则：** ADE 使用目标 mask 的全部有效点，FDE 使用每条目标最后有效点，航向差先环绕到 `[-π,π]`；预测 horizon 小于任一样本有效长度时直接拒绝评估，不能取共同短前缀。
- **原因：** 截断会删除后段和终点误差，使短输出模型出现虚假改善；直接相减航向则会把跨越 `±π` 的小误差放大到接近 `2π`。
- **实现与契约：** `metrics/open_loop.py`、`training/data.py`，见 `MODEL-EVAL-001/002`。

## 车体局部左为正不能直接映射为屏幕右为正

- **触发：** 用车体局部轨迹绘制俯视验收图，尤其是先掉头再小角度转向的原地旋转序列。
- **规则：** 数值局部系固定为前方 `x`、车体左方 `y`、正 `yaw` 表示相对当前车头左转；画面水平轴需反向显示数值 `y`，原地旋转箭头必须从旋转前航向沿有符号累计角绘制。
- **原因：** 数学坐标轴默认屏幕右为正，但车体语义要求正 Left 在画面上仍位于左侧；如果不处理这一显示差异，数值正确的左转会被画成直觉上的右转。
- **实现与契约：** `dataset/inspection.py::_orient_left_positive_axis/_pivot_arrow_points`，见 `EXPTRAJ-DATA-012`。

## 贴近规划超时上限的单次成功不等于稳定准入

- **触发：** 某专家能力单元只有少数 case 成功，且成功耗时贴近规划器墙钟上限。
- **规则：** 将该成功视为边界证据，必须对相同 task 做一次有界复现；如果复现超时，暂不准入正式长任务，不以提高重试次数代替稳定性。
- **原因：** Windows 调度和搜索边界会使贴线 case 在相同 task ID 上跨过截止时间；将这类单元加入数千条生成会放大为不稳定中断。
- **证据与契约：** `S5_crusher/T2` 的 v4 唯一成功贴近 8 秒且同 task 复现超时；v5 唯一倒车成功也先经历一次超时。v5 的 S9/T1、T3 前进各只有 1/2 case 成功，均按不稳定单元排除；见 `EXPTRAJ-DATA-014/016`。

## 远距紧 bay 不可规划先查"起点是否与 bay 轴线对齐"，再查场景净空

- **触发：** 某紧 bay 场景（维修 bay/破碎槽/加油位）的 T2/T3 在 8s 内反复超时或"未找到可行轨迹"，尤其是只改作业道深度/去立柱/放宽净空后仍失败。
- **规则：** 先检查采样出的起点与目标 bay 的横向偏移与随机转角；偏移 ≥4m 或带 ±25–70° 随机转角时，Hybrid A* 的 Reeds-Shepp/履带解析候选会在紧 bay 口全部碰撞、离散搜索难以收敛（25s/10 万节点仍失败）。修复应让远距起点沿 bay 轴线对齐（yaw 平行于 bay、按机动方向从鼻前/鼻后侧接近），这与真实卡车"先对齐再入位"一致；同时检查场景净空是否计入了 `collision_margin`（S7 靠岛位曾因未计入 margin 实际只剩 0.1m）以及破碎槽朝向是否与规格一致（S9 CS0 曾为前进入槽）。
- **原因：** S3/T3 在 v5 正式构建反复耗尽重试，起点对齐后 6/6 且 0.016s；S6/T3 根因是西南岩石堵住接近走廊（去岩石后 12/12 秒过）；S7/T2、S9/T2 同为几何/朝向问题。这些都不是规划器超时参数或栅格分辨率问题。
- **实现与契约：** `sim/tasks.py::TaskSampler._sample_axial_start`；`sim/scenes.py::s3_maintenance/s7_fuel_station/s9_mine_complex`；准入与方向见 `dataset/build.py::_EXPERT_MANEUVER_OVERRIDES`，见 `EXPTRAJ-DATA-017`、`SCENE-SCALE-002`。

## 起点 footprint 自由不代表从起点到车位的入口可达

- **触发：** 结构化车位的起点和目标各自都无碰撞，但相邻占用后仍频繁在同一目标超时，或 T4 明明存在可达候选却先耗尽单 case 预算。
- **规则：** 对轴线入位任务必须检查目标到起点的整段车辆外廓走廊，而非只检查远端起点；相邻占用时按请求方向过滤入口被占用车辆截断的目标。固定车位航向不能同时表示前进入位和倒车入位，普通垂直/斜列车位的倒车目标应翻转 180°。T4 的候选顺序必须先包含用于构造起点的参考目标。
- **原因：** v6 的 408 例无重采复核暴露 S7/T2 9/12、S9/T2 7/12、S9/T5 8/12，另有 S3/S9 T4 超预算；失败目标的端点均自由，但入口侧被相邻车辆截断、目标航向与请求机动相反，或先尝试了与起点不匹配的候选。修复后相同 34 单元×12 例达到 408/408。
- **实现与契约：** `sim/tasks.py::TaskSampler._axial_clearance_limit/_orient_spots_for_maneuver/_candidate_spots`，`sim/scenes.py` 的普通车位 kind/基准航向，见 `EXPTRAJ-DATA-018`。
